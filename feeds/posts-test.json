{
  "version": "https://jsonfeed.org/version/1",
  "title": "Ultimate Jekyll Blog",
  "home_page_url": "https://template.itwcreativeworks.com/blog",
  "feed_url": "https://template.itwcreativeworks.com/feeds/posts-test.json",
  "description": "Default description.",
  "generator": "Jekyll 4.3.2",
  "language": "en",
  "favicon": "",
  "items": [
      {"id": "https://template.itwcreativeworks.com/blog/example-post-7",
        "url": "https://template.itwcreativeworks.com/blog/example-post-7",
        "title": "Example Post 7",
        "date_published": "2024-01-07T00:00:00.000Z",
        "image": "",
        "content_text": "Title inside post 7 This is a wonderful paragrah inside a post! This is post #7 with ID 1722471527.  Before ads                                     Between ads                                     After ads ",
        "content_html": "<h2 id=\"title-inside-post-7\">Title inside post 7</h2> <p>This is a wonderful paragrah inside a post! This is post #7 with ID 1722471527.</p>  <p>Before ads</p>  <!-- Adunit - Adsense In-article Ad --><p>Between ads</p>  <!-- Adunit - Adsense In-article Ad --><p>After ads</p> ",

        "test_1": "<h2 id="title-inside-post-7">Title inside post 7</h2>
<p>This is a wonderful paragrah inside a post! This is post #7 with ID 1722471527.</p>

<p>Before ads</p>

<!-- Adunit - Adsense In-article Ad -->
<ad-unit><div class="uj-vert-unit" data-plan-id="basic" data-plan-visibility="visible">
      <ins class="adsbygoogle" style="display:block; text-align:center; " data-ad-layout="in-article" data-ad-format="fluid" data-ad-client="ca-pub-7372939085773564" data-ad-slot="7329067043"></ins>
      <script>
          (adsbygoogle = window.adsbygoogle || []).push({});
      </script>
    </div>

    <div id="vert-template" data-template="
  &lt;iframe
    id=&quot;{id}&quot;
    name=&quot;{id}&quot;
    class=&quot;lazyload&quot;
    style=&quot;max-height: 300px;&quot;
    sandbox=&quot;allow-forms allow-popups allow-popups-to-escape-sandbox allow-same-origin allow-scripts allow-top-navigation-by-user-activation&quot;
    width=&quot;100%&quot; height=&quot;100%&quot;
    frameborder=&quot;0&quot; marginwidth=&quot;0&quot; marginheight=&quot;0&quot; vspace=&quot;0&quot; hspace=&quot;0&quot; allowtransparency=&quot;true&quot; scrolling=&quot;no&quot;
    data-src=&quot;{src}&quot;
  &gt;&lt;/iframe&gt;
">
</div>

<script async="" defer="" type="text/javascript">
  Manager.ready(function () {
    var select = Manager.dom().select;
    var $adblockerDetector = select('#uj-antivert-detector');

    var $iframeVertTemplate = select('#vert-template').get(0).getAttribute('data-template');

    var qsDebug = new URLSearchParams(window.location.search).get('debug');

    var slotId = new Date().getTime();

    // Selectors
    var vertSlotsSelector = '.uj-vert-unit';
    var vertSlotsSelectorLoaded = '.uj-vert-unit ins[data-ad-status]';
    var vertSlotsSelectorUnfilled = '.uj-vert-unit ins[data-ad-status="unfilled"]';

    // Set the anti-vert detector script
    /*
      /bottomads.js
      /content-ads.js
      /core-ads.js
      /insertads.js
      /jsads.js
      /left_ads.js
      /nativeads.js
      /openads.js
      /popupads.js
      /ads/ad-script.js?ad_network=123&tracking_id=abc#banner_ad
    */
    // var scriptUrl = '/assets/js/taboola_ads.js';
    // var scriptUrl = 'assets/js/pagead.js';
    var scriptUrl = '/assets/js/popupads.js?t1=/ads/native.js&t2=/nativeads.js&t3=/content-ads.js&cb=' + Manager.properties.global.cacheBreaker;

    // Check if being handled already
    if (window._ujVertCheckerStarted) {
      return;
    }
    window._ujVertCheckerStarted = true;

    // Message Handler
    function messageHandler(event) {
      var message = event.data || {};
      var command = message.command || '';
      var payload = message.payload || {};

      // Log
      console.log('Message', command, message, event.source);

      // Handle the message
      if (command === 'uj-vert-unit:set-dimensions') {
        // Get the iframe
        var $iframe = select('#uj-vert-' + payload.id).get(0);

        // Set the dimensions
        $iframe.style.height = payload.height + 'px';
      } else if (command === 'uj-vert-unit:click') {
        // Navigate to the URL
        window.location.href = payload.url;
      }
    }

    // Fill the units
    function fillUnits() {
      var $vertSlots = select(vertSlotsSelector);
      var adURL = qsDebug === 'true'
        ? 'http://localhost:4001/verts/main'
        : 'https://promo-server.itwcreativeworks.com/verts/main'

      adURL = new URL(adURL);

      adURL.searchParams.set('parentURL', window.location.href);

      // Load the ad units
      $vertSlots.each(function ($el) {
        var isFilled = $el.matches('[data-ad-status="filled"]');
        var finalURL = new URL(adURL.toString());

        finalURL.searchParams.set('frameId', slotId);

        finalURL = finalURL.toString();

        if (isFilled) {
          return;
        }

        console.log('Loading ad unit', $el, finalURL);

        $el.innerHTML = $iframeVertTemplate
          .replace(/{id}/g, 'uj-vert-' + slotId++)
          .replace(/{src}/g, finalURL)
      })
    }

    window.addEventListener('message', messageHandler, false);

    // Load the antivert checker
    Manager.dom().loadScript({src: scriptUrl})
    .then(function (e) {
      console.log('Loaded antivert checker', e);
    })
    .catch(function (e) {
      console.error('Failed to load antivert checker', e);
    })
    .finally(function () {
      var filledChecker = setInterval(function () {
        // Check if there are unfilled units
        var isOGVertsLoaded = select(vertSlotsSelectorLoaded).elements.exists;
        var isUnfilledVertSlots = select(vertSlotsSelectorUnfilled).elements.exists;

        // Set the global adblocker enabled flag
        Manager.properties.global.isAdblockerEnabled = !select('#uj-antivert-detector').elements.exists;

        var needsCustomVerts = isUnfilledVertSlots
          || Manager.properties.global.isAdblockerEnabled
          || Manager.properties.meta.environment === 'development';

        console.log(
          'isAdblockerEnabled', Manager.properties.global.isAdblockerEnabled,
          'isOGVertsLoaded', isOGVertsLoaded,
          'isUnfilledVertSlots', isUnfilledVertSlots,
          'needsCustomVerts', needsCustomVerts,
        );

        if (!Manager.properties.global.isAdblockerEnabled && !isOGVertsLoaded) {
          return;
        }

        if (isOGVertsLoaded && !isUnfilledVertSlots) {
          return clearInterval(filledChecker);
        }

        if (needsCustomVerts) {
          clearInterval(filledChecker);
          fillUnits();
        } else {
          return;
        }
      }, 300)
    })
  })
</script>

  </ad-unit>

<p>Between ads</p>

<!-- Adunit - Adsense In-article Ad -->
<ad-unit><div class="uj-vert-unit" data-plan-id="basic" data-plan-visibility="visible">
      <ins class="adsbygoogle" style="display:block; text-align:center; " data-ad-layout="in-article" data-ad-format="fluid" data-ad-client="ca-pub-7372939085773564" data-ad-slot="7329067043"></ins>
      <script>
          (adsbygoogle = window.adsbygoogle || []).push({});
      </script>
    </div>

    <div id="vert-template" data-template="
  &lt;iframe
    id=&quot;{id}&quot;
    name=&quot;{id}&quot;
    class=&quot;lazyload&quot;
    style=&quot;max-height: 300px;&quot;
    sandbox=&quot;allow-forms allow-popups allow-popups-to-escape-sandbox allow-same-origin allow-scripts allow-top-navigation-by-user-activation&quot;
    width=&quot;100%&quot; height=&quot;100%&quot;
    frameborder=&quot;0&quot; marginwidth=&quot;0&quot; marginheight=&quot;0&quot; vspace=&quot;0&quot; hspace=&quot;0&quot; allowtransparency=&quot;true&quot; scrolling=&quot;no&quot;
    data-src=&quot;{src}&quot;
  &gt;&lt;/iframe&gt;
">
</div>

<script async="" defer="" type="text/javascript">
  Manager.ready(function () {
    var select = Manager.dom().select;
    var $adblockerDetector = select('#uj-antivert-detector');

    var $iframeVertTemplate = select('#vert-template').get(0).getAttribute('data-template');

    var qsDebug = new URLSearchParams(window.location.search).get('debug');

    var slotId = new Date().getTime();

    // Selectors
    var vertSlotsSelector = '.uj-vert-unit';
    var vertSlotsSelectorLoaded = '.uj-vert-unit ins[data-ad-status]';
    var vertSlotsSelectorUnfilled = '.uj-vert-unit ins[data-ad-status="unfilled"]';

    // Set the anti-vert detector script
    /*
      /bottomads.js
      /content-ads.js
      /core-ads.js
      /insertads.js
      /jsads.js
      /left_ads.js
      /nativeads.js
      /openads.js
      /popupads.js
      /ads/ad-script.js?ad_network=123&tracking_id=abc#banner_ad
    */
    // var scriptUrl = '/assets/js/taboola_ads.js';
    // var scriptUrl = 'assets/js/pagead.js';
    var scriptUrl = '/assets/js/popupads.js?t1=/ads/native.js&t2=/nativeads.js&t3=/content-ads.js&cb=' + Manager.properties.global.cacheBreaker;

    // Check if being handled already
    if (window._ujVertCheckerStarted) {
      return;
    }
    window._ujVertCheckerStarted = true;

    // Message Handler
    function messageHandler(event) {
      var message = event.data || {};
      var command = message.command || '';
      var payload = message.payload || {};

      // Log
      console.log('Message', command, message, event.source);

      // Handle the message
      if (command === 'uj-vert-unit:set-dimensions') {
        // Get the iframe
        var $iframe = select('#uj-vert-' + payload.id).get(0);

        // Set the dimensions
        $iframe.style.height = payload.height + 'px';
      } else if (command === 'uj-vert-unit:click') {
        // Navigate to the URL
        window.location.href = payload.url;
      }
    }

    // Fill the units
    function fillUnits() {
      var $vertSlots = select(vertSlotsSelector);
      var adURL = qsDebug === 'true'
        ? 'http://localhost:4001/verts/main'
        : 'https://promo-server.itwcreativeworks.com/verts/main'

      adURL = new URL(adURL);

      adURL.searchParams.set('parentURL', window.location.href);

      // Load the ad units
      $vertSlots.each(function ($el) {
        var isFilled = $el.matches('[data-ad-status="filled"]');
        var finalURL = new URL(adURL.toString());

        finalURL.searchParams.set('frameId', slotId);

        finalURL = finalURL.toString();

        if (isFilled) {
          return;
        }

        console.log('Loading ad unit', $el, finalURL);

        $el.innerHTML = $iframeVertTemplate
          .replace(/{id}/g, 'uj-vert-' + slotId++)
          .replace(/{src}/g, finalURL)
      })
    }

    window.addEventListener('message', messageHandler, false);

    // Load the antivert checker
    Manager.dom().loadScript({src: scriptUrl})
    .then(function (e) {
      console.log('Loaded antivert checker', e);
    })
    .catch(function (e) {
      console.error('Failed to load antivert checker', e);
    })
    .finally(function () {
      var filledChecker = setInterval(function () {
        // Check if there are unfilled units
        var isOGVertsLoaded = select(vertSlotsSelectorLoaded).elements.exists;
        var isUnfilledVertSlots = select(vertSlotsSelectorUnfilled).elements.exists;

        // Set the global adblocker enabled flag
        Manager.properties.global.isAdblockerEnabled = !select('#uj-antivert-detector').elements.exists;

        var needsCustomVerts = isUnfilledVertSlots
          || Manager.properties.global.isAdblockerEnabled
          || Manager.properties.meta.environment === 'development';

        console.log(
          'isAdblockerEnabled', Manager.properties.global.isAdblockerEnabled,
          'isOGVertsLoaded', isOGVertsLoaded,
          'isUnfilledVertSlots', isUnfilledVertSlots,
          'needsCustomVerts', needsCustomVerts,
        );

        if (!Manager.properties.global.isAdblockerEnabled && !isOGVertsLoaded) {
          return;
        }

        if (isOGVertsLoaded && !isUnfilledVertSlots) {
          return clearInterval(filledChecker);
        }

        if (needsCustomVerts) {
          clearInterval(filledChecker);
          fillUnits();
        } else {
          return;
        }
      }, 300)
    })
  })
</script>

  </ad-unit>

<p>After ads</p>
",
        "test_1_2": "<h2 id="title-inside-post-7">Title inside post 7</h2>
<p>This is a wonderful paragrah inside a post! This is post #7 with ID 1722471527.</p>

<p>Before ads</p>

<!-- Adunit - Adsense In-article Ad --><p>Between ads</p>

<!-- Adunit - Adsense In-article Ad --><p>After ads</p>
",
        "test_1_3": "<h2 id=\"title-inside-post-7\">Title inside post 7</h2> <p>This is a wonderful paragrah inside a post! This is post #7 with ID 1722471527.</p>  <p>Before ads</p>  <!-- Adunit - Adsense In-article Ad --> <p>Between ads</p>  <!-- Adunit - Adsense In-article Ad --> <p>After ads</p> ",


        "test_2": "<h2 id="title-inside-post-7">Title inside post 7</h2>
<p>This is a wonderful paragrah inside a post! This is post #7 with ID 1722471527.</p>

<p>Before ads</p>

<!-- Adunit - Adsense In-article Ad -->
<ad-unit><div class="uj-vert-unit" data-plan-id="basic" data-plan-visibility="visible">
      <ins class="adsbygoogle" style="display:block; text-align:center; " data-ad-layout="in-article" data-ad-format="fluid" data-ad-client="ca-pub-7372939085773564" data-ad-slot="7329067043"></ins>
      <script>
          (adsbygoogle = window.adsbygoogle || []).push({});
      </script>
    </div>

    <div id="vert-template" data-template="
  &lt;iframe
    id=&quot;{id}&quot;
    name=&quot;{id}&quot;
    class=&quot;lazyload&quot;
    style=&quot;max-height: 300px;&quot;
    sandbox=&quot;allow-forms allow-popups allow-popups-to-escape-sandbox allow-same-origin allow-scripts allow-top-navigation-by-user-activation&quot;
    width=&quot;100%&quot; height=&quot;100%&quot;
    frameborder=&quot;0&quot; marginwidth=&quot;0&quot; marginheight=&quot;0&quot; vspace=&quot;0&quot; hspace=&quot;0&quot; allowtransparency=&quot;true&quot; scrolling=&quot;no&quot;
    data-src=&quot;{src}&quot;
  &gt;&lt;/iframe&gt;
">
</div>

<script async="" defer="" type="text/javascript">
  Manager.ready(function () {
    var select = Manager.dom().select;
    var $adblockerDetector = select('#uj-antivert-detector');

    var $iframeVertTemplate = select('#vert-template').get(0).getAttribute('data-template');

    var qsDebug = new URLSearchParams(window.location.search).get('debug');

    var slotId = new Date().getTime();

    // Selectors
    var vertSlotsSelector = '.uj-vert-unit';
    var vertSlotsSelectorLoaded = '.uj-vert-unit ins[data-ad-status]';
    var vertSlotsSelectorUnfilled = '.uj-vert-unit ins[data-ad-status="unfilled"]';

    // Set the anti-vert detector script
    /*
      /bottomads.js
      /content-ads.js
      /core-ads.js
      /insertads.js
      /jsads.js
      /left_ads.js
      /nativeads.js
      /openads.js
      /popupads.js
      /ads/ad-script.js?ad_network=123&tracking_id=abc#banner_ad
    */
    // var scriptUrl = '/assets/js/taboola_ads.js';
    // var scriptUrl = 'assets/js/pagead.js';
    var scriptUrl = '/assets/js/popupads.js?t1=/ads/native.js&t2=/nativeads.js&t3=/content-ads.js&cb=' + Manager.properties.global.cacheBreaker;

    // Check if being handled already
    if (window._ujVertCheckerStarted) {
      return;
    }
    window._ujVertCheckerStarted = true;

    // Message Handler
    function messageHandler(event) {
      var message = event.data || {};
      var command = message.command || '';
      var payload = message.payload || {};

      // Log
      console.log('Message', command, message, event.source);

      // Handle the message
      if (command === 'uj-vert-unit:set-dimensions') {
        // Get the iframe
        var $iframe = select('#uj-vert-' + payload.id).get(0);

        // Set the dimensions
        $iframe.style.height = payload.height + 'px';
      } else if (command === 'uj-vert-unit:click') {
        // Navigate to the URL
        window.location.href = payload.url;
      }
    }

    // Fill the units
    function fillUnits() {
      var $vertSlots = select(vertSlotsSelector);
      var adURL = qsDebug === 'true'
        ? 'http://localhost:4001/verts/main'
        : 'https://promo-server.itwcreativeworks.com/verts/main'

      adURL = new URL(adURL);

      adURL.searchParams.set('parentURL', window.location.href);

      // Load the ad units
      $vertSlots.each(function ($el) {
        var isFilled = $el.matches('[data-ad-status="filled"]');
        var finalURL = new URL(adURL.toString());

        finalURL.searchParams.set('frameId', slotId);

        finalURL = finalURL.toString();

        if (isFilled) {
          return;
        }

        console.log('Loading ad unit', $el, finalURL);

        $el.innerHTML = $iframeVertTemplate
          .replace(/{id}/g, 'uj-vert-' + slotId++)
          .replace(/{src}/g, finalURL)
      })
    }

    window.addEventListener('message', messageHandler, false);

    // Load the antivert checker
    Manager.dom().loadScript({src: scriptUrl})
    .then(function (e) {
      console.log('Loaded antivert checker', e);
    })
    .catch(function (e) {
      console.error('Failed to load antivert checker', e);
    })
    .finally(function () {
      var filledChecker = setInterval(function () {
        // Check if there are unfilled units
        var isOGVertsLoaded = select(vertSlotsSelectorLoaded).elements.exists;
        var isUnfilledVertSlots = select(vertSlotsSelectorUnfilled).elements.exists;

        // Set the global adblocker enabled flag
        Manager.properties.global.isAdblockerEnabled = !select('#uj-antivert-detector').elements.exists;

        var needsCustomVerts = isUnfilledVertSlots
          || Manager.properties.global.isAdblockerEnabled
          || Manager.properties.meta.environment === 'development';

        console.log(
          'isAdblockerEnabled', Manager.properties.global.isAdblockerEnabled,
          'isOGVertsLoaded', isOGVertsLoaded,
          'isUnfilledVertSlots', isUnfilledVertSlots,
          'needsCustomVerts', needsCustomVerts,
        );

        if (!Manager.properties.global.isAdblockerEnabled && !isOGVertsLoaded) {
          return;
        }

        if (isOGVertsLoaded && !isUnfilledVertSlots) {
          return clearInterval(filledChecker);
        }

        if (needsCustomVerts) {
          clearInterval(filledChecker);
          fillUnits();
        } else {
          return;
        }
      }, 300)
    })
  })
</script>

  </ad-unit>

<p>Between ads</p>

<!-- Adunit - Adsense In-article Ad -->
<ad-unit><div class="uj-vert-unit" data-plan-id="basic" data-plan-visibility="visible">
      <ins class="adsbygoogle" style="display:block; text-align:center; " data-ad-layout="in-article" data-ad-format="fluid" data-ad-client="ca-pub-7372939085773564" data-ad-slot="7329067043"></ins>
      <script>
          (adsbygoogle = window.adsbygoogle || []).push({});
      </script>
    </div>

    <div id="vert-template" data-template="
  &lt;iframe
    id=&quot;{id}&quot;
    name=&quot;{id}&quot;
    class=&quot;lazyload&quot;
    style=&quot;max-height: 300px;&quot;
    sandbox=&quot;allow-forms allow-popups allow-popups-to-escape-sandbox allow-same-origin allow-scripts allow-top-navigation-by-user-activation&quot;
    width=&quot;100%&quot; height=&quot;100%&quot;
    frameborder=&quot;0&quot; marginwidth=&quot;0&quot; marginheight=&quot;0&quot; vspace=&quot;0&quot; hspace=&quot;0&quot; allowtransparency=&quot;true&quot; scrolling=&quot;no&quot;
    data-src=&quot;{src}&quot;
  &gt;&lt;/iframe&gt;
">
</div>

<script async="" defer="" type="text/javascript">
  Manager.ready(function () {
    var select = Manager.dom().select;
    var $adblockerDetector = select('#uj-antivert-detector');

    var $iframeVertTemplate = select('#vert-template').get(0).getAttribute('data-template');

    var qsDebug = new URLSearchParams(window.location.search).get('debug');

    var slotId = new Date().getTime();

    // Selectors
    var vertSlotsSelector = '.uj-vert-unit';
    var vertSlotsSelectorLoaded = '.uj-vert-unit ins[data-ad-status]';
    var vertSlotsSelectorUnfilled = '.uj-vert-unit ins[data-ad-status="unfilled"]';

    // Set the anti-vert detector script
    /*
      /bottomads.js
      /content-ads.js
      /core-ads.js
      /insertads.js
      /jsads.js
      /left_ads.js
      /nativeads.js
      /openads.js
      /popupads.js
      /ads/ad-script.js?ad_network=123&tracking_id=abc#banner_ad
    */
    // var scriptUrl = '/assets/js/taboola_ads.js';
    // var scriptUrl = 'assets/js/pagead.js';
    var scriptUrl = '/assets/js/popupads.js?t1=/ads/native.js&t2=/nativeads.js&t3=/content-ads.js&cb=' + Manager.properties.global.cacheBreaker;

    // Check if being handled already
    if (window._ujVertCheckerStarted) {
      return;
    }
    window._ujVertCheckerStarted = true;

    // Message Handler
    function messageHandler(event) {
      var message = event.data || {};
      var command = message.command || '';
      var payload = message.payload || {};

      // Log
      console.log('Message', command, message, event.source);

      // Handle the message
      if (command === 'uj-vert-unit:set-dimensions') {
        // Get the iframe
        var $iframe = select('#uj-vert-' + payload.id).get(0);

        // Set the dimensions
        $iframe.style.height = payload.height + 'px';
      } else if (command === 'uj-vert-unit:click') {
        // Navigate to the URL
        window.location.href = payload.url;
      }
    }

    // Fill the units
    function fillUnits() {
      var $vertSlots = select(vertSlotsSelector);
      var adURL = qsDebug === 'true'
        ? 'http://localhost:4001/verts/main'
        : 'https://promo-server.itwcreativeworks.com/verts/main'

      adURL = new URL(adURL);

      adURL.searchParams.set('parentURL', window.location.href);

      // Load the ad units
      $vertSlots.each(function ($el) {
        var isFilled = $el.matches('[data-ad-status="filled"]');
        var finalURL = new URL(adURL.toString());

        finalURL.searchParams.set('frameId', slotId);

        finalURL = finalURL.toString();

        if (isFilled) {
          return;
        }

        console.log('Loading ad unit', $el, finalURL);

        $el.innerHTML = $iframeVertTemplate
          .replace(/{id}/g, 'uj-vert-' + slotId++)
          .replace(/{src}/g, finalURL)
      })
    }

    window.addEventListener('message', messageHandler, false);

    // Load the antivert checker
    Manager.dom().loadScript({src: scriptUrl})
    .then(function (e) {
      console.log('Loaded antivert checker', e);
    })
    .catch(function (e) {
      console.error('Failed to load antivert checker', e);
    })
    .finally(function () {
      var filledChecker = setInterval(function () {
        // Check if there are unfilled units
        var isOGVertsLoaded = select(vertSlotsSelectorLoaded).elements.exists;
        var isUnfilledVertSlots = select(vertSlotsSelectorUnfilled).elements.exists;

        // Set the global adblocker enabled flag
        Manager.properties.global.isAdblockerEnabled = !select('#uj-antivert-detector').elements.exists;

        var needsCustomVerts = isUnfilledVertSlots
          || Manager.properties.global.isAdblockerEnabled
          || Manager.properties.meta.environment === 'development';

        console.log(
          'isAdblockerEnabled', Manager.properties.global.isAdblockerEnabled,
          'isOGVertsLoaded', isOGVertsLoaded,
          'isUnfilledVertSlots', isUnfilledVertSlots,
          'needsCustomVerts', needsCustomVerts,
        );

        if (!Manager.properties.global.isAdblockerEnabled && !isOGVertsLoaded) {
          return;
        }

        if (isOGVertsLoaded && !isUnfilledVertSlots) {
          return clearInterval(filledChecker);
        }

        if (needsCustomVerts) {
          clearInterval(filledChecker);
          fillUnits();
        } else {
          return;
        }
      }, 300)
    })
  })
</script>

  </ad-unit>

<p>After ads</p>
",
        "test_3": "<h2 id="title-inside-post-7">Title inside post 7</h2>
<p>This is a wonderful paragrah inside a post! This is post #7 with ID 1722471527.</p>

<p>Before ads</p>

<!-- Adunit - Adsense In-article Ad -->
<ad-unit><div class="uj-vert-unit" data-plan-id="basic" data-plan-visibility="visible">
      <ins class="adsbygoogle" style="display:block; text-align:center; " data-ad-layout="in-article" data-ad-format="fluid" data-ad-client="ca-pub-7372939085773564" data-ad-slot="7329067043"></ins>
      <script>
          (adsbygoogle = window.adsbygoogle || []).push({});
      </script>
    </div>

    <div id="vert-template" data-template="
  &lt;iframe
    id=&quot;{id}&quot;
    name=&quot;{id}&quot;
    class=&quot;lazyload&quot;
    style=&quot;max-height: 300px;&quot;
    sandbox=&quot;allow-forms allow-popups allow-popups-to-escape-sandbox allow-same-origin allow-scripts allow-top-navigation-by-user-activation&quot;
    width=&quot;100%&quot; height=&quot;100%&quot;
    frameborder=&quot;0&quot; marginwidth=&quot;0&quot; marginheight=&quot;0&quot; vspace=&quot;0&quot; hspace=&quot;0&quot; allowtransparency=&quot;true&quot; scrolling=&quot;no&quot;
    data-src=&quot;{src}&quot;
  &gt;&lt;/iframe&gt;
">
</div>

<script async="" defer="" type="text/javascript">
  Manager.ready(function () {
    var select = Manager.dom().select;
    var $adblockerDetector = select('#uj-antivert-detector');

    var $iframeVertTemplate = select('#vert-template').get(0).getAttribute('data-template');

    var qsDebug = new URLSearchParams(window.location.search).get('debug');

    var slotId = new Date().getTime();

    // Selectors
    var vertSlotsSelector = '.uj-vert-unit';
    var vertSlotsSelectorLoaded = '.uj-vert-unit ins[data-ad-status]';
    var vertSlotsSelectorUnfilled = '.uj-vert-unit ins[data-ad-status="unfilled"]';

    // Set the anti-vert detector script
    /*
      /bottomads.js
      /content-ads.js
      /core-ads.js
      /insertads.js
      /jsads.js
      /left_ads.js
      /nativeads.js
      /openads.js
      /popupads.js
      /ads/ad-script.js?ad_network=123&tracking_id=abc#banner_ad
    */
    // var scriptUrl = '/assets/js/taboola_ads.js';
    // var scriptUrl = 'assets/js/pagead.js';
    var scriptUrl = '/assets/js/popupads.js?t1=/ads/native.js&t2=/nativeads.js&t3=/content-ads.js&cb=' + Manager.properties.global.cacheBreaker;

    // Check if being handled already
    if (window._ujVertCheckerStarted) {
      return;
    }
    window._ujVertCheckerStarted = true;

    // Message Handler
    function messageHandler(event) {
      var message = event.data || {};
      var command = message.command || '';
      var payload = message.payload || {};

      // Log
      console.log('Message', command, message, event.source);

      // Handle the message
      if (command === 'uj-vert-unit:set-dimensions') {
        // Get the iframe
        var $iframe = select('#uj-vert-' + payload.id).get(0);

        // Set the dimensions
        $iframe.style.height = payload.height + 'px';
      } else if (command === 'uj-vert-unit:click') {
        // Navigate to the URL
        window.location.href = payload.url;
      }
    }

    // Fill the units
    function fillUnits() {
      var $vertSlots = select(vertSlotsSelector);
      var adURL = qsDebug === 'true'
        ? 'http://localhost:4001/verts/main'
        : 'https://promo-server.itwcreativeworks.com/verts/main'

      adURL = new URL(adURL);

      adURL.searchParams.set('parentURL', window.location.href);

      // Load the ad units
      $vertSlots.each(function ($el) {
        var isFilled = $el.matches('[data-ad-status="filled"]');
        var finalURL = new URL(adURL.toString());

        finalURL.searchParams.set('frameId', slotId);

        finalURL = finalURL.toString();

        if (isFilled) {
          return;
        }

        console.log('Loading ad unit', $el, finalURL);

        $el.innerHTML = $iframeVertTemplate
          .replace(/{id}/g, 'uj-vert-' + slotId++)
          .replace(/{src}/g, finalURL)
      })
    }

    window.addEventListener('message', messageHandler, false);

    // Load the antivert checker
    Manager.dom().loadScript({src: scriptUrl})
    .then(function (e) {
      console.log('Loaded antivert checker', e);
    })
    .catch(function (e) {
      console.error('Failed to load antivert checker', e);
    })
    .finally(function () {
      var filledChecker = setInterval(function () {
        // Check if there are unfilled units
        var isOGVertsLoaded = select(vertSlotsSelectorLoaded).elements.exists;
        var isUnfilledVertSlots = select(vertSlotsSelectorUnfilled).elements.exists;

        // Set the global adblocker enabled flag
        Manager.properties.global.isAdblockerEnabled = !select('#uj-antivert-detector').elements.exists;

        var needsCustomVerts = isUnfilledVertSlots
          || Manager.properties.global.isAdblockerEnabled
          || Manager.properties.meta.environment === 'development';

        console.log(
          'isAdblockerEnabled', Manager.properties.global.isAdblockerEnabled,
          'isOGVertsLoaded', isOGVertsLoaded,
          'isUnfilledVertSlots', isUnfilledVertSlots,
          'needsCustomVerts', needsCustomVerts,
        );

        if (!Manager.properties.global.isAdblockerEnabled && !isOGVertsLoaded) {
          return;
        }

        if (isOGVertsLoaded && !isUnfilledVertSlots) {
          return clearInterval(filledChecker);
        }

        if (needsCustomVerts) {
          clearInterval(filledChecker);
          fillUnits();
        } else {
          return;
        }
      }, 300)
    })
  })
</script>

  </ad-unit>

<p>Between ads</p>

<!-- Adunit - Adsense In-article Ad -->
<ad-unit><div class="uj-vert-unit" data-plan-id="basic" data-plan-visibility="visible">
      <ins class="adsbygoogle" style="display:block; text-align:center; " data-ad-layout="in-article" data-ad-format="fluid" data-ad-client="ca-pub-7372939085773564" data-ad-slot="7329067043"></ins>
      <script>
          (adsbygoogle = window.adsbygoogle || []).push({});
      </script>
    </div>

    <div id="vert-template" data-template="
  &lt;iframe
    id=&quot;{id}&quot;
    name=&quot;{id}&quot;
    class=&quot;lazyload&quot;
    style=&quot;max-height: 300px;&quot;
    sandbox=&quot;allow-forms allow-popups allow-popups-to-escape-sandbox allow-same-origin allow-scripts allow-top-navigation-by-user-activation&quot;
    width=&quot;100%&quot; height=&quot;100%&quot;
    frameborder=&quot;0&quot; marginwidth=&quot;0&quot; marginheight=&quot;0&quot; vspace=&quot;0&quot; hspace=&quot;0&quot; allowtransparency=&quot;true&quot; scrolling=&quot;no&quot;
    data-src=&quot;{src}&quot;
  &gt;&lt;/iframe&gt;
">
</div>

<script async="" defer="" type="text/javascript">
  Manager.ready(function () {
    var select = Manager.dom().select;
    var $adblockerDetector = select('#uj-antivert-detector');

    var $iframeVertTemplate = select('#vert-template').get(0).getAttribute('data-template');

    var qsDebug = new URLSearchParams(window.location.search).get('debug');

    var slotId = new Date().getTime();

    // Selectors
    var vertSlotsSelector = '.uj-vert-unit';
    var vertSlotsSelectorLoaded = '.uj-vert-unit ins[data-ad-status]';
    var vertSlotsSelectorUnfilled = '.uj-vert-unit ins[data-ad-status="unfilled"]';

    // Set the anti-vert detector script
    /*
      /bottomads.js
      /content-ads.js
      /core-ads.js
      /insertads.js
      /jsads.js
      /left_ads.js
      /nativeads.js
      /openads.js
      /popupads.js
      /ads/ad-script.js?ad_network=123&tracking_id=abc#banner_ad
    */
    // var scriptUrl = '/assets/js/taboola_ads.js';
    // var scriptUrl = 'assets/js/pagead.js';
    var scriptUrl = '/assets/js/popupads.js?t1=/ads/native.js&t2=/nativeads.js&t3=/content-ads.js&cb=' + Manager.properties.global.cacheBreaker;

    // Check if being handled already
    if (window._ujVertCheckerStarted) {
      return;
    }
    window._ujVertCheckerStarted = true;

    // Message Handler
    function messageHandler(event) {
      var message = event.data || {};
      var command = message.command || '';
      var payload = message.payload || {};

      // Log
      console.log('Message', command, message, event.source);

      // Handle the message
      if (command === 'uj-vert-unit:set-dimensions') {
        // Get the iframe
        var $iframe = select('#uj-vert-' + payload.id).get(0);

        // Set the dimensions
        $iframe.style.height = payload.height + 'px';
      } else if (command === 'uj-vert-unit:click') {
        // Navigate to the URL
        window.location.href = payload.url;
      }
    }

    // Fill the units
    function fillUnits() {
      var $vertSlots = select(vertSlotsSelector);
      var adURL = qsDebug === 'true'
        ? 'http://localhost:4001/verts/main'
        : 'https://promo-server.itwcreativeworks.com/verts/main'

      adURL = new URL(adURL);

      adURL.searchParams.set('parentURL', window.location.href);

      // Load the ad units
      $vertSlots.each(function ($el) {
        var isFilled = $el.matches('[data-ad-status="filled"]');
        var finalURL = new URL(adURL.toString());

        finalURL.searchParams.set('frameId', slotId);

        finalURL = finalURL.toString();

        if (isFilled) {
          return;
        }

        console.log('Loading ad unit', $el, finalURL);

        $el.innerHTML = $iframeVertTemplate
          .replace(/{id}/g, 'uj-vert-' + slotId++)
          .replace(/{src}/g, finalURL)
      })
    }

    window.addEventListener('message', messageHandler, false);

    // Load the antivert checker
    Manager.dom().loadScript({src: scriptUrl})
    .then(function (e) {
      console.log('Loaded antivert checker', e);
    })
    .catch(function (e) {
      console.error('Failed to load antivert checker', e);
    })
    .finally(function () {
      var filledChecker = setInterval(function () {
        // Check if there are unfilled units
        var isOGVertsLoaded = select(vertSlotsSelectorLoaded).elements.exists;
        var isUnfilledVertSlots = select(vertSlotsSelectorUnfilled).elements.exists;

        // Set the global adblocker enabled flag
        Manager.properties.global.isAdblockerEnabled = !select('#uj-antivert-detector').elements.exists;

        var needsCustomVerts = isUnfilledVertSlots
          || Manager.properties.global.isAdblockerEnabled
          || Manager.properties.meta.environment === 'development';

        console.log(
          'isAdblockerEnabled', Manager.properties.global.isAdblockerEnabled,
          'isOGVertsLoaded', isOGVertsLoaded,
          'isUnfilledVertSlots', isUnfilledVertSlots,
          'needsCustomVerts', needsCustomVerts,
        );

        if (!Manager.properties.global.isAdblockerEnabled && !isOGVertsLoaded) {
          return;
        }

        if (isOGVertsLoaded && !isUnfilledVertSlots) {
          return clearInterval(filledChecker);
        }

        if (needsCustomVerts) {
          clearInterval(filledChecker);
          fillUnits();
        } else {
          return;
        }
      }, 300)
    })
  })
</script>

  </ad-unit>

<p>After ads</p>
",
        "test_4": "<h2 id="title-inside-post-7">Title inside post 7</h2>
<p>This is a wonderful paragrah inside a post! This is post #7 with ID 1722471527.</p>

<p>Before ads</p>

<!-- Adunit - Adsense In-article Ad --><p>Between ads</p>

<!-- Adunit - Adsense In-article Ad --><p>After ads</p>
",
        "test_5": "<h2 id=\"title-inside-post-7\">Title inside post 7</h2>\n<p>This is a wonderful paragrah inside a post! This is post #7 with ID 1722471527.</p>\n\n<p>Before ads</p>\n\n<!-- Adunit - Adsense In-article Ad --><p>Between ads</p>\n\n<!-- Adunit - Adsense In-article Ad --><p>After ads</p>\n",

        "test_6": "<h2 id="title-inside-post-7">Title inside post 7</h2>
<p>This is a wonderful paragrah inside a post! This is post #7 with ID 1722471527.</p>

<p>Before ads</p>

<!-- Adunit - Adsense In-article Ad -->
<ad-unit><div class="uj-vert-unit" data-plan-id="basic" data-plan-visibility="visible">
      <ins class="adsbygoogle" style="display:block; text-align:center; " data-ad-layout="in-article" data-ad-format="fluid" data-ad-client="ca-pub-7372939085773564" data-ad-slot="7329067043"></ins>
      <script>
          (adsbygoogle = window.adsbygoogle || []).push({});
      </script>
    </div>

    <div id="vert-template" data-template="
  &lt;iframe
    id=&quot;{id}&quot;
    name=&quot;{id}&quot;
    class=&quot;lazyload&quot;
    style=&quot;max-height: 300px;&quot;
    sandbox=&quot;allow-forms allow-popups allow-popups-to-escape-sandbox allow-same-origin allow-scripts allow-top-navigation-by-user-activation&quot;
    width=&quot;100%&quot; height=&quot;100%&quot;
    frameborder=&quot;0&quot; marginwidth=&quot;0&quot; marginheight=&quot;0&quot; vspace=&quot;0&quot; hspace=&quot;0&quot; allowtransparency=&quot;true&quot; scrolling=&quot;no&quot;
    data-src=&quot;{src}&quot;
  &gt;&lt;/iframe&gt;
">
</div>

<script async="" defer="" type="text/javascript">
  Manager.ready(function () {
    var select = Manager.dom().select;
    var $adblockerDetector = select('#uj-antivert-detector');

    var $iframeVertTemplate = select('#vert-template').get(0).getAttribute('data-template');

    var qsDebug = new URLSearchParams(window.location.search).get('debug');

    var slotId = new Date().getTime();

    // Selectors
    var vertSlotsSelector = '.uj-vert-unit';
    var vertSlotsSelectorLoaded = '.uj-vert-unit ins[data-ad-status]';
    var vertSlotsSelectorUnfilled = '.uj-vert-unit ins[data-ad-status="unfilled"]';

    // Set the anti-vert detector script
    /*
      /bottomads.js
      /content-ads.js
      /core-ads.js
      /insertads.js
      /jsads.js
      /left_ads.js
      /nativeads.js
      /openads.js
      /popupads.js
      /ads/ad-script.js?ad_network=123&tracking_id=abc#banner_ad
    */
    // var scriptUrl = '/assets/js/taboola_ads.js';
    // var scriptUrl = 'assets/js/pagead.js';
    var scriptUrl = '/assets/js/popupads.js?t1=/ads/native.js&t2=/nativeads.js&t3=/content-ads.js&cb=' + Manager.properties.global.cacheBreaker;

    // Check if being handled already
    if (window._ujVertCheckerStarted) {
      return;
    }
    window._ujVertCheckerStarted = true;

    // Message Handler
    function messageHandler(event) {
      var message = event.data || {};
      var command = message.command || '';
      var payload = message.payload || {};

      // Log
      console.log('Message', command, message, event.source);

      // Handle the message
      if (command === 'uj-vert-unit:set-dimensions') {
        // Get the iframe
        var $iframe = select('#uj-vert-' + payload.id).get(0);

        // Set the dimensions
        $iframe.style.height = payload.height + 'px';
      } else if (command === 'uj-vert-unit:click') {
        // Navigate to the URL
        window.location.href = payload.url;
      }
    }

    // Fill the units
    function fillUnits() {
      var $vertSlots = select(vertSlotsSelector);
      var adURL = qsDebug === 'true'
        ? 'http://localhost:4001/verts/main'
        : 'https://promo-server.itwcreativeworks.com/verts/main'

      adURL = new URL(adURL);

      adURL.searchParams.set('parentURL', window.location.href);

      // Load the ad units
      $vertSlots.each(function ($el) {
        var isFilled = $el.matches('[data-ad-status="filled"]');
        var finalURL = new URL(adURL.toString());

        finalURL.searchParams.set('frameId', slotId);

        finalURL = finalURL.toString();

        if (isFilled) {
          return;
        }

        console.log('Loading ad unit', $el, finalURL);

        $el.innerHTML = $iframeVertTemplate
          .replace(/{id}/g, 'uj-vert-' + slotId++)
          .replace(/{src}/g, finalURL)
      })
    }

    window.addEventListener('message', messageHandler, false);

    // Load the antivert checker
    Manager.dom().loadScript({src: scriptUrl})
    .then(function (e) {
      console.log('Loaded antivert checker', e);
    })
    .catch(function (e) {
      console.error('Failed to load antivert checker', e);
    })
    .finally(function () {
      var filledChecker = setInterval(function () {
        // Check if there are unfilled units
        var isOGVertsLoaded = select(vertSlotsSelectorLoaded).elements.exists;
        var isUnfilledVertSlots = select(vertSlotsSelectorUnfilled).elements.exists;

        // Set the global adblocker enabled flag
        Manager.properties.global.isAdblockerEnabled = !select('#uj-antivert-detector').elements.exists;

        var needsCustomVerts = isUnfilledVertSlots
          || Manager.properties.global.isAdblockerEnabled
          || Manager.properties.meta.environment === 'development';

        console.log(
          'isAdblockerEnabled', Manager.properties.global.isAdblockerEnabled,
          'isOGVertsLoaded', isOGVertsLoaded,
          'isUnfilledVertSlots', isUnfilledVertSlots,
          'needsCustomVerts', needsCustomVerts,
        );

        if (!Manager.properties.global.isAdblockerEnabled && !isOGVertsLoaded) {
          return;
        }

        if (isOGVertsLoaded && !isUnfilledVertSlots) {
          return clearInterval(filledChecker);
        }

        if (needsCustomVerts) {
          clearInterval(filledChecker);
          fillUnits();
        } else {
          return;
        }
      }, 300)
    })
  })
</script>

  </ad-unit>

<p>Between ads</p>

<!-- Adunit - Adsense In-article Ad -->
<ad-unit><div class="uj-vert-unit" data-plan-id="basic" data-plan-visibility="visible">
      <ins class="adsbygoogle" style="display:block; text-align:center; " data-ad-layout="in-article" data-ad-format="fluid" data-ad-client="ca-pub-7372939085773564" data-ad-slot="7329067043"></ins>
      <script>
          (adsbygoogle = window.adsbygoogle || []).push({});
      </script>
    </div>

    <div id="vert-template" data-template="
  &lt;iframe
    id=&quot;{id}&quot;
    name=&quot;{id}&quot;
    class=&quot;lazyload&quot;
    style=&quot;max-height: 300px;&quot;
    sandbox=&quot;allow-forms allow-popups allow-popups-to-escape-sandbox allow-same-origin allow-scripts allow-top-navigation-by-user-activation&quot;
    width=&quot;100%&quot; height=&quot;100%&quot;
    frameborder=&quot;0&quot; marginwidth=&quot;0&quot; marginheight=&quot;0&quot; vspace=&quot;0&quot; hspace=&quot;0&quot; allowtransparency=&quot;true&quot; scrolling=&quot;no&quot;
    data-src=&quot;{src}&quot;
  &gt;&lt;/iframe&gt;
">
</div>

<script async="" defer="" type="text/javascript">
  Manager.ready(function () {
    var select = Manager.dom().select;
    var $adblockerDetector = select('#uj-antivert-detector');

    var $iframeVertTemplate = select('#vert-template').get(0).getAttribute('data-template');

    var qsDebug = new URLSearchParams(window.location.search).get('debug');

    var slotId = new Date().getTime();

    // Selectors
    var vertSlotsSelector = '.uj-vert-unit';
    var vertSlotsSelectorLoaded = '.uj-vert-unit ins[data-ad-status]';
    var vertSlotsSelectorUnfilled = '.uj-vert-unit ins[data-ad-status="unfilled"]';

    // Set the anti-vert detector script
    /*
      /bottomads.js
      /content-ads.js
      /core-ads.js
      /insertads.js
      /jsads.js
      /left_ads.js
      /nativeads.js
      /openads.js
      /popupads.js
      /ads/ad-script.js?ad_network=123&tracking_id=abc#banner_ad
    */
    // var scriptUrl = '/assets/js/taboola_ads.js';
    // var scriptUrl = 'assets/js/pagead.js';
    var scriptUrl = '/assets/js/popupads.js?t1=/ads/native.js&t2=/nativeads.js&t3=/content-ads.js&cb=' + Manager.properties.global.cacheBreaker;

    // Check if being handled already
    if (window._ujVertCheckerStarted) {
      return;
    }
    window._ujVertCheckerStarted = true;

    // Message Handler
    function messageHandler(event) {
      var message = event.data || {};
      var command = message.command || '';
      var payload = message.payload || {};

      // Log
      console.log('Message', command, message, event.source);

      // Handle the message
      if (command === 'uj-vert-unit:set-dimensions') {
        // Get the iframe
        var $iframe = select('#uj-vert-' + payload.id).get(0);

        // Set the dimensions
        $iframe.style.height = payload.height + 'px';
      } else if (command === 'uj-vert-unit:click') {
        // Navigate to the URL
        window.location.href = payload.url;
      }
    }

    // Fill the units
    function fillUnits() {
      var $vertSlots = select(vertSlotsSelector);
      var adURL = qsDebug === 'true'
        ? 'http://localhost:4001/verts/main'
        : 'https://promo-server.itwcreativeworks.com/verts/main'

      adURL = new URL(adURL);

      adURL.searchParams.set('parentURL', window.location.href);

      // Load the ad units
      $vertSlots.each(function ($el) {
        var isFilled = $el.matches('[data-ad-status="filled"]');
        var finalURL = new URL(adURL.toString());

        finalURL.searchParams.set('frameId', slotId);

        finalURL = finalURL.toString();

        if (isFilled) {
          return;
        }

        console.log('Loading ad unit', $el, finalURL);

        $el.innerHTML = $iframeVertTemplate
          .replace(/{id}/g, 'uj-vert-' + slotId++)
          .replace(/{src}/g, finalURL)
      })
    }

    window.addEventListener('message', messageHandler, false);

    // Load the antivert checker
    Manager.dom().loadScript({src: scriptUrl})
    .then(function (e) {
      console.log('Loaded antivert checker', e);
    })
    .catch(function (e) {
      console.error('Failed to load antivert checker', e);
    })
    .finally(function () {
      var filledChecker = setInterval(function () {
        // Check if there are unfilled units
        var isOGVertsLoaded = select(vertSlotsSelectorLoaded).elements.exists;
        var isUnfilledVertSlots = select(vertSlotsSelectorUnfilled).elements.exists;

        // Set the global adblocker enabled flag
        Manager.properties.global.isAdblockerEnabled = !select('#uj-antivert-detector').elements.exists;

        var needsCustomVerts = isUnfilledVertSlots
          || Manager.properties.global.isAdblockerEnabled
          || Manager.properties.meta.environment === 'development';

        console.log(
          'isAdblockerEnabled', Manager.properties.global.isAdblockerEnabled,
          'isOGVertsLoaded', isOGVertsLoaded,
          'isUnfilledVertSlots', isUnfilledVertSlots,
          'needsCustomVerts', needsCustomVerts,
        );

        if (!Manager.properties.global.isAdblockerEnabled && !isOGVertsLoaded) {
          return;
        }

        if (isOGVertsLoaded && !isUnfilledVertSlots) {
          return clearInterval(filledChecker);
        }

        if (needsCustomVerts) {
          clearInterval(filledChecker);
          fillUnits();
        } else {
          return;
        }
      }, 300)
    })
  })
</script>

  </ad-unit>

<p>After ads</p>
",
        "test_7": "<h2 id="title-inside-post-7">Title inside post 7</h2>
<p>This is a wonderful paragrah inside a post! This is post #7 with ID 1722471527.</p>

<p>Before ads</p>

<!-- Adunit - Adsense In-article Ad --><p>Between ads</p>

<!-- Adunit - Adsense In-article Ad --><p>After ads</p>
",
        "test_8": "<h2 id=\"title-inside-post-7\">Title inside post 7</h2>\n<p>This is a wonderful paragrah inside a post! This is post #7 with ID 1722471527.</p>\n\n<p>Before ads</p>\n\n<!-- Adunit - Adsense In-article Ad --><p>Between ads</p>\n\n<!-- Adunit - Adsense In-article Ad --><p>After ads</p>\n",

        "authors": [
          {
            "name": "Ultimate Jekyll"
          }
        ],
        "attachments": [
          {
            "url": ""
          }
        ],
        "summary": "This is a sample excerpt for post number 7",
        "tags": ["Marketing","Business"]
      },
    
      {"id": "https://template.itwcreativeworks.com/blog/example-post-6",
        "url": "https://template.itwcreativeworks.com/blog/example-post-6",
        "title": "Example Post 6",
        "date_published": "2024-01-06T00:00:00.000Z",
        "image": "",
        "content_text": "Title inside post 6 This is a wonderful paragrah inside a post! This is post #6 with ID 1722471526.  Before ads                                     Between ads                                     After ads ",
        "content_html": "<h2 id=\"title-inside-post-6\">Title inside post 6</h2> <p>This is a wonderful paragrah inside a post! This is post #6 with ID 1722471526.</p>  <p>Before ads</p>  <!-- Adunit - Adsense In-article Ad --><p>Between ads</p>  <!-- Adunit - Adsense In-article Ad --><p>After ads</p> ",

        "test_1": "<h2 id="title-inside-post-6">Title inside post 6</h2>
<p>This is a wonderful paragrah inside a post! This is post #6 with ID 1722471526.</p>

<p>Before ads</p>

<!-- Adunit - Adsense In-article Ad -->
<ad-unit><div class="uj-vert-unit" data-plan-id="basic" data-plan-visibility="visible">
      <ins class="adsbygoogle" style="display:block; text-align:center; " data-ad-layout="in-article" data-ad-format="fluid" data-ad-client="ca-pub-7372939085773564" data-ad-slot="7329067043"></ins>
      <script>
          (adsbygoogle = window.adsbygoogle || []).push({});
      </script>
    </div>

    <div id="vert-template" data-template="
  &lt;iframe
    id=&quot;{id}&quot;
    name=&quot;{id}&quot;
    class=&quot;lazyload&quot;
    style=&quot;max-height: 300px;&quot;
    sandbox=&quot;allow-forms allow-popups allow-popups-to-escape-sandbox allow-same-origin allow-scripts allow-top-navigation-by-user-activation&quot;
    width=&quot;100%&quot; height=&quot;100%&quot;
    frameborder=&quot;0&quot; marginwidth=&quot;0&quot; marginheight=&quot;0&quot; vspace=&quot;0&quot; hspace=&quot;0&quot; allowtransparency=&quot;true&quot; scrolling=&quot;no&quot;
    data-src=&quot;{src}&quot;
  &gt;&lt;/iframe&gt;
">
</div>

<script async="" defer="" type="text/javascript">
  Manager.ready(function () {
    var select = Manager.dom().select;
    var $adblockerDetector = select('#uj-antivert-detector');

    var $iframeVertTemplate = select('#vert-template').get(0).getAttribute('data-template');

    var qsDebug = new URLSearchParams(window.location.search).get('debug');

    var slotId = new Date().getTime();

    // Selectors
    var vertSlotsSelector = '.uj-vert-unit';
    var vertSlotsSelectorLoaded = '.uj-vert-unit ins[data-ad-status]';
    var vertSlotsSelectorUnfilled = '.uj-vert-unit ins[data-ad-status="unfilled"]';

    // Set the anti-vert detector script
    /*
      /bottomads.js
      /content-ads.js
      /core-ads.js
      /insertads.js
      /jsads.js
      /left_ads.js
      /nativeads.js
      /openads.js
      /popupads.js
      /ads/ad-script.js?ad_network=123&tracking_id=abc#banner_ad
    */
    // var scriptUrl = '/assets/js/taboola_ads.js';
    // var scriptUrl = 'assets/js/pagead.js';
    var scriptUrl = '/assets/js/popupads.js?t1=/ads/native.js&t2=/nativeads.js&t3=/content-ads.js&cb=' + Manager.properties.global.cacheBreaker;

    // Check if being handled already
    if (window._ujVertCheckerStarted) {
      return;
    }
    window._ujVertCheckerStarted = true;

    // Message Handler
    function messageHandler(event) {
      var message = event.data || {};
      var command = message.command || '';
      var payload = message.payload || {};

      // Log
      console.log('Message', command, message, event.source);

      // Handle the message
      if (command === 'uj-vert-unit:set-dimensions') {
        // Get the iframe
        var $iframe = select('#uj-vert-' + payload.id).get(0);

        // Set the dimensions
        $iframe.style.height = payload.height + 'px';
      } else if (command === 'uj-vert-unit:click') {
        // Navigate to the URL
        window.location.href = payload.url;
      }
    }

    // Fill the units
    function fillUnits() {
      var $vertSlots = select(vertSlotsSelector);
      var adURL = qsDebug === 'true'
        ? 'http://localhost:4001/verts/main'
        : 'https://promo-server.itwcreativeworks.com/verts/main'

      adURL = new URL(adURL);

      adURL.searchParams.set('parentURL', window.location.href);

      // Load the ad units
      $vertSlots.each(function ($el) {
        var isFilled = $el.matches('[data-ad-status="filled"]');
        var finalURL = new URL(adURL.toString());

        finalURL.searchParams.set('frameId', slotId);

        finalURL = finalURL.toString();

        if (isFilled) {
          return;
        }

        console.log('Loading ad unit', $el, finalURL);

        $el.innerHTML = $iframeVertTemplate
          .replace(/{id}/g, 'uj-vert-' + slotId++)
          .replace(/{src}/g, finalURL)
      })
    }

    window.addEventListener('message', messageHandler, false);

    // Load the antivert checker
    Manager.dom().loadScript({src: scriptUrl})
    .then(function (e) {
      console.log('Loaded antivert checker', e);
    })
    .catch(function (e) {
      console.error('Failed to load antivert checker', e);
    })
    .finally(function () {
      var filledChecker = setInterval(function () {
        // Check if there are unfilled units
        var isOGVertsLoaded = select(vertSlotsSelectorLoaded).elements.exists;
        var isUnfilledVertSlots = select(vertSlotsSelectorUnfilled).elements.exists;

        // Set the global adblocker enabled flag
        Manager.properties.global.isAdblockerEnabled = !select('#uj-antivert-detector').elements.exists;

        var needsCustomVerts = isUnfilledVertSlots
          || Manager.properties.global.isAdblockerEnabled
          || Manager.properties.meta.environment === 'development';

        console.log(
          'isAdblockerEnabled', Manager.properties.global.isAdblockerEnabled,
          'isOGVertsLoaded', isOGVertsLoaded,
          'isUnfilledVertSlots', isUnfilledVertSlots,
          'needsCustomVerts', needsCustomVerts,
        );

        if (!Manager.properties.global.isAdblockerEnabled && !isOGVertsLoaded) {
          return;
        }

        if (isOGVertsLoaded && !isUnfilledVertSlots) {
          return clearInterval(filledChecker);
        }

        if (needsCustomVerts) {
          clearInterval(filledChecker);
          fillUnits();
        } else {
          return;
        }
      }, 300)
    })
  })
</script>

  </ad-unit>

<p>Between ads</p>

<!-- Adunit - Adsense In-article Ad -->
<ad-unit><div class="uj-vert-unit" data-plan-id="basic" data-plan-visibility="visible">
      <ins class="adsbygoogle" style="display:block; text-align:center; " data-ad-layout="in-article" data-ad-format="fluid" data-ad-client="ca-pub-7372939085773564" data-ad-slot="7329067043"></ins>
      <script>
          (adsbygoogle = window.adsbygoogle || []).push({});
      </script>
    </div>

    <div id="vert-template" data-template="
  &lt;iframe
    id=&quot;{id}&quot;
    name=&quot;{id}&quot;
    class=&quot;lazyload&quot;
    style=&quot;max-height: 300px;&quot;
    sandbox=&quot;allow-forms allow-popups allow-popups-to-escape-sandbox allow-same-origin allow-scripts allow-top-navigation-by-user-activation&quot;
    width=&quot;100%&quot; height=&quot;100%&quot;
    frameborder=&quot;0&quot; marginwidth=&quot;0&quot; marginheight=&quot;0&quot; vspace=&quot;0&quot; hspace=&quot;0&quot; allowtransparency=&quot;true&quot; scrolling=&quot;no&quot;
    data-src=&quot;{src}&quot;
  &gt;&lt;/iframe&gt;
">
</div>

<script async="" defer="" type="text/javascript">
  Manager.ready(function () {
    var select = Manager.dom().select;
    var $adblockerDetector = select('#uj-antivert-detector');

    var $iframeVertTemplate = select('#vert-template').get(0).getAttribute('data-template');

    var qsDebug = new URLSearchParams(window.location.search).get('debug');

    var slotId = new Date().getTime();

    // Selectors
    var vertSlotsSelector = '.uj-vert-unit';
    var vertSlotsSelectorLoaded = '.uj-vert-unit ins[data-ad-status]';
    var vertSlotsSelectorUnfilled = '.uj-vert-unit ins[data-ad-status="unfilled"]';

    // Set the anti-vert detector script
    /*
      /bottomads.js
      /content-ads.js
      /core-ads.js
      /insertads.js
      /jsads.js
      /left_ads.js
      /nativeads.js
      /openads.js
      /popupads.js
      /ads/ad-script.js?ad_network=123&tracking_id=abc#banner_ad
    */
    // var scriptUrl = '/assets/js/taboola_ads.js';
    // var scriptUrl = 'assets/js/pagead.js';
    var scriptUrl = '/assets/js/popupads.js?t1=/ads/native.js&t2=/nativeads.js&t3=/content-ads.js&cb=' + Manager.properties.global.cacheBreaker;

    // Check if being handled already
    if (window._ujVertCheckerStarted) {
      return;
    }
    window._ujVertCheckerStarted = true;

    // Message Handler
    function messageHandler(event) {
      var message = event.data || {};
      var command = message.command || '';
      var payload = message.payload || {};

      // Log
      console.log('Message', command, message, event.source);

      // Handle the message
      if (command === 'uj-vert-unit:set-dimensions') {
        // Get the iframe
        var $iframe = select('#uj-vert-' + payload.id).get(0);

        // Set the dimensions
        $iframe.style.height = payload.height + 'px';
      } else if (command === 'uj-vert-unit:click') {
        // Navigate to the URL
        window.location.href = payload.url;
      }
    }

    // Fill the units
    function fillUnits() {
      var $vertSlots = select(vertSlotsSelector);
      var adURL = qsDebug === 'true'
        ? 'http://localhost:4001/verts/main'
        : 'https://promo-server.itwcreativeworks.com/verts/main'

      adURL = new URL(adURL);

      adURL.searchParams.set('parentURL', window.location.href);

      // Load the ad units
      $vertSlots.each(function ($el) {
        var isFilled = $el.matches('[data-ad-status="filled"]');
        var finalURL = new URL(adURL.toString());

        finalURL.searchParams.set('frameId', slotId);

        finalURL = finalURL.toString();

        if (isFilled) {
          return;
        }

        console.log('Loading ad unit', $el, finalURL);

        $el.innerHTML = $iframeVertTemplate
          .replace(/{id}/g, 'uj-vert-' + slotId++)
          .replace(/{src}/g, finalURL)
      })
    }

    window.addEventListener('message', messageHandler, false);

    // Load the antivert checker
    Manager.dom().loadScript({src: scriptUrl})
    .then(function (e) {
      console.log('Loaded antivert checker', e);
    })
    .catch(function (e) {
      console.error('Failed to load antivert checker', e);
    })
    .finally(function () {
      var filledChecker = setInterval(function () {
        // Check if there are unfilled units
        var isOGVertsLoaded = select(vertSlotsSelectorLoaded).elements.exists;
        var isUnfilledVertSlots = select(vertSlotsSelectorUnfilled).elements.exists;

        // Set the global adblocker enabled flag
        Manager.properties.global.isAdblockerEnabled = !select('#uj-antivert-detector').elements.exists;

        var needsCustomVerts = isUnfilledVertSlots
          || Manager.properties.global.isAdblockerEnabled
          || Manager.properties.meta.environment === 'development';

        console.log(
          'isAdblockerEnabled', Manager.properties.global.isAdblockerEnabled,
          'isOGVertsLoaded', isOGVertsLoaded,
          'isUnfilledVertSlots', isUnfilledVertSlots,
          'needsCustomVerts', needsCustomVerts,
        );

        if (!Manager.properties.global.isAdblockerEnabled && !isOGVertsLoaded) {
          return;
        }

        if (isOGVertsLoaded && !isUnfilledVertSlots) {
          return clearInterval(filledChecker);
        }

        if (needsCustomVerts) {
          clearInterval(filledChecker);
          fillUnits();
        } else {
          return;
        }
      }, 300)
    })
  })
</script>

  </ad-unit>

<p>After ads</p>
",
        "test_1_2": "<h2 id="title-inside-post-6">Title inside post 6</h2>
<p>This is a wonderful paragrah inside a post! This is post #6 with ID 1722471526.</p>

<p>Before ads</p>

<!-- Adunit - Adsense In-article Ad --><p>Between ads</p>

<!-- Adunit - Adsense In-article Ad --><p>After ads</p>
",
        "test_1_3": "<h2 id=\"title-inside-post-6\">Title inside post 6</h2> <p>This is a wonderful paragrah inside a post! This is post #6 with ID 1722471526.</p>  <p>Before ads</p>  <!-- Adunit - Adsense In-article Ad --> <p>Between ads</p>  <!-- Adunit - Adsense In-article Ad --> <p>After ads</p> ",


        "test_2": "<h2 id="title-inside-post-6">Title inside post 6</h2>
<p>This is a wonderful paragrah inside a post! This is post #6 with ID 1722471526.</p>

<p>Before ads</p>

<!-- Adunit - Adsense In-article Ad -->
<ad-unit><div class="uj-vert-unit" data-plan-id="basic" data-plan-visibility="visible">
      <ins class="adsbygoogle" style="display:block; text-align:center; " data-ad-layout="in-article" data-ad-format="fluid" data-ad-client="ca-pub-7372939085773564" data-ad-slot="7329067043"></ins>
      <script>
          (adsbygoogle = window.adsbygoogle || []).push({});
      </script>
    </div>

    <div id="vert-template" data-template="
  &lt;iframe
    id=&quot;{id}&quot;
    name=&quot;{id}&quot;
    class=&quot;lazyload&quot;
    style=&quot;max-height: 300px;&quot;
    sandbox=&quot;allow-forms allow-popups allow-popups-to-escape-sandbox allow-same-origin allow-scripts allow-top-navigation-by-user-activation&quot;
    width=&quot;100%&quot; height=&quot;100%&quot;
    frameborder=&quot;0&quot; marginwidth=&quot;0&quot; marginheight=&quot;0&quot; vspace=&quot;0&quot; hspace=&quot;0&quot; allowtransparency=&quot;true&quot; scrolling=&quot;no&quot;
    data-src=&quot;{src}&quot;
  &gt;&lt;/iframe&gt;
">
</div>

<script async="" defer="" type="text/javascript">
  Manager.ready(function () {
    var select = Manager.dom().select;
    var $adblockerDetector = select('#uj-antivert-detector');

    var $iframeVertTemplate = select('#vert-template').get(0).getAttribute('data-template');

    var qsDebug = new URLSearchParams(window.location.search).get('debug');

    var slotId = new Date().getTime();

    // Selectors
    var vertSlotsSelector = '.uj-vert-unit';
    var vertSlotsSelectorLoaded = '.uj-vert-unit ins[data-ad-status]';
    var vertSlotsSelectorUnfilled = '.uj-vert-unit ins[data-ad-status="unfilled"]';

    // Set the anti-vert detector script
    /*
      /bottomads.js
      /content-ads.js
      /core-ads.js
      /insertads.js
      /jsads.js
      /left_ads.js
      /nativeads.js
      /openads.js
      /popupads.js
      /ads/ad-script.js?ad_network=123&tracking_id=abc#banner_ad
    */
    // var scriptUrl = '/assets/js/taboola_ads.js';
    // var scriptUrl = 'assets/js/pagead.js';
    var scriptUrl = '/assets/js/popupads.js?t1=/ads/native.js&t2=/nativeads.js&t3=/content-ads.js&cb=' + Manager.properties.global.cacheBreaker;

    // Check if being handled already
    if (window._ujVertCheckerStarted) {
      return;
    }
    window._ujVertCheckerStarted = true;

    // Message Handler
    function messageHandler(event) {
      var message = event.data || {};
      var command = message.command || '';
      var payload = message.payload || {};

      // Log
      console.log('Message', command, message, event.source);

      // Handle the message
      if (command === 'uj-vert-unit:set-dimensions') {
        // Get the iframe
        var $iframe = select('#uj-vert-' + payload.id).get(0);

        // Set the dimensions
        $iframe.style.height = payload.height + 'px';
      } else if (command === 'uj-vert-unit:click') {
        // Navigate to the URL
        window.location.href = payload.url;
      }
    }

    // Fill the units
    function fillUnits() {
      var $vertSlots = select(vertSlotsSelector);
      var adURL = qsDebug === 'true'
        ? 'http://localhost:4001/verts/main'
        : 'https://promo-server.itwcreativeworks.com/verts/main'

      adURL = new URL(adURL);

      adURL.searchParams.set('parentURL', window.location.href);

      // Load the ad units
      $vertSlots.each(function ($el) {
        var isFilled = $el.matches('[data-ad-status="filled"]');
        var finalURL = new URL(adURL.toString());

        finalURL.searchParams.set('frameId', slotId);

        finalURL = finalURL.toString();

        if (isFilled) {
          return;
        }

        console.log('Loading ad unit', $el, finalURL);

        $el.innerHTML = $iframeVertTemplate
          .replace(/{id}/g, 'uj-vert-' + slotId++)
          .replace(/{src}/g, finalURL)
      })
    }

    window.addEventListener('message', messageHandler, false);

    // Load the antivert checker
    Manager.dom().loadScript({src: scriptUrl})
    .then(function (e) {
      console.log('Loaded antivert checker', e);
    })
    .catch(function (e) {
      console.error('Failed to load antivert checker', e);
    })
    .finally(function () {
      var filledChecker = setInterval(function () {
        // Check if there are unfilled units
        var isOGVertsLoaded = select(vertSlotsSelectorLoaded).elements.exists;
        var isUnfilledVertSlots = select(vertSlotsSelectorUnfilled).elements.exists;

        // Set the global adblocker enabled flag
        Manager.properties.global.isAdblockerEnabled = !select('#uj-antivert-detector').elements.exists;

        var needsCustomVerts = isUnfilledVertSlots
          || Manager.properties.global.isAdblockerEnabled
          || Manager.properties.meta.environment === 'development';

        console.log(
          'isAdblockerEnabled', Manager.properties.global.isAdblockerEnabled,
          'isOGVertsLoaded', isOGVertsLoaded,
          'isUnfilledVertSlots', isUnfilledVertSlots,
          'needsCustomVerts', needsCustomVerts,
        );

        if (!Manager.properties.global.isAdblockerEnabled && !isOGVertsLoaded) {
          return;
        }

        if (isOGVertsLoaded && !isUnfilledVertSlots) {
          return clearInterval(filledChecker);
        }

        if (needsCustomVerts) {
          clearInterval(filledChecker);
          fillUnits();
        } else {
          return;
        }
      }, 300)
    })
  })
</script>

  </ad-unit>

<p>Between ads</p>

<!-- Adunit - Adsense In-article Ad -->
<ad-unit><div class="uj-vert-unit" data-plan-id="basic" data-plan-visibility="visible">
      <ins class="adsbygoogle" style="display:block; text-align:center; " data-ad-layout="in-article" data-ad-format="fluid" data-ad-client="ca-pub-7372939085773564" data-ad-slot="7329067043"></ins>
      <script>
          (adsbygoogle = window.adsbygoogle || []).push({});
      </script>
    </div>

    <div id="vert-template" data-template="
  &lt;iframe
    id=&quot;{id}&quot;
    name=&quot;{id}&quot;
    class=&quot;lazyload&quot;
    style=&quot;max-height: 300px;&quot;
    sandbox=&quot;allow-forms allow-popups allow-popups-to-escape-sandbox allow-same-origin allow-scripts allow-top-navigation-by-user-activation&quot;
    width=&quot;100%&quot; height=&quot;100%&quot;
    frameborder=&quot;0&quot; marginwidth=&quot;0&quot; marginheight=&quot;0&quot; vspace=&quot;0&quot; hspace=&quot;0&quot; allowtransparency=&quot;true&quot; scrolling=&quot;no&quot;
    data-src=&quot;{src}&quot;
  &gt;&lt;/iframe&gt;
">
</div>

<script async="" defer="" type="text/javascript">
  Manager.ready(function () {
    var select = Manager.dom().select;
    var $adblockerDetector = select('#uj-antivert-detector');

    var $iframeVertTemplate = select('#vert-template').get(0).getAttribute('data-template');

    var qsDebug = new URLSearchParams(window.location.search).get('debug');

    var slotId = new Date().getTime();

    // Selectors
    var vertSlotsSelector = '.uj-vert-unit';
    var vertSlotsSelectorLoaded = '.uj-vert-unit ins[data-ad-status]';
    var vertSlotsSelectorUnfilled = '.uj-vert-unit ins[data-ad-status="unfilled"]';

    // Set the anti-vert detector script
    /*
      /bottomads.js
      /content-ads.js
      /core-ads.js
      /insertads.js
      /jsads.js
      /left_ads.js
      /nativeads.js
      /openads.js
      /popupads.js
      /ads/ad-script.js?ad_network=123&tracking_id=abc#banner_ad
    */
    // var scriptUrl = '/assets/js/taboola_ads.js';
    // var scriptUrl = 'assets/js/pagead.js';
    var scriptUrl = '/assets/js/popupads.js?t1=/ads/native.js&t2=/nativeads.js&t3=/content-ads.js&cb=' + Manager.properties.global.cacheBreaker;

    // Check if being handled already
    if (window._ujVertCheckerStarted) {
      return;
    }
    window._ujVertCheckerStarted = true;

    // Message Handler
    function messageHandler(event) {
      var message = event.data || {};
      var command = message.command || '';
      var payload = message.payload || {};

      // Log
      console.log('Message', command, message, event.source);

      // Handle the message
      if (command === 'uj-vert-unit:set-dimensions') {
        // Get the iframe
        var $iframe = select('#uj-vert-' + payload.id).get(0);

        // Set the dimensions
        $iframe.style.height = payload.height + 'px';
      } else if (command === 'uj-vert-unit:click') {
        // Navigate to the URL
        window.location.href = payload.url;
      }
    }

    // Fill the units
    function fillUnits() {
      var $vertSlots = select(vertSlotsSelector);
      var adURL = qsDebug === 'true'
        ? 'http://localhost:4001/verts/main'
        : 'https://promo-server.itwcreativeworks.com/verts/main'

      adURL = new URL(adURL);

      adURL.searchParams.set('parentURL', window.location.href);

      // Load the ad units
      $vertSlots.each(function ($el) {
        var isFilled = $el.matches('[data-ad-status="filled"]');
        var finalURL = new URL(adURL.toString());

        finalURL.searchParams.set('frameId', slotId);

        finalURL = finalURL.toString();

        if (isFilled) {
          return;
        }

        console.log('Loading ad unit', $el, finalURL);

        $el.innerHTML = $iframeVertTemplate
          .replace(/{id}/g, 'uj-vert-' + slotId++)
          .replace(/{src}/g, finalURL)
      })
    }

    window.addEventListener('message', messageHandler, false);

    // Load the antivert checker
    Manager.dom().loadScript({src: scriptUrl})
    .then(function (e) {
      console.log('Loaded antivert checker', e);
    })
    .catch(function (e) {
      console.error('Failed to load antivert checker', e);
    })
    .finally(function () {
      var filledChecker = setInterval(function () {
        // Check if there are unfilled units
        var isOGVertsLoaded = select(vertSlotsSelectorLoaded).elements.exists;
        var isUnfilledVertSlots = select(vertSlotsSelectorUnfilled).elements.exists;

        // Set the global adblocker enabled flag
        Manager.properties.global.isAdblockerEnabled = !select('#uj-antivert-detector').elements.exists;

        var needsCustomVerts = isUnfilledVertSlots
          || Manager.properties.global.isAdblockerEnabled
          || Manager.properties.meta.environment === 'development';

        console.log(
          'isAdblockerEnabled', Manager.properties.global.isAdblockerEnabled,
          'isOGVertsLoaded', isOGVertsLoaded,
          'isUnfilledVertSlots', isUnfilledVertSlots,
          'needsCustomVerts', needsCustomVerts,
        );

        if (!Manager.properties.global.isAdblockerEnabled && !isOGVertsLoaded) {
          return;
        }

        if (isOGVertsLoaded && !isUnfilledVertSlots) {
          return clearInterval(filledChecker);
        }

        if (needsCustomVerts) {
          clearInterval(filledChecker);
          fillUnits();
        } else {
          return;
        }
      }, 300)
    })
  })
</script>

  </ad-unit>

<p>After ads</p>
",
        "test_3": "<h2 id="title-inside-post-6">Title inside post 6</h2>
<p>This is a wonderful paragrah inside a post! This is post #6 with ID 1722471526.</p>

<p>Before ads</p>

<!-- Adunit - Adsense In-article Ad -->
<ad-unit><div class="uj-vert-unit" data-plan-id="basic" data-plan-visibility="visible">
      <ins class="adsbygoogle" style="display:block; text-align:center; " data-ad-layout="in-article" data-ad-format="fluid" data-ad-client="ca-pub-7372939085773564" data-ad-slot="7329067043"></ins>
      <script>
          (adsbygoogle = window.adsbygoogle || []).push({});
      </script>
    </div>

    <div id="vert-template" data-template="
  &lt;iframe
    id=&quot;{id}&quot;
    name=&quot;{id}&quot;
    class=&quot;lazyload&quot;
    style=&quot;max-height: 300px;&quot;
    sandbox=&quot;allow-forms allow-popups allow-popups-to-escape-sandbox allow-same-origin allow-scripts allow-top-navigation-by-user-activation&quot;
    width=&quot;100%&quot; height=&quot;100%&quot;
    frameborder=&quot;0&quot; marginwidth=&quot;0&quot; marginheight=&quot;0&quot; vspace=&quot;0&quot; hspace=&quot;0&quot; allowtransparency=&quot;true&quot; scrolling=&quot;no&quot;
    data-src=&quot;{src}&quot;
  &gt;&lt;/iframe&gt;
">
</div>

<script async="" defer="" type="text/javascript">
  Manager.ready(function () {
    var select = Manager.dom().select;
    var $adblockerDetector = select('#uj-antivert-detector');

    var $iframeVertTemplate = select('#vert-template').get(0).getAttribute('data-template');

    var qsDebug = new URLSearchParams(window.location.search).get('debug');

    var slotId = new Date().getTime();

    // Selectors
    var vertSlotsSelector = '.uj-vert-unit';
    var vertSlotsSelectorLoaded = '.uj-vert-unit ins[data-ad-status]';
    var vertSlotsSelectorUnfilled = '.uj-vert-unit ins[data-ad-status="unfilled"]';

    // Set the anti-vert detector script
    /*
      /bottomads.js
      /content-ads.js
      /core-ads.js
      /insertads.js
      /jsads.js
      /left_ads.js
      /nativeads.js
      /openads.js
      /popupads.js
      /ads/ad-script.js?ad_network=123&tracking_id=abc#banner_ad
    */
    // var scriptUrl = '/assets/js/taboola_ads.js';
    // var scriptUrl = 'assets/js/pagead.js';
    var scriptUrl = '/assets/js/popupads.js?t1=/ads/native.js&t2=/nativeads.js&t3=/content-ads.js&cb=' + Manager.properties.global.cacheBreaker;

    // Check if being handled already
    if (window._ujVertCheckerStarted) {
      return;
    }
    window._ujVertCheckerStarted = true;

    // Message Handler
    function messageHandler(event) {
      var message = event.data || {};
      var command = message.command || '';
      var payload = message.payload || {};

      // Log
      console.log('Message', command, message, event.source);

      // Handle the message
      if (command === 'uj-vert-unit:set-dimensions') {
        // Get the iframe
        var $iframe = select('#uj-vert-' + payload.id).get(0);

        // Set the dimensions
        $iframe.style.height = payload.height + 'px';
      } else if (command === 'uj-vert-unit:click') {
        // Navigate to the URL
        window.location.href = payload.url;
      }
    }

    // Fill the units
    function fillUnits() {
      var $vertSlots = select(vertSlotsSelector);
      var adURL = qsDebug === 'true'
        ? 'http://localhost:4001/verts/main'
        : 'https://promo-server.itwcreativeworks.com/verts/main'

      adURL = new URL(adURL);

      adURL.searchParams.set('parentURL', window.location.href);

      // Load the ad units
      $vertSlots.each(function ($el) {
        var isFilled = $el.matches('[data-ad-status="filled"]');
        var finalURL = new URL(adURL.toString());

        finalURL.searchParams.set('frameId', slotId);

        finalURL = finalURL.toString();

        if (isFilled) {
          return;
        }

        console.log('Loading ad unit', $el, finalURL);

        $el.innerHTML = $iframeVertTemplate
          .replace(/{id}/g, 'uj-vert-' + slotId++)
          .replace(/{src}/g, finalURL)
      })
    }

    window.addEventListener('message', messageHandler, false);

    // Load the antivert checker
    Manager.dom().loadScript({src: scriptUrl})
    .then(function (e) {
      console.log('Loaded antivert checker', e);
    })
    .catch(function (e) {
      console.error('Failed to load antivert checker', e);
    })
    .finally(function () {
      var filledChecker = setInterval(function () {
        // Check if there are unfilled units
        var isOGVertsLoaded = select(vertSlotsSelectorLoaded).elements.exists;
        var isUnfilledVertSlots = select(vertSlotsSelectorUnfilled).elements.exists;

        // Set the global adblocker enabled flag
        Manager.properties.global.isAdblockerEnabled = !select('#uj-antivert-detector').elements.exists;

        var needsCustomVerts = isUnfilledVertSlots
          || Manager.properties.global.isAdblockerEnabled
          || Manager.properties.meta.environment === 'development';

        console.log(
          'isAdblockerEnabled', Manager.properties.global.isAdblockerEnabled,
          'isOGVertsLoaded', isOGVertsLoaded,
          'isUnfilledVertSlots', isUnfilledVertSlots,
          'needsCustomVerts', needsCustomVerts,
        );

        if (!Manager.properties.global.isAdblockerEnabled && !isOGVertsLoaded) {
          return;
        }

        if (isOGVertsLoaded && !isUnfilledVertSlots) {
          return clearInterval(filledChecker);
        }

        if (needsCustomVerts) {
          clearInterval(filledChecker);
          fillUnits();
        } else {
          return;
        }
      }, 300)
    })
  })
</script>

  </ad-unit>

<p>Between ads</p>

<!-- Adunit - Adsense In-article Ad -->
<ad-unit><div class="uj-vert-unit" data-plan-id="basic" data-plan-visibility="visible">
      <ins class="adsbygoogle" style="display:block; text-align:center; " data-ad-layout="in-article" data-ad-format="fluid" data-ad-client="ca-pub-7372939085773564" data-ad-slot="7329067043"></ins>
      <script>
          (adsbygoogle = window.adsbygoogle || []).push({});
      </script>
    </div>

    <div id="vert-template" data-template="
  &lt;iframe
    id=&quot;{id}&quot;
    name=&quot;{id}&quot;
    class=&quot;lazyload&quot;
    style=&quot;max-height: 300px;&quot;
    sandbox=&quot;allow-forms allow-popups allow-popups-to-escape-sandbox allow-same-origin allow-scripts allow-top-navigation-by-user-activation&quot;
    width=&quot;100%&quot; height=&quot;100%&quot;
    frameborder=&quot;0&quot; marginwidth=&quot;0&quot; marginheight=&quot;0&quot; vspace=&quot;0&quot; hspace=&quot;0&quot; allowtransparency=&quot;true&quot; scrolling=&quot;no&quot;
    data-src=&quot;{src}&quot;
  &gt;&lt;/iframe&gt;
">
</div>

<script async="" defer="" type="text/javascript">
  Manager.ready(function () {
    var select = Manager.dom().select;
    var $adblockerDetector = select('#uj-antivert-detector');

    var $iframeVertTemplate = select('#vert-template').get(0).getAttribute('data-template');

    var qsDebug = new URLSearchParams(window.location.search).get('debug');

    var slotId = new Date().getTime();

    // Selectors
    var vertSlotsSelector = '.uj-vert-unit';
    var vertSlotsSelectorLoaded = '.uj-vert-unit ins[data-ad-status]';
    var vertSlotsSelectorUnfilled = '.uj-vert-unit ins[data-ad-status="unfilled"]';

    // Set the anti-vert detector script
    /*
      /bottomads.js
      /content-ads.js
      /core-ads.js
      /insertads.js
      /jsads.js
      /left_ads.js
      /nativeads.js
      /openads.js
      /popupads.js
      /ads/ad-script.js?ad_network=123&tracking_id=abc#banner_ad
    */
    // var scriptUrl = '/assets/js/taboola_ads.js';
    // var scriptUrl = 'assets/js/pagead.js';
    var scriptUrl = '/assets/js/popupads.js?t1=/ads/native.js&t2=/nativeads.js&t3=/content-ads.js&cb=' + Manager.properties.global.cacheBreaker;

    // Check if being handled already
    if (window._ujVertCheckerStarted) {
      return;
    }
    window._ujVertCheckerStarted = true;

    // Message Handler
    function messageHandler(event) {
      var message = event.data || {};
      var command = message.command || '';
      var payload = message.payload || {};

      // Log
      console.log('Message', command, message, event.source);

      // Handle the message
      if (command === 'uj-vert-unit:set-dimensions') {
        // Get the iframe
        var $iframe = select('#uj-vert-' + payload.id).get(0);

        // Set the dimensions
        $iframe.style.height = payload.height + 'px';
      } else if (command === 'uj-vert-unit:click') {
        // Navigate to the URL
        window.location.href = payload.url;
      }
    }

    // Fill the units
    function fillUnits() {
      var $vertSlots = select(vertSlotsSelector);
      var adURL = qsDebug === 'true'
        ? 'http://localhost:4001/verts/main'
        : 'https://promo-server.itwcreativeworks.com/verts/main'

      adURL = new URL(adURL);

      adURL.searchParams.set('parentURL', window.location.href);

      // Load the ad units
      $vertSlots.each(function ($el) {
        var isFilled = $el.matches('[data-ad-status="filled"]');
        var finalURL = new URL(adURL.toString());

        finalURL.searchParams.set('frameId', slotId);

        finalURL = finalURL.toString();

        if (isFilled) {
          return;
        }

        console.log('Loading ad unit', $el, finalURL);

        $el.innerHTML = $iframeVertTemplate
          .replace(/{id}/g, 'uj-vert-' + slotId++)
          .replace(/{src}/g, finalURL)
      })
    }

    window.addEventListener('message', messageHandler, false);

    // Load the antivert checker
    Manager.dom().loadScript({src: scriptUrl})
    .then(function (e) {
      console.log('Loaded antivert checker', e);
    })
    .catch(function (e) {
      console.error('Failed to load antivert checker', e);
    })
    .finally(function () {
      var filledChecker = setInterval(function () {
        // Check if there are unfilled units
        var isOGVertsLoaded = select(vertSlotsSelectorLoaded).elements.exists;
        var isUnfilledVertSlots = select(vertSlotsSelectorUnfilled).elements.exists;

        // Set the global adblocker enabled flag
        Manager.properties.global.isAdblockerEnabled = !select('#uj-antivert-detector').elements.exists;

        var needsCustomVerts = isUnfilledVertSlots
          || Manager.properties.global.isAdblockerEnabled
          || Manager.properties.meta.environment === 'development';

        console.log(
          'isAdblockerEnabled', Manager.properties.global.isAdblockerEnabled,
          'isOGVertsLoaded', isOGVertsLoaded,
          'isUnfilledVertSlots', isUnfilledVertSlots,
          'needsCustomVerts', needsCustomVerts,
        );

        if (!Manager.properties.global.isAdblockerEnabled && !isOGVertsLoaded) {
          return;
        }

        if (isOGVertsLoaded && !isUnfilledVertSlots) {
          return clearInterval(filledChecker);
        }

        if (needsCustomVerts) {
          clearInterval(filledChecker);
          fillUnits();
        } else {
          return;
        }
      }, 300)
    })
  })
</script>

  </ad-unit>

<p>After ads</p>
",
        "test_4": "<h2 id="title-inside-post-6">Title inside post 6</h2>
<p>This is a wonderful paragrah inside a post! This is post #6 with ID 1722471526.</p>

<p>Before ads</p>

<!-- Adunit - Adsense In-article Ad --><p>Between ads</p>

<!-- Adunit - Adsense In-article Ad --><p>After ads</p>
",
        "test_5": "<h2 id=\"title-inside-post-6\">Title inside post 6</h2>\n<p>This is a wonderful paragrah inside a post! This is post #6 with ID 1722471526.</p>\n\n<p>Before ads</p>\n\n<!-- Adunit - Adsense In-article Ad --><p>Between ads</p>\n\n<!-- Adunit - Adsense In-article Ad --><p>After ads</p>\n",

        "test_6": "<h2 id="title-inside-post-6">Title inside post 6</h2>
<p>This is a wonderful paragrah inside a post! This is post #6 with ID 1722471526.</p>

<p>Before ads</p>

<!-- Adunit - Adsense In-article Ad -->
<ad-unit><div class="uj-vert-unit" data-plan-id="basic" data-plan-visibility="visible">
      <ins class="adsbygoogle" style="display:block; text-align:center; " data-ad-layout="in-article" data-ad-format="fluid" data-ad-client="ca-pub-7372939085773564" data-ad-slot="7329067043"></ins>
      <script>
          (adsbygoogle = window.adsbygoogle || []).push({});
      </script>
    </div>

    <div id="vert-template" data-template="
  &lt;iframe
    id=&quot;{id}&quot;
    name=&quot;{id}&quot;
    class=&quot;lazyload&quot;
    style=&quot;max-height: 300px;&quot;
    sandbox=&quot;allow-forms allow-popups allow-popups-to-escape-sandbox allow-same-origin allow-scripts allow-top-navigation-by-user-activation&quot;
    width=&quot;100%&quot; height=&quot;100%&quot;
    frameborder=&quot;0&quot; marginwidth=&quot;0&quot; marginheight=&quot;0&quot; vspace=&quot;0&quot; hspace=&quot;0&quot; allowtransparency=&quot;true&quot; scrolling=&quot;no&quot;
    data-src=&quot;{src}&quot;
  &gt;&lt;/iframe&gt;
">
</div>

<script async="" defer="" type="text/javascript">
  Manager.ready(function () {
    var select = Manager.dom().select;
    var $adblockerDetector = select('#uj-antivert-detector');

    var $iframeVertTemplate = select('#vert-template').get(0).getAttribute('data-template');

    var qsDebug = new URLSearchParams(window.location.search).get('debug');

    var slotId = new Date().getTime();

    // Selectors
    var vertSlotsSelector = '.uj-vert-unit';
    var vertSlotsSelectorLoaded = '.uj-vert-unit ins[data-ad-status]';
    var vertSlotsSelectorUnfilled = '.uj-vert-unit ins[data-ad-status="unfilled"]';

    // Set the anti-vert detector script
    /*
      /bottomads.js
      /content-ads.js
      /core-ads.js
      /insertads.js
      /jsads.js
      /left_ads.js
      /nativeads.js
      /openads.js
      /popupads.js
      /ads/ad-script.js?ad_network=123&tracking_id=abc#banner_ad
    */
    // var scriptUrl = '/assets/js/taboola_ads.js';
    // var scriptUrl = 'assets/js/pagead.js';
    var scriptUrl = '/assets/js/popupads.js?t1=/ads/native.js&t2=/nativeads.js&t3=/content-ads.js&cb=' + Manager.properties.global.cacheBreaker;

    // Check if being handled already
    if (window._ujVertCheckerStarted) {
      return;
    }
    window._ujVertCheckerStarted = true;

    // Message Handler
    function messageHandler(event) {
      var message = event.data || {};
      var command = message.command || '';
      var payload = message.payload || {};

      // Log
      console.log('Message', command, message, event.source);

      // Handle the message
      if (command === 'uj-vert-unit:set-dimensions') {
        // Get the iframe
        var $iframe = select('#uj-vert-' + payload.id).get(0);

        // Set the dimensions
        $iframe.style.height = payload.height + 'px';
      } else if (command === 'uj-vert-unit:click') {
        // Navigate to the URL
        window.location.href = payload.url;
      }
    }

    // Fill the units
    function fillUnits() {
      var $vertSlots = select(vertSlotsSelector);
      var adURL = qsDebug === 'true'
        ? 'http://localhost:4001/verts/main'
        : 'https://promo-server.itwcreativeworks.com/verts/main'

      adURL = new URL(adURL);

      adURL.searchParams.set('parentURL', window.location.href);

      // Load the ad units
      $vertSlots.each(function ($el) {
        var isFilled = $el.matches('[data-ad-status="filled"]');
        var finalURL = new URL(adURL.toString());

        finalURL.searchParams.set('frameId', slotId);

        finalURL = finalURL.toString();

        if (isFilled) {
          return;
        }

        console.log('Loading ad unit', $el, finalURL);

        $el.innerHTML = $iframeVertTemplate
          .replace(/{id}/g, 'uj-vert-' + slotId++)
          .replace(/{src}/g, finalURL)
      })
    }

    window.addEventListener('message', messageHandler, false);

    // Load the antivert checker
    Manager.dom().loadScript({src: scriptUrl})
    .then(function (e) {
      console.log('Loaded antivert checker', e);
    })
    .catch(function (e) {
      console.error('Failed to load antivert checker', e);
    })
    .finally(function () {
      var filledChecker = setInterval(function () {
        // Check if there are unfilled units
        var isOGVertsLoaded = select(vertSlotsSelectorLoaded).elements.exists;
        var isUnfilledVertSlots = select(vertSlotsSelectorUnfilled).elements.exists;

        // Set the global adblocker enabled flag
        Manager.properties.global.isAdblockerEnabled = !select('#uj-antivert-detector').elements.exists;

        var needsCustomVerts = isUnfilledVertSlots
          || Manager.properties.global.isAdblockerEnabled
          || Manager.properties.meta.environment === 'development';

        console.log(
          'isAdblockerEnabled', Manager.properties.global.isAdblockerEnabled,
          'isOGVertsLoaded', isOGVertsLoaded,
          'isUnfilledVertSlots', isUnfilledVertSlots,
          'needsCustomVerts', needsCustomVerts,
        );

        if (!Manager.properties.global.isAdblockerEnabled && !isOGVertsLoaded) {
          return;
        }

        if (isOGVertsLoaded && !isUnfilledVertSlots) {
          return clearInterval(filledChecker);
        }

        if (needsCustomVerts) {
          clearInterval(filledChecker);
          fillUnits();
        } else {
          return;
        }
      }, 300)
    })
  })
</script>

  </ad-unit>

<p>Between ads</p>

<!-- Adunit - Adsense In-article Ad -->
<ad-unit><div class="uj-vert-unit" data-plan-id="basic" data-plan-visibility="visible">
      <ins class="adsbygoogle" style="display:block; text-align:center; " data-ad-layout="in-article" data-ad-format="fluid" data-ad-client="ca-pub-7372939085773564" data-ad-slot="7329067043"></ins>
      <script>
          (adsbygoogle = window.adsbygoogle || []).push({});
      </script>
    </div>

    <div id="vert-template" data-template="
  &lt;iframe
    id=&quot;{id}&quot;
    name=&quot;{id}&quot;
    class=&quot;lazyload&quot;
    style=&quot;max-height: 300px;&quot;
    sandbox=&quot;allow-forms allow-popups allow-popups-to-escape-sandbox allow-same-origin allow-scripts allow-top-navigation-by-user-activation&quot;
    width=&quot;100%&quot; height=&quot;100%&quot;
    frameborder=&quot;0&quot; marginwidth=&quot;0&quot; marginheight=&quot;0&quot; vspace=&quot;0&quot; hspace=&quot;0&quot; allowtransparency=&quot;true&quot; scrolling=&quot;no&quot;
    data-src=&quot;{src}&quot;
  &gt;&lt;/iframe&gt;
">
</div>

<script async="" defer="" type="text/javascript">
  Manager.ready(function () {
    var select = Manager.dom().select;
    var $adblockerDetector = select('#uj-antivert-detector');

    var $iframeVertTemplate = select('#vert-template').get(0).getAttribute('data-template');

    var qsDebug = new URLSearchParams(window.location.search).get('debug');

    var slotId = new Date().getTime();

    // Selectors
    var vertSlotsSelector = '.uj-vert-unit';
    var vertSlotsSelectorLoaded = '.uj-vert-unit ins[data-ad-status]';
    var vertSlotsSelectorUnfilled = '.uj-vert-unit ins[data-ad-status="unfilled"]';

    // Set the anti-vert detector script
    /*
      /bottomads.js
      /content-ads.js
      /core-ads.js
      /insertads.js
      /jsads.js
      /left_ads.js
      /nativeads.js
      /openads.js
      /popupads.js
      /ads/ad-script.js?ad_network=123&tracking_id=abc#banner_ad
    */
    // var scriptUrl = '/assets/js/taboola_ads.js';
    // var scriptUrl = 'assets/js/pagead.js';
    var scriptUrl = '/assets/js/popupads.js?t1=/ads/native.js&t2=/nativeads.js&t3=/content-ads.js&cb=' + Manager.properties.global.cacheBreaker;

    // Check if being handled already
    if (window._ujVertCheckerStarted) {
      return;
    }
    window._ujVertCheckerStarted = true;

    // Message Handler
    function messageHandler(event) {
      var message = event.data || {};
      var command = message.command || '';
      var payload = message.payload || {};

      // Log
      console.log('Message', command, message, event.source);

      // Handle the message
      if (command === 'uj-vert-unit:set-dimensions') {
        // Get the iframe
        var $iframe = select('#uj-vert-' + payload.id).get(0);

        // Set the dimensions
        $iframe.style.height = payload.height + 'px';
      } else if (command === 'uj-vert-unit:click') {
        // Navigate to the URL
        window.location.href = payload.url;
      }
    }

    // Fill the units
    function fillUnits() {
      var $vertSlots = select(vertSlotsSelector);
      var adURL = qsDebug === 'true'
        ? 'http://localhost:4001/verts/main'
        : 'https://promo-server.itwcreativeworks.com/verts/main'

      adURL = new URL(adURL);

      adURL.searchParams.set('parentURL', window.location.href);

      // Load the ad units
      $vertSlots.each(function ($el) {
        var isFilled = $el.matches('[data-ad-status="filled"]');
        var finalURL = new URL(adURL.toString());

        finalURL.searchParams.set('frameId', slotId);

        finalURL = finalURL.toString();

        if (isFilled) {
          return;
        }

        console.log('Loading ad unit', $el, finalURL);

        $el.innerHTML = $iframeVertTemplate
          .replace(/{id}/g, 'uj-vert-' + slotId++)
          .replace(/{src}/g, finalURL)
      })
    }

    window.addEventListener('message', messageHandler, false);

    // Load the antivert checker
    Manager.dom().loadScript({src: scriptUrl})
    .then(function (e) {
      console.log('Loaded antivert checker', e);
    })
    .catch(function (e) {
      console.error('Failed to load antivert checker', e);
    })
    .finally(function () {
      var filledChecker = setInterval(function () {
        // Check if there are unfilled units
        var isOGVertsLoaded = select(vertSlotsSelectorLoaded).elements.exists;
        var isUnfilledVertSlots = select(vertSlotsSelectorUnfilled).elements.exists;

        // Set the global adblocker enabled flag
        Manager.properties.global.isAdblockerEnabled = !select('#uj-antivert-detector').elements.exists;

        var needsCustomVerts = isUnfilledVertSlots
          || Manager.properties.global.isAdblockerEnabled
          || Manager.properties.meta.environment === 'development';

        console.log(
          'isAdblockerEnabled', Manager.properties.global.isAdblockerEnabled,
          'isOGVertsLoaded', isOGVertsLoaded,
          'isUnfilledVertSlots', isUnfilledVertSlots,
          'needsCustomVerts', needsCustomVerts,
        );

        if (!Manager.properties.global.isAdblockerEnabled && !isOGVertsLoaded) {
          return;
        }

        if (isOGVertsLoaded && !isUnfilledVertSlots) {
          return clearInterval(filledChecker);
        }

        if (needsCustomVerts) {
          clearInterval(filledChecker);
          fillUnits();
        } else {
          return;
        }
      }, 300)
    })
  })
</script>

  </ad-unit>

<p>After ads</p>
",
        "test_7": "<h2 id="title-inside-post-6">Title inside post 6</h2>
<p>This is a wonderful paragrah inside a post! This is post #6 with ID 1722471526.</p>

<p>Before ads</p>

<!-- Adunit - Adsense In-article Ad --><p>Between ads</p>

<!-- Adunit - Adsense In-article Ad --><p>After ads</p>
",
        "test_8": "<h2 id=\"title-inside-post-6\">Title inside post 6</h2>\n<p>This is a wonderful paragrah inside a post! This is post #6 with ID 1722471526.</p>\n\n<p>Before ads</p>\n\n<!-- Adunit - Adsense In-article Ad --><p>Between ads</p>\n\n<!-- Adunit - Adsense In-article Ad --><p>After ads</p>\n",

        "authors": [
          {
            "name": "Ultimate Jekyll"
          }
        ],
        "attachments": [
          {
            "url": ""
          }
        ],
        "summary": "This is a sample excerpt for post number 6",
        "tags": ["Marketing","Business"]
      },
    
      {"id": "https://template.itwcreativeworks.com/blog/example-post-5",
        "url": "https://template.itwcreativeworks.com/blog/example-post-5",
        "title": "Example Post 5",
        "date_published": "2024-01-05T00:00:00.000Z",
        "image": "",
        "content_text": "Title inside post 5 This is a wonderful paragrah inside a post! This is post #5 with ID 1722471525.  Before ads                                     Between ads                                     After ads ",
        "content_html": "<h2 id=\"title-inside-post-5\">Title inside post 5</h2> <p>This is a wonderful paragrah inside a post! This is post #5 with ID 1722471525.</p>  <p>Before ads</p>  <!-- Adunit - Adsense In-article Ad --><p>Between ads</p>  <!-- Adunit - Adsense In-article Ad --><p>After ads</p> ",

        "test_1": "<h2 id="title-inside-post-5">Title inside post 5</h2>
<p>This is a wonderful paragrah inside a post! This is post #5 with ID 1722471525.</p>

<p>Before ads</p>

<!-- Adunit - Adsense In-article Ad -->
<ad-unit><div class="uj-vert-unit" data-plan-id="basic" data-plan-visibility="visible">
      <ins class="adsbygoogle" style="display:block; text-align:center; " data-ad-layout="in-article" data-ad-format="fluid" data-ad-client="ca-pub-7372939085773564" data-ad-slot="7329067043"></ins>
      <script>
          (adsbygoogle = window.adsbygoogle || []).push({});
      </script>
    </div>

    <div id="vert-template" data-template="
  &lt;iframe
    id=&quot;{id}&quot;
    name=&quot;{id}&quot;
    class=&quot;lazyload&quot;
    style=&quot;max-height: 300px;&quot;
    sandbox=&quot;allow-forms allow-popups allow-popups-to-escape-sandbox allow-same-origin allow-scripts allow-top-navigation-by-user-activation&quot;
    width=&quot;100%&quot; height=&quot;100%&quot;
    frameborder=&quot;0&quot; marginwidth=&quot;0&quot; marginheight=&quot;0&quot; vspace=&quot;0&quot; hspace=&quot;0&quot; allowtransparency=&quot;true&quot; scrolling=&quot;no&quot;
    data-src=&quot;{src}&quot;
  &gt;&lt;/iframe&gt;
">
</div>

<script async="" defer="" type="text/javascript">
  Manager.ready(function () {
    var select = Manager.dom().select;
    var $adblockerDetector = select('#uj-antivert-detector');

    var $iframeVertTemplate = select('#vert-template').get(0).getAttribute('data-template');

    var qsDebug = new URLSearchParams(window.location.search).get('debug');

    var slotId = new Date().getTime();

    // Selectors
    var vertSlotsSelector = '.uj-vert-unit';
    var vertSlotsSelectorLoaded = '.uj-vert-unit ins[data-ad-status]';
    var vertSlotsSelectorUnfilled = '.uj-vert-unit ins[data-ad-status="unfilled"]';

    // Set the anti-vert detector script
    /*
      /bottomads.js
      /content-ads.js
      /core-ads.js
      /insertads.js
      /jsads.js
      /left_ads.js
      /nativeads.js
      /openads.js
      /popupads.js
      /ads/ad-script.js?ad_network=123&tracking_id=abc#banner_ad
    */
    // var scriptUrl = '/assets/js/taboola_ads.js';
    // var scriptUrl = 'assets/js/pagead.js';
    var scriptUrl = '/assets/js/popupads.js?t1=/ads/native.js&t2=/nativeads.js&t3=/content-ads.js&cb=' + Manager.properties.global.cacheBreaker;

    // Check if being handled already
    if (window._ujVertCheckerStarted) {
      return;
    }
    window._ujVertCheckerStarted = true;

    // Message Handler
    function messageHandler(event) {
      var message = event.data || {};
      var command = message.command || '';
      var payload = message.payload || {};

      // Log
      console.log('Message', command, message, event.source);

      // Handle the message
      if (command === 'uj-vert-unit:set-dimensions') {
        // Get the iframe
        var $iframe = select('#uj-vert-' + payload.id).get(0);

        // Set the dimensions
        $iframe.style.height = payload.height + 'px';
      } else if (command === 'uj-vert-unit:click') {
        // Navigate to the URL
        window.location.href = payload.url;
      }
    }

    // Fill the units
    function fillUnits() {
      var $vertSlots = select(vertSlotsSelector);
      var adURL = qsDebug === 'true'
        ? 'http://localhost:4001/verts/main'
        : 'https://promo-server.itwcreativeworks.com/verts/main'

      adURL = new URL(adURL);

      adURL.searchParams.set('parentURL', window.location.href);

      // Load the ad units
      $vertSlots.each(function ($el) {
        var isFilled = $el.matches('[data-ad-status="filled"]');
        var finalURL = new URL(adURL.toString());

        finalURL.searchParams.set('frameId', slotId);

        finalURL = finalURL.toString();

        if (isFilled) {
          return;
        }

        console.log('Loading ad unit', $el, finalURL);

        $el.innerHTML = $iframeVertTemplate
          .replace(/{id}/g, 'uj-vert-' + slotId++)
          .replace(/{src}/g, finalURL)
      })
    }

    window.addEventListener('message', messageHandler, false);

    // Load the antivert checker
    Manager.dom().loadScript({src: scriptUrl})
    .then(function (e) {
      console.log('Loaded antivert checker', e);
    })
    .catch(function (e) {
      console.error('Failed to load antivert checker', e);
    })
    .finally(function () {
      var filledChecker = setInterval(function () {
        // Check if there are unfilled units
        var isOGVertsLoaded = select(vertSlotsSelectorLoaded).elements.exists;
        var isUnfilledVertSlots = select(vertSlotsSelectorUnfilled).elements.exists;

        // Set the global adblocker enabled flag
        Manager.properties.global.isAdblockerEnabled = !select('#uj-antivert-detector').elements.exists;

        var needsCustomVerts = isUnfilledVertSlots
          || Manager.properties.global.isAdblockerEnabled
          || Manager.properties.meta.environment === 'development';

        console.log(
          'isAdblockerEnabled', Manager.properties.global.isAdblockerEnabled,
          'isOGVertsLoaded', isOGVertsLoaded,
          'isUnfilledVertSlots', isUnfilledVertSlots,
          'needsCustomVerts', needsCustomVerts,
        );

        if (!Manager.properties.global.isAdblockerEnabled && !isOGVertsLoaded) {
          return;
        }

        if (isOGVertsLoaded && !isUnfilledVertSlots) {
          return clearInterval(filledChecker);
        }

        if (needsCustomVerts) {
          clearInterval(filledChecker);
          fillUnits();
        } else {
          return;
        }
      }, 300)
    })
  })
</script>

  </ad-unit>

<p>Between ads</p>

<!-- Adunit - Adsense In-article Ad -->
<ad-unit><div class="uj-vert-unit" data-plan-id="basic" data-plan-visibility="visible">
      <ins class="adsbygoogle" style="display:block; text-align:center; " data-ad-layout="in-article" data-ad-format="fluid" data-ad-client="ca-pub-7372939085773564" data-ad-slot="7329067043"></ins>
      <script>
          (adsbygoogle = window.adsbygoogle || []).push({});
      </script>
    </div>

    <div id="vert-template" data-template="
  &lt;iframe
    id=&quot;{id}&quot;
    name=&quot;{id}&quot;
    class=&quot;lazyload&quot;
    style=&quot;max-height: 300px;&quot;
    sandbox=&quot;allow-forms allow-popups allow-popups-to-escape-sandbox allow-same-origin allow-scripts allow-top-navigation-by-user-activation&quot;
    width=&quot;100%&quot; height=&quot;100%&quot;
    frameborder=&quot;0&quot; marginwidth=&quot;0&quot; marginheight=&quot;0&quot; vspace=&quot;0&quot; hspace=&quot;0&quot; allowtransparency=&quot;true&quot; scrolling=&quot;no&quot;
    data-src=&quot;{src}&quot;
  &gt;&lt;/iframe&gt;
">
</div>

<script async="" defer="" type="text/javascript">
  Manager.ready(function () {
    var select = Manager.dom().select;
    var $adblockerDetector = select('#uj-antivert-detector');

    var $iframeVertTemplate = select('#vert-template').get(0).getAttribute('data-template');

    var qsDebug = new URLSearchParams(window.location.search).get('debug');

    var slotId = new Date().getTime();

    // Selectors
    var vertSlotsSelector = '.uj-vert-unit';
    var vertSlotsSelectorLoaded = '.uj-vert-unit ins[data-ad-status]';
    var vertSlotsSelectorUnfilled = '.uj-vert-unit ins[data-ad-status="unfilled"]';

    // Set the anti-vert detector script
    /*
      /bottomads.js
      /content-ads.js
      /core-ads.js
      /insertads.js
      /jsads.js
      /left_ads.js
      /nativeads.js
      /openads.js
      /popupads.js
      /ads/ad-script.js?ad_network=123&tracking_id=abc#banner_ad
    */
    // var scriptUrl = '/assets/js/taboola_ads.js';
    // var scriptUrl = 'assets/js/pagead.js';
    var scriptUrl = '/assets/js/popupads.js?t1=/ads/native.js&t2=/nativeads.js&t3=/content-ads.js&cb=' + Manager.properties.global.cacheBreaker;

    // Check if being handled already
    if (window._ujVertCheckerStarted) {
      return;
    }
    window._ujVertCheckerStarted = true;

    // Message Handler
    function messageHandler(event) {
      var message = event.data || {};
      var command = message.command || '';
      var payload = message.payload || {};

      // Log
      console.log('Message', command, message, event.source);

      // Handle the message
      if (command === 'uj-vert-unit:set-dimensions') {
        // Get the iframe
        var $iframe = select('#uj-vert-' + payload.id).get(0);

        // Set the dimensions
        $iframe.style.height = payload.height + 'px';
      } else if (command === 'uj-vert-unit:click') {
        // Navigate to the URL
        window.location.href = payload.url;
      }
    }

    // Fill the units
    function fillUnits() {
      var $vertSlots = select(vertSlotsSelector);
      var adURL = qsDebug === 'true'
        ? 'http://localhost:4001/verts/main'
        : 'https://promo-server.itwcreativeworks.com/verts/main'

      adURL = new URL(adURL);

      adURL.searchParams.set('parentURL', window.location.href);

      // Load the ad units
      $vertSlots.each(function ($el) {
        var isFilled = $el.matches('[data-ad-status="filled"]');
        var finalURL = new URL(adURL.toString());

        finalURL.searchParams.set('frameId', slotId);

        finalURL = finalURL.toString();

        if (isFilled) {
          return;
        }

        console.log('Loading ad unit', $el, finalURL);

        $el.innerHTML = $iframeVertTemplate
          .replace(/{id}/g, 'uj-vert-' + slotId++)
          .replace(/{src}/g, finalURL)
      })
    }

    window.addEventListener('message', messageHandler, false);

    // Load the antivert checker
    Manager.dom().loadScript({src: scriptUrl})
    .then(function (e) {
      console.log('Loaded antivert checker', e);
    })
    .catch(function (e) {
      console.error('Failed to load antivert checker', e);
    })
    .finally(function () {
      var filledChecker = setInterval(function () {
        // Check if there are unfilled units
        var isOGVertsLoaded = select(vertSlotsSelectorLoaded).elements.exists;
        var isUnfilledVertSlots = select(vertSlotsSelectorUnfilled).elements.exists;

        // Set the global adblocker enabled flag
        Manager.properties.global.isAdblockerEnabled = !select('#uj-antivert-detector').elements.exists;

        var needsCustomVerts = isUnfilledVertSlots
          || Manager.properties.global.isAdblockerEnabled
          || Manager.properties.meta.environment === 'development';

        console.log(
          'isAdblockerEnabled', Manager.properties.global.isAdblockerEnabled,
          'isOGVertsLoaded', isOGVertsLoaded,
          'isUnfilledVertSlots', isUnfilledVertSlots,
          'needsCustomVerts', needsCustomVerts,
        );

        if (!Manager.properties.global.isAdblockerEnabled && !isOGVertsLoaded) {
          return;
        }

        if (isOGVertsLoaded && !isUnfilledVertSlots) {
          return clearInterval(filledChecker);
        }

        if (needsCustomVerts) {
          clearInterval(filledChecker);
          fillUnits();
        } else {
          return;
        }
      }, 300)
    })
  })
</script>

  </ad-unit>

<p>After ads</p>
",
        "test_1_2": "<h2 id="title-inside-post-5">Title inside post 5</h2>
<p>This is a wonderful paragrah inside a post! This is post #5 with ID 1722471525.</p>

<p>Before ads</p>

<!-- Adunit - Adsense In-article Ad --><p>Between ads</p>

<!-- Adunit - Adsense In-article Ad --><p>After ads</p>
",
        "test_1_3": "<h2 id=\"title-inside-post-5\">Title inside post 5</h2> <p>This is a wonderful paragrah inside a post! This is post #5 with ID 1722471525.</p>  <p>Before ads</p>  <!-- Adunit - Adsense In-article Ad --> <p>Between ads</p>  <!-- Adunit - Adsense In-article Ad --> <p>After ads</p> ",


        "test_2": "<h2 id="title-inside-post-5">Title inside post 5</h2>
<p>This is a wonderful paragrah inside a post! This is post #5 with ID 1722471525.</p>

<p>Before ads</p>

<!-- Adunit - Adsense In-article Ad -->
<ad-unit><div class="uj-vert-unit" data-plan-id="basic" data-plan-visibility="visible">
      <ins class="adsbygoogle" style="display:block; text-align:center; " data-ad-layout="in-article" data-ad-format="fluid" data-ad-client="ca-pub-7372939085773564" data-ad-slot="7329067043"></ins>
      <script>
          (adsbygoogle = window.adsbygoogle || []).push({});
      </script>
    </div>

    <div id="vert-template" data-template="
  &lt;iframe
    id=&quot;{id}&quot;
    name=&quot;{id}&quot;
    class=&quot;lazyload&quot;
    style=&quot;max-height: 300px;&quot;
    sandbox=&quot;allow-forms allow-popups allow-popups-to-escape-sandbox allow-same-origin allow-scripts allow-top-navigation-by-user-activation&quot;
    width=&quot;100%&quot; height=&quot;100%&quot;
    frameborder=&quot;0&quot; marginwidth=&quot;0&quot; marginheight=&quot;0&quot; vspace=&quot;0&quot; hspace=&quot;0&quot; allowtransparency=&quot;true&quot; scrolling=&quot;no&quot;
    data-src=&quot;{src}&quot;
  &gt;&lt;/iframe&gt;
">
</div>

<script async="" defer="" type="text/javascript">
  Manager.ready(function () {
    var select = Manager.dom().select;
    var $adblockerDetector = select('#uj-antivert-detector');

    var $iframeVertTemplate = select('#vert-template').get(0).getAttribute('data-template');

    var qsDebug = new URLSearchParams(window.location.search).get('debug');

    var slotId = new Date().getTime();

    // Selectors
    var vertSlotsSelector = '.uj-vert-unit';
    var vertSlotsSelectorLoaded = '.uj-vert-unit ins[data-ad-status]';
    var vertSlotsSelectorUnfilled = '.uj-vert-unit ins[data-ad-status="unfilled"]';

    // Set the anti-vert detector script
    /*
      /bottomads.js
      /content-ads.js
      /core-ads.js
      /insertads.js
      /jsads.js
      /left_ads.js
      /nativeads.js
      /openads.js
      /popupads.js
      /ads/ad-script.js?ad_network=123&tracking_id=abc#banner_ad
    */
    // var scriptUrl = '/assets/js/taboola_ads.js';
    // var scriptUrl = 'assets/js/pagead.js';
    var scriptUrl = '/assets/js/popupads.js?t1=/ads/native.js&t2=/nativeads.js&t3=/content-ads.js&cb=' + Manager.properties.global.cacheBreaker;

    // Check if being handled already
    if (window._ujVertCheckerStarted) {
      return;
    }
    window._ujVertCheckerStarted = true;

    // Message Handler
    function messageHandler(event) {
      var message = event.data || {};
      var command = message.command || '';
      var payload = message.payload || {};

      // Log
      console.log('Message', command, message, event.source);

      // Handle the message
      if (command === 'uj-vert-unit:set-dimensions') {
        // Get the iframe
        var $iframe = select('#uj-vert-' + payload.id).get(0);

        // Set the dimensions
        $iframe.style.height = payload.height + 'px';
      } else if (command === 'uj-vert-unit:click') {
        // Navigate to the URL
        window.location.href = payload.url;
      }
    }

    // Fill the units
    function fillUnits() {
      var $vertSlots = select(vertSlotsSelector);
      var adURL = qsDebug === 'true'
        ? 'http://localhost:4001/verts/main'
        : 'https://promo-server.itwcreativeworks.com/verts/main'

      adURL = new URL(adURL);

      adURL.searchParams.set('parentURL', window.location.href);

      // Load the ad units
      $vertSlots.each(function ($el) {
        var isFilled = $el.matches('[data-ad-status="filled"]');
        var finalURL = new URL(adURL.toString());

        finalURL.searchParams.set('frameId', slotId);

        finalURL = finalURL.toString();

        if (isFilled) {
          return;
        }

        console.log('Loading ad unit', $el, finalURL);

        $el.innerHTML = $iframeVertTemplate
          .replace(/{id}/g, 'uj-vert-' + slotId++)
          .replace(/{src}/g, finalURL)
      })
    }

    window.addEventListener('message', messageHandler, false);

    // Load the antivert checker
    Manager.dom().loadScript({src: scriptUrl})
    .then(function (e) {
      console.log('Loaded antivert checker', e);
    })
    .catch(function (e) {
      console.error('Failed to load antivert checker', e);
    })
    .finally(function () {
      var filledChecker = setInterval(function () {
        // Check if there are unfilled units
        var isOGVertsLoaded = select(vertSlotsSelectorLoaded).elements.exists;
        var isUnfilledVertSlots = select(vertSlotsSelectorUnfilled).elements.exists;

        // Set the global adblocker enabled flag
        Manager.properties.global.isAdblockerEnabled = !select('#uj-antivert-detector').elements.exists;

        var needsCustomVerts = isUnfilledVertSlots
          || Manager.properties.global.isAdblockerEnabled
          || Manager.properties.meta.environment === 'development';

        console.log(
          'isAdblockerEnabled', Manager.properties.global.isAdblockerEnabled,
          'isOGVertsLoaded', isOGVertsLoaded,
          'isUnfilledVertSlots', isUnfilledVertSlots,
          'needsCustomVerts', needsCustomVerts,
        );

        if (!Manager.properties.global.isAdblockerEnabled && !isOGVertsLoaded) {
          return;
        }

        if (isOGVertsLoaded && !isUnfilledVertSlots) {
          return clearInterval(filledChecker);
        }

        if (needsCustomVerts) {
          clearInterval(filledChecker);
          fillUnits();
        } else {
          return;
        }
      }, 300)
    })
  })
</script>

  </ad-unit>

<p>Between ads</p>

<!-- Adunit - Adsense In-article Ad -->
<ad-unit><div class="uj-vert-unit" data-plan-id="basic" data-plan-visibility="visible">
      <ins class="adsbygoogle" style="display:block; text-align:center; " data-ad-layout="in-article" data-ad-format="fluid" data-ad-client="ca-pub-7372939085773564" data-ad-slot="7329067043"></ins>
      <script>
          (adsbygoogle = window.adsbygoogle || []).push({});
      </script>
    </div>

    <div id="vert-template" data-template="
  &lt;iframe
    id=&quot;{id}&quot;
    name=&quot;{id}&quot;
    class=&quot;lazyload&quot;
    style=&quot;max-height: 300px;&quot;
    sandbox=&quot;allow-forms allow-popups allow-popups-to-escape-sandbox allow-same-origin allow-scripts allow-top-navigation-by-user-activation&quot;
    width=&quot;100%&quot; height=&quot;100%&quot;
    frameborder=&quot;0&quot; marginwidth=&quot;0&quot; marginheight=&quot;0&quot; vspace=&quot;0&quot; hspace=&quot;0&quot; allowtransparency=&quot;true&quot; scrolling=&quot;no&quot;
    data-src=&quot;{src}&quot;
  &gt;&lt;/iframe&gt;
">
</div>

<script async="" defer="" type="text/javascript">
  Manager.ready(function () {
    var select = Manager.dom().select;
    var $adblockerDetector = select('#uj-antivert-detector');

    var $iframeVertTemplate = select('#vert-template').get(0).getAttribute('data-template');

    var qsDebug = new URLSearchParams(window.location.search).get('debug');

    var slotId = new Date().getTime();

    // Selectors
    var vertSlotsSelector = '.uj-vert-unit';
    var vertSlotsSelectorLoaded = '.uj-vert-unit ins[data-ad-status]';
    var vertSlotsSelectorUnfilled = '.uj-vert-unit ins[data-ad-status="unfilled"]';

    // Set the anti-vert detector script
    /*
      /bottomads.js
      /content-ads.js
      /core-ads.js
      /insertads.js
      /jsads.js
      /left_ads.js
      /nativeads.js
      /openads.js
      /popupads.js
      /ads/ad-script.js?ad_network=123&tracking_id=abc#banner_ad
    */
    // var scriptUrl = '/assets/js/taboola_ads.js';
    // var scriptUrl = 'assets/js/pagead.js';
    var scriptUrl = '/assets/js/popupads.js?t1=/ads/native.js&t2=/nativeads.js&t3=/content-ads.js&cb=' + Manager.properties.global.cacheBreaker;

    // Check if being handled already
    if (window._ujVertCheckerStarted) {
      return;
    }
    window._ujVertCheckerStarted = true;

    // Message Handler
    function messageHandler(event) {
      var message = event.data || {};
      var command = message.command || '';
      var payload = message.payload || {};

      // Log
      console.log('Message', command, message, event.source);

      // Handle the message
      if (command === 'uj-vert-unit:set-dimensions') {
        // Get the iframe
        var $iframe = select('#uj-vert-' + payload.id).get(0);

        // Set the dimensions
        $iframe.style.height = payload.height + 'px';
      } else if (command === 'uj-vert-unit:click') {
        // Navigate to the URL
        window.location.href = payload.url;
      }
    }

    // Fill the units
    function fillUnits() {
      var $vertSlots = select(vertSlotsSelector);
      var adURL = qsDebug === 'true'
        ? 'http://localhost:4001/verts/main'
        : 'https://promo-server.itwcreativeworks.com/verts/main'

      adURL = new URL(adURL);

      adURL.searchParams.set('parentURL', window.location.href);

      // Load the ad units
      $vertSlots.each(function ($el) {
        var isFilled = $el.matches('[data-ad-status="filled"]');
        var finalURL = new URL(adURL.toString());

        finalURL.searchParams.set('frameId', slotId);

        finalURL = finalURL.toString();

        if (isFilled) {
          return;
        }

        console.log('Loading ad unit', $el, finalURL);

        $el.innerHTML = $iframeVertTemplate
          .replace(/{id}/g, 'uj-vert-' + slotId++)
          .replace(/{src}/g, finalURL)
      })
    }

    window.addEventListener('message', messageHandler, false);

    // Load the antivert checker
    Manager.dom().loadScript({src: scriptUrl})
    .then(function (e) {
      console.log('Loaded antivert checker', e);
    })
    .catch(function (e) {
      console.error('Failed to load antivert checker', e);
    })
    .finally(function () {
      var filledChecker = setInterval(function () {
        // Check if there are unfilled units
        var isOGVertsLoaded = select(vertSlotsSelectorLoaded).elements.exists;
        var isUnfilledVertSlots = select(vertSlotsSelectorUnfilled).elements.exists;

        // Set the global adblocker enabled flag
        Manager.properties.global.isAdblockerEnabled = !select('#uj-antivert-detector').elements.exists;

        var needsCustomVerts = isUnfilledVertSlots
          || Manager.properties.global.isAdblockerEnabled
          || Manager.properties.meta.environment === 'development';

        console.log(
          'isAdblockerEnabled', Manager.properties.global.isAdblockerEnabled,
          'isOGVertsLoaded', isOGVertsLoaded,
          'isUnfilledVertSlots', isUnfilledVertSlots,
          'needsCustomVerts', needsCustomVerts,
        );

        if (!Manager.properties.global.isAdblockerEnabled && !isOGVertsLoaded) {
          return;
        }

        if (isOGVertsLoaded && !isUnfilledVertSlots) {
          return clearInterval(filledChecker);
        }

        if (needsCustomVerts) {
          clearInterval(filledChecker);
          fillUnits();
        } else {
          return;
        }
      }, 300)
    })
  })
</script>

  </ad-unit>

<p>After ads</p>
",
        "test_3": "<h2 id="title-inside-post-5">Title inside post 5</h2>
<p>This is a wonderful paragrah inside a post! This is post #5 with ID 1722471525.</p>

<p>Before ads</p>

<!-- Adunit - Adsense In-article Ad -->
<ad-unit><div class="uj-vert-unit" data-plan-id="basic" data-plan-visibility="visible">
      <ins class="adsbygoogle" style="display:block; text-align:center; " data-ad-layout="in-article" data-ad-format="fluid" data-ad-client="ca-pub-7372939085773564" data-ad-slot="7329067043"></ins>
      <script>
          (adsbygoogle = window.adsbygoogle || []).push({});
      </script>
    </div>

    <div id="vert-template" data-template="
  &lt;iframe
    id=&quot;{id}&quot;
    name=&quot;{id}&quot;
    class=&quot;lazyload&quot;
    style=&quot;max-height: 300px;&quot;
    sandbox=&quot;allow-forms allow-popups allow-popups-to-escape-sandbox allow-same-origin allow-scripts allow-top-navigation-by-user-activation&quot;
    width=&quot;100%&quot; height=&quot;100%&quot;
    frameborder=&quot;0&quot; marginwidth=&quot;0&quot; marginheight=&quot;0&quot; vspace=&quot;0&quot; hspace=&quot;0&quot; allowtransparency=&quot;true&quot; scrolling=&quot;no&quot;
    data-src=&quot;{src}&quot;
  &gt;&lt;/iframe&gt;
">
</div>

<script async="" defer="" type="text/javascript">
  Manager.ready(function () {
    var select = Manager.dom().select;
    var $adblockerDetector = select('#uj-antivert-detector');

    var $iframeVertTemplate = select('#vert-template').get(0).getAttribute('data-template');

    var qsDebug = new URLSearchParams(window.location.search).get('debug');

    var slotId = new Date().getTime();

    // Selectors
    var vertSlotsSelector = '.uj-vert-unit';
    var vertSlotsSelectorLoaded = '.uj-vert-unit ins[data-ad-status]';
    var vertSlotsSelectorUnfilled = '.uj-vert-unit ins[data-ad-status="unfilled"]';

    // Set the anti-vert detector script
    /*
      /bottomads.js
      /content-ads.js
      /core-ads.js
      /insertads.js
      /jsads.js
      /left_ads.js
      /nativeads.js
      /openads.js
      /popupads.js
      /ads/ad-script.js?ad_network=123&tracking_id=abc#banner_ad
    */
    // var scriptUrl = '/assets/js/taboola_ads.js';
    // var scriptUrl = 'assets/js/pagead.js';
    var scriptUrl = '/assets/js/popupads.js?t1=/ads/native.js&t2=/nativeads.js&t3=/content-ads.js&cb=' + Manager.properties.global.cacheBreaker;

    // Check if being handled already
    if (window._ujVertCheckerStarted) {
      return;
    }
    window._ujVertCheckerStarted = true;

    // Message Handler
    function messageHandler(event) {
      var message = event.data || {};
      var command = message.command || '';
      var payload = message.payload || {};

      // Log
      console.log('Message', command, message, event.source);

      // Handle the message
      if (command === 'uj-vert-unit:set-dimensions') {
        // Get the iframe
        var $iframe = select('#uj-vert-' + payload.id).get(0);

        // Set the dimensions
        $iframe.style.height = payload.height + 'px';
      } else if (command === 'uj-vert-unit:click') {
        // Navigate to the URL
        window.location.href = payload.url;
      }
    }

    // Fill the units
    function fillUnits() {
      var $vertSlots = select(vertSlotsSelector);
      var adURL = qsDebug === 'true'
        ? 'http://localhost:4001/verts/main'
        : 'https://promo-server.itwcreativeworks.com/verts/main'

      adURL = new URL(adURL);

      adURL.searchParams.set('parentURL', window.location.href);

      // Load the ad units
      $vertSlots.each(function ($el) {
        var isFilled = $el.matches('[data-ad-status="filled"]');
        var finalURL = new URL(adURL.toString());

        finalURL.searchParams.set('frameId', slotId);

        finalURL = finalURL.toString();

        if (isFilled) {
          return;
        }

        console.log('Loading ad unit', $el, finalURL);

        $el.innerHTML = $iframeVertTemplate
          .replace(/{id}/g, 'uj-vert-' + slotId++)
          .replace(/{src}/g, finalURL)
      })
    }

    window.addEventListener('message', messageHandler, false);

    // Load the antivert checker
    Manager.dom().loadScript({src: scriptUrl})
    .then(function (e) {
      console.log('Loaded antivert checker', e);
    })
    .catch(function (e) {
      console.error('Failed to load antivert checker', e);
    })
    .finally(function () {
      var filledChecker = setInterval(function () {
        // Check if there are unfilled units
        var isOGVertsLoaded = select(vertSlotsSelectorLoaded).elements.exists;
        var isUnfilledVertSlots = select(vertSlotsSelectorUnfilled).elements.exists;

        // Set the global adblocker enabled flag
        Manager.properties.global.isAdblockerEnabled = !select('#uj-antivert-detector').elements.exists;

        var needsCustomVerts = isUnfilledVertSlots
          || Manager.properties.global.isAdblockerEnabled
          || Manager.properties.meta.environment === 'development';

        console.log(
          'isAdblockerEnabled', Manager.properties.global.isAdblockerEnabled,
          'isOGVertsLoaded', isOGVertsLoaded,
          'isUnfilledVertSlots', isUnfilledVertSlots,
          'needsCustomVerts', needsCustomVerts,
        );

        if (!Manager.properties.global.isAdblockerEnabled && !isOGVertsLoaded) {
          return;
        }

        if (isOGVertsLoaded && !isUnfilledVertSlots) {
          return clearInterval(filledChecker);
        }

        if (needsCustomVerts) {
          clearInterval(filledChecker);
          fillUnits();
        } else {
          return;
        }
      }, 300)
    })
  })
</script>

  </ad-unit>

<p>Between ads</p>

<!-- Adunit - Adsense In-article Ad -->
<ad-unit><div class="uj-vert-unit" data-plan-id="basic" data-plan-visibility="visible">
      <ins class="adsbygoogle" style="display:block; text-align:center; " data-ad-layout="in-article" data-ad-format="fluid" data-ad-client="ca-pub-7372939085773564" data-ad-slot="7329067043"></ins>
      <script>
          (adsbygoogle = window.adsbygoogle || []).push({});
      </script>
    </div>

    <div id="vert-template" data-template="
  &lt;iframe
    id=&quot;{id}&quot;
    name=&quot;{id}&quot;
    class=&quot;lazyload&quot;
    style=&quot;max-height: 300px;&quot;
    sandbox=&quot;allow-forms allow-popups allow-popups-to-escape-sandbox allow-same-origin allow-scripts allow-top-navigation-by-user-activation&quot;
    width=&quot;100%&quot; height=&quot;100%&quot;
    frameborder=&quot;0&quot; marginwidth=&quot;0&quot; marginheight=&quot;0&quot; vspace=&quot;0&quot; hspace=&quot;0&quot; allowtransparency=&quot;true&quot; scrolling=&quot;no&quot;
    data-src=&quot;{src}&quot;
  &gt;&lt;/iframe&gt;
">
</div>

<script async="" defer="" type="text/javascript">
  Manager.ready(function () {
    var select = Manager.dom().select;
    var $adblockerDetector = select('#uj-antivert-detector');

    var $iframeVertTemplate = select('#vert-template').get(0).getAttribute('data-template');

    var qsDebug = new URLSearchParams(window.location.search).get('debug');

    var slotId = new Date().getTime();

    // Selectors
    var vertSlotsSelector = '.uj-vert-unit';
    var vertSlotsSelectorLoaded = '.uj-vert-unit ins[data-ad-status]';
    var vertSlotsSelectorUnfilled = '.uj-vert-unit ins[data-ad-status="unfilled"]';

    // Set the anti-vert detector script
    /*
      /bottomads.js
      /content-ads.js
      /core-ads.js
      /insertads.js
      /jsads.js
      /left_ads.js
      /nativeads.js
      /openads.js
      /popupads.js
      /ads/ad-script.js?ad_network=123&tracking_id=abc#banner_ad
    */
    // var scriptUrl = '/assets/js/taboola_ads.js';
    // var scriptUrl = 'assets/js/pagead.js';
    var scriptUrl = '/assets/js/popupads.js?t1=/ads/native.js&t2=/nativeads.js&t3=/content-ads.js&cb=' + Manager.properties.global.cacheBreaker;

    // Check if being handled already
    if (window._ujVertCheckerStarted) {
      return;
    }
    window._ujVertCheckerStarted = true;

    // Message Handler
    function messageHandler(event) {
      var message = event.data || {};
      var command = message.command || '';
      var payload = message.payload || {};

      // Log
      console.log('Message', command, message, event.source);

      // Handle the message
      if (command === 'uj-vert-unit:set-dimensions') {
        // Get the iframe
        var $iframe = select('#uj-vert-' + payload.id).get(0);

        // Set the dimensions
        $iframe.style.height = payload.height + 'px';
      } else if (command === 'uj-vert-unit:click') {
        // Navigate to the URL
        window.location.href = payload.url;
      }
    }

    // Fill the units
    function fillUnits() {
      var $vertSlots = select(vertSlotsSelector);
      var adURL = qsDebug === 'true'
        ? 'http://localhost:4001/verts/main'
        : 'https://promo-server.itwcreativeworks.com/verts/main'

      adURL = new URL(adURL);

      adURL.searchParams.set('parentURL', window.location.href);

      // Load the ad units
      $vertSlots.each(function ($el) {
        var isFilled = $el.matches('[data-ad-status="filled"]');
        var finalURL = new URL(adURL.toString());

        finalURL.searchParams.set('frameId', slotId);

        finalURL = finalURL.toString();

        if (isFilled) {
          return;
        }

        console.log('Loading ad unit', $el, finalURL);

        $el.innerHTML = $iframeVertTemplate
          .replace(/{id}/g, 'uj-vert-' + slotId++)
          .replace(/{src}/g, finalURL)
      })
    }

    window.addEventListener('message', messageHandler, false);

    // Load the antivert checker
    Manager.dom().loadScript({src: scriptUrl})
    .then(function (e) {
      console.log('Loaded antivert checker', e);
    })
    .catch(function (e) {
      console.error('Failed to load antivert checker', e);
    })
    .finally(function () {
      var filledChecker = setInterval(function () {
        // Check if there are unfilled units
        var isOGVertsLoaded = select(vertSlotsSelectorLoaded).elements.exists;
        var isUnfilledVertSlots = select(vertSlotsSelectorUnfilled).elements.exists;

        // Set the global adblocker enabled flag
        Manager.properties.global.isAdblockerEnabled = !select('#uj-antivert-detector').elements.exists;

        var needsCustomVerts = isUnfilledVertSlots
          || Manager.properties.global.isAdblockerEnabled
          || Manager.properties.meta.environment === 'development';

        console.log(
          'isAdblockerEnabled', Manager.properties.global.isAdblockerEnabled,
          'isOGVertsLoaded', isOGVertsLoaded,
          'isUnfilledVertSlots', isUnfilledVertSlots,
          'needsCustomVerts', needsCustomVerts,
        );

        if (!Manager.properties.global.isAdblockerEnabled && !isOGVertsLoaded) {
          return;
        }

        if (isOGVertsLoaded && !isUnfilledVertSlots) {
          return clearInterval(filledChecker);
        }

        if (needsCustomVerts) {
          clearInterval(filledChecker);
          fillUnits();
        } else {
          return;
        }
      }, 300)
    })
  })
</script>

  </ad-unit>

<p>After ads</p>
",
        "test_4": "<h2 id="title-inside-post-5">Title inside post 5</h2>
<p>This is a wonderful paragrah inside a post! This is post #5 with ID 1722471525.</p>

<p>Before ads</p>

<!-- Adunit - Adsense In-article Ad --><p>Between ads</p>

<!-- Adunit - Adsense In-article Ad --><p>After ads</p>
",
        "test_5": "<h2 id=\"title-inside-post-5\">Title inside post 5</h2>\n<p>This is a wonderful paragrah inside a post! This is post #5 with ID 1722471525.</p>\n\n<p>Before ads</p>\n\n<!-- Adunit - Adsense In-article Ad --><p>Between ads</p>\n\n<!-- Adunit - Adsense In-article Ad --><p>After ads</p>\n",

        "test_6": "<h2 id="title-inside-post-5">Title inside post 5</h2>
<p>This is a wonderful paragrah inside a post! This is post #5 with ID 1722471525.</p>

<p>Before ads</p>

<!-- Adunit - Adsense In-article Ad -->
<ad-unit><div class="uj-vert-unit" data-plan-id="basic" data-plan-visibility="visible">
      <ins class="adsbygoogle" style="display:block; text-align:center; " data-ad-layout="in-article" data-ad-format="fluid" data-ad-client="ca-pub-7372939085773564" data-ad-slot="7329067043"></ins>
      <script>
          (adsbygoogle = window.adsbygoogle || []).push({});
      </script>
    </div>

    <div id="vert-template" data-template="
  &lt;iframe
    id=&quot;{id}&quot;
    name=&quot;{id}&quot;
    class=&quot;lazyload&quot;
    style=&quot;max-height: 300px;&quot;
    sandbox=&quot;allow-forms allow-popups allow-popups-to-escape-sandbox allow-same-origin allow-scripts allow-top-navigation-by-user-activation&quot;
    width=&quot;100%&quot; height=&quot;100%&quot;
    frameborder=&quot;0&quot; marginwidth=&quot;0&quot; marginheight=&quot;0&quot; vspace=&quot;0&quot; hspace=&quot;0&quot; allowtransparency=&quot;true&quot; scrolling=&quot;no&quot;
    data-src=&quot;{src}&quot;
  &gt;&lt;/iframe&gt;
">
</div>

<script async="" defer="" type="text/javascript">
  Manager.ready(function () {
    var select = Manager.dom().select;
    var $adblockerDetector = select('#uj-antivert-detector');

    var $iframeVertTemplate = select('#vert-template').get(0).getAttribute('data-template');

    var qsDebug = new URLSearchParams(window.location.search).get('debug');

    var slotId = new Date().getTime();

    // Selectors
    var vertSlotsSelector = '.uj-vert-unit';
    var vertSlotsSelectorLoaded = '.uj-vert-unit ins[data-ad-status]';
    var vertSlotsSelectorUnfilled = '.uj-vert-unit ins[data-ad-status="unfilled"]';

    // Set the anti-vert detector script
    /*
      /bottomads.js
      /content-ads.js
      /core-ads.js
      /insertads.js
      /jsads.js
      /left_ads.js
      /nativeads.js
      /openads.js
      /popupads.js
      /ads/ad-script.js?ad_network=123&tracking_id=abc#banner_ad
    */
    // var scriptUrl = '/assets/js/taboola_ads.js';
    // var scriptUrl = 'assets/js/pagead.js';
    var scriptUrl = '/assets/js/popupads.js?t1=/ads/native.js&t2=/nativeads.js&t3=/content-ads.js&cb=' + Manager.properties.global.cacheBreaker;

    // Check if being handled already
    if (window._ujVertCheckerStarted) {
      return;
    }
    window._ujVertCheckerStarted = true;

    // Message Handler
    function messageHandler(event) {
      var message = event.data || {};
      var command = message.command || '';
      var payload = message.payload || {};

      // Log
      console.log('Message', command, message, event.source);

      // Handle the message
      if (command === 'uj-vert-unit:set-dimensions') {
        // Get the iframe
        var $iframe = select('#uj-vert-' + payload.id).get(0);

        // Set the dimensions
        $iframe.style.height = payload.height + 'px';
      } else if (command === 'uj-vert-unit:click') {
        // Navigate to the URL
        window.location.href = payload.url;
      }
    }

    // Fill the units
    function fillUnits() {
      var $vertSlots = select(vertSlotsSelector);
      var adURL = qsDebug === 'true'
        ? 'http://localhost:4001/verts/main'
        : 'https://promo-server.itwcreativeworks.com/verts/main'

      adURL = new URL(adURL);

      adURL.searchParams.set('parentURL', window.location.href);

      // Load the ad units
      $vertSlots.each(function ($el) {
        var isFilled = $el.matches('[data-ad-status="filled"]');
        var finalURL = new URL(adURL.toString());

        finalURL.searchParams.set('frameId', slotId);

        finalURL = finalURL.toString();

        if (isFilled) {
          return;
        }

        console.log('Loading ad unit', $el, finalURL);

        $el.innerHTML = $iframeVertTemplate
          .replace(/{id}/g, 'uj-vert-' + slotId++)
          .replace(/{src}/g, finalURL)
      })
    }

    window.addEventListener('message', messageHandler, false);

    // Load the antivert checker
    Manager.dom().loadScript({src: scriptUrl})
    .then(function (e) {
      console.log('Loaded antivert checker', e);
    })
    .catch(function (e) {
      console.error('Failed to load antivert checker', e);
    })
    .finally(function () {
      var filledChecker = setInterval(function () {
        // Check if there are unfilled units
        var isOGVertsLoaded = select(vertSlotsSelectorLoaded).elements.exists;
        var isUnfilledVertSlots = select(vertSlotsSelectorUnfilled).elements.exists;

        // Set the global adblocker enabled flag
        Manager.properties.global.isAdblockerEnabled = !select('#uj-antivert-detector').elements.exists;

        var needsCustomVerts = isUnfilledVertSlots
          || Manager.properties.global.isAdblockerEnabled
          || Manager.properties.meta.environment === 'development';

        console.log(
          'isAdblockerEnabled', Manager.properties.global.isAdblockerEnabled,
          'isOGVertsLoaded', isOGVertsLoaded,
          'isUnfilledVertSlots', isUnfilledVertSlots,
          'needsCustomVerts', needsCustomVerts,
        );

        if (!Manager.properties.global.isAdblockerEnabled && !isOGVertsLoaded) {
          return;
        }

        if (isOGVertsLoaded && !isUnfilledVertSlots) {
          return clearInterval(filledChecker);
        }

        if (needsCustomVerts) {
          clearInterval(filledChecker);
          fillUnits();
        } else {
          return;
        }
      }, 300)
    })
  })
</script>

  </ad-unit>

<p>Between ads</p>

<!-- Adunit - Adsense In-article Ad -->
<ad-unit><div class="uj-vert-unit" data-plan-id="basic" data-plan-visibility="visible">
      <ins class="adsbygoogle" style="display:block; text-align:center; " data-ad-layout="in-article" data-ad-format="fluid" data-ad-client="ca-pub-7372939085773564" data-ad-slot="7329067043"></ins>
      <script>
          (adsbygoogle = window.adsbygoogle || []).push({});
      </script>
    </div>

    <div id="vert-template" data-template="
  &lt;iframe
    id=&quot;{id}&quot;
    name=&quot;{id}&quot;
    class=&quot;lazyload&quot;
    style=&quot;max-height: 300px;&quot;
    sandbox=&quot;allow-forms allow-popups allow-popups-to-escape-sandbox allow-same-origin allow-scripts allow-top-navigation-by-user-activation&quot;
    width=&quot;100%&quot; height=&quot;100%&quot;
    frameborder=&quot;0&quot; marginwidth=&quot;0&quot; marginheight=&quot;0&quot; vspace=&quot;0&quot; hspace=&quot;0&quot; allowtransparency=&quot;true&quot; scrolling=&quot;no&quot;
    data-src=&quot;{src}&quot;
  &gt;&lt;/iframe&gt;
">
</div>

<script async="" defer="" type="text/javascript">
  Manager.ready(function () {
    var select = Manager.dom().select;
    var $adblockerDetector = select('#uj-antivert-detector');

    var $iframeVertTemplate = select('#vert-template').get(0).getAttribute('data-template');

    var qsDebug = new URLSearchParams(window.location.search).get('debug');

    var slotId = new Date().getTime();

    // Selectors
    var vertSlotsSelector = '.uj-vert-unit';
    var vertSlotsSelectorLoaded = '.uj-vert-unit ins[data-ad-status]';
    var vertSlotsSelectorUnfilled = '.uj-vert-unit ins[data-ad-status="unfilled"]';

    // Set the anti-vert detector script
    /*
      /bottomads.js
      /content-ads.js
      /core-ads.js
      /insertads.js
      /jsads.js
      /left_ads.js
      /nativeads.js
      /openads.js
      /popupads.js
      /ads/ad-script.js?ad_network=123&tracking_id=abc#banner_ad
    */
    // var scriptUrl = '/assets/js/taboola_ads.js';
    // var scriptUrl = 'assets/js/pagead.js';
    var scriptUrl = '/assets/js/popupads.js?t1=/ads/native.js&t2=/nativeads.js&t3=/content-ads.js&cb=' + Manager.properties.global.cacheBreaker;

    // Check if being handled already
    if (window._ujVertCheckerStarted) {
      return;
    }
    window._ujVertCheckerStarted = true;

    // Message Handler
    function messageHandler(event) {
      var message = event.data || {};
      var command = message.command || '';
      var payload = message.payload || {};

      // Log
      console.log('Message', command, message, event.source);

      // Handle the message
      if (command === 'uj-vert-unit:set-dimensions') {
        // Get the iframe
        var $iframe = select('#uj-vert-' + payload.id).get(0);

        // Set the dimensions
        $iframe.style.height = payload.height + 'px';
      } else if (command === 'uj-vert-unit:click') {
        // Navigate to the URL
        window.location.href = payload.url;
      }
    }

    // Fill the units
    function fillUnits() {
      var $vertSlots = select(vertSlotsSelector);
      var adURL = qsDebug === 'true'
        ? 'http://localhost:4001/verts/main'
        : 'https://promo-server.itwcreativeworks.com/verts/main'

      adURL = new URL(adURL);

      adURL.searchParams.set('parentURL', window.location.href);

      // Load the ad units
      $vertSlots.each(function ($el) {
        var isFilled = $el.matches('[data-ad-status="filled"]');
        var finalURL = new URL(adURL.toString());

        finalURL.searchParams.set('frameId', slotId);

        finalURL = finalURL.toString();

        if (isFilled) {
          return;
        }

        console.log('Loading ad unit', $el, finalURL);

        $el.innerHTML = $iframeVertTemplate
          .replace(/{id}/g, 'uj-vert-' + slotId++)
          .replace(/{src}/g, finalURL)
      })
    }

    window.addEventListener('message', messageHandler, false);

    // Load the antivert checker
    Manager.dom().loadScript({src: scriptUrl})
    .then(function (e) {
      console.log('Loaded antivert checker', e);
    })
    .catch(function (e) {
      console.error('Failed to load antivert checker', e);
    })
    .finally(function () {
      var filledChecker = setInterval(function () {
        // Check if there are unfilled units
        var isOGVertsLoaded = select(vertSlotsSelectorLoaded).elements.exists;
        var isUnfilledVertSlots = select(vertSlotsSelectorUnfilled).elements.exists;

        // Set the global adblocker enabled flag
        Manager.properties.global.isAdblockerEnabled = !select('#uj-antivert-detector').elements.exists;

        var needsCustomVerts = isUnfilledVertSlots
          || Manager.properties.global.isAdblockerEnabled
          || Manager.properties.meta.environment === 'development';

        console.log(
          'isAdblockerEnabled', Manager.properties.global.isAdblockerEnabled,
          'isOGVertsLoaded', isOGVertsLoaded,
          'isUnfilledVertSlots', isUnfilledVertSlots,
          'needsCustomVerts', needsCustomVerts,
        );

        if (!Manager.properties.global.isAdblockerEnabled && !isOGVertsLoaded) {
          return;
        }

        if (isOGVertsLoaded && !isUnfilledVertSlots) {
          return clearInterval(filledChecker);
        }

        if (needsCustomVerts) {
          clearInterval(filledChecker);
          fillUnits();
        } else {
          return;
        }
      }, 300)
    })
  })
</script>

  </ad-unit>

<p>After ads</p>
",
        "test_7": "<h2 id="title-inside-post-5">Title inside post 5</h2>
<p>This is a wonderful paragrah inside a post! This is post #5 with ID 1722471525.</p>

<p>Before ads</p>

<!-- Adunit - Adsense In-article Ad --><p>Between ads</p>

<!-- Adunit - Adsense In-article Ad --><p>After ads</p>
",
        "test_8": "<h2 id=\"title-inside-post-5\">Title inside post 5</h2>\n<p>This is a wonderful paragrah inside a post! This is post #5 with ID 1722471525.</p>\n\n<p>Before ads</p>\n\n<!-- Adunit - Adsense In-article Ad --><p>Between ads</p>\n\n<!-- Adunit - Adsense In-article Ad --><p>After ads</p>\n",

        "authors": [
          {
            "name": "Ultimate Jekyll"
          }
        ],
        "attachments": [
          {
            "url": ""
          }
        ],
        "summary": "This is a sample excerpt for post number 5",
        "tags": ["Marketing","Business"]
      },
    
      {"id": "https://template.itwcreativeworks.com/blog/example-post-4",
        "url": "https://template.itwcreativeworks.com/blog/example-post-4",
        "title": "Example Post 4",
        "date_published": "2024-01-04T00:00:00.000Z",
        "image": "",
        "content_text": "Title inside post 4 This is a wonderful paragrah inside a post! This is post #4 with ID 1722471524.  Before ads                                     Between ads                                     After ads ",
        "content_html": "<h2 id=\"title-inside-post-4\">Title inside post 4</h2> <p>This is a wonderful paragrah inside a post! This is post #4 with ID 1722471524.</p>  <p>Before ads</p>  <!-- Adunit - Adsense In-article Ad --><p>Between ads</p>  <!-- Adunit - Adsense In-article Ad --><p>After ads</p> ",

        "test_1": "<h2 id="title-inside-post-4">Title inside post 4</h2>
<p>This is a wonderful paragrah inside a post! This is post #4 with ID 1722471524.</p>

<p>Before ads</p>

<!-- Adunit - Adsense In-article Ad -->
<ad-unit><div class="uj-vert-unit" data-plan-id="basic" data-plan-visibility="visible">
      <ins class="adsbygoogle" style="display:block; text-align:center; " data-ad-layout="in-article" data-ad-format="fluid" data-ad-client="ca-pub-7372939085773564" data-ad-slot="7329067043"></ins>
      <script>
          (adsbygoogle = window.adsbygoogle || []).push({});
      </script>
    </div>

    <div id="vert-template" data-template="
  &lt;iframe
    id=&quot;{id}&quot;
    name=&quot;{id}&quot;
    class=&quot;lazyload&quot;
    style=&quot;max-height: 300px;&quot;
    sandbox=&quot;allow-forms allow-popups allow-popups-to-escape-sandbox allow-same-origin allow-scripts allow-top-navigation-by-user-activation&quot;
    width=&quot;100%&quot; height=&quot;100%&quot;
    frameborder=&quot;0&quot; marginwidth=&quot;0&quot; marginheight=&quot;0&quot; vspace=&quot;0&quot; hspace=&quot;0&quot; allowtransparency=&quot;true&quot; scrolling=&quot;no&quot;
    data-src=&quot;{src}&quot;
  &gt;&lt;/iframe&gt;
">
</div>

<script async="" defer="" type="text/javascript">
  Manager.ready(function () {
    var select = Manager.dom().select;
    var $adblockerDetector = select('#uj-antivert-detector');

    var $iframeVertTemplate = select('#vert-template').get(0).getAttribute('data-template');

    var qsDebug = new URLSearchParams(window.location.search).get('debug');

    var slotId = new Date().getTime();

    // Selectors
    var vertSlotsSelector = '.uj-vert-unit';
    var vertSlotsSelectorLoaded = '.uj-vert-unit ins[data-ad-status]';
    var vertSlotsSelectorUnfilled = '.uj-vert-unit ins[data-ad-status="unfilled"]';

    // Set the anti-vert detector script
    /*
      /bottomads.js
      /content-ads.js
      /core-ads.js
      /insertads.js
      /jsads.js
      /left_ads.js
      /nativeads.js
      /openads.js
      /popupads.js
      /ads/ad-script.js?ad_network=123&tracking_id=abc#banner_ad
    */
    // var scriptUrl = '/assets/js/taboola_ads.js';
    // var scriptUrl = 'assets/js/pagead.js';
    var scriptUrl = '/assets/js/popupads.js?t1=/ads/native.js&t2=/nativeads.js&t3=/content-ads.js&cb=' + Manager.properties.global.cacheBreaker;

    // Check if being handled already
    if (window._ujVertCheckerStarted) {
      return;
    }
    window._ujVertCheckerStarted = true;

    // Message Handler
    function messageHandler(event) {
      var message = event.data || {};
      var command = message.command || '';
      var payload = message.payload || {};

      // Log
      console.log('Message', command, message, event.source);

      // Handle the message
      if (command === 'uj-vert-unit:set-dimensions') {
        // Get the iframe
        var $iframe = select('#uj-vert-' + payload.id).get(0);

        // Set the dimensions
        $iframe.style.height = payload.height + 'px';
      } else if (command === 'uj-vert-unit:click') {
        // Navigate to the URL
        window.location.href = payload.url;
      }
    }

    // Fill the units
    function fillUnits() {
      var $vertSlots = select(vertSlotsSelector);
      var adURL = qsDebug === 'true'
        ? 'http://localhost:4001/verts/main'
        : 'https://promo-server.itwcreativeworks.com/verts/main'

      adURL = new URL(adURL);

      adURL.searchParams.set('parentURL', window.location.href);

      // Load the ad units
      $vertSlots.each(function ($el) {
        var isFilled = $el.matches('[data-ad-status="filled"]');
        var finalURL = new URL(adURL.toString());

        finalURL.searchParams.set('frameId', slotId);

        finalURL = finalURL.toString();

        if (isFilled) {
          return;
        }

        console.log('Loading ad unit', $el, finalURL);

        $el.innerHTML = $iframeVertTemplate
          .replace(/{id}/g, 'uj-vert-' + slotId++)
          .replace(/{src}/g, finalURL)
      })
    }

    window.addEventListener('message', messageHandler, false);

    // Load the antivert checker
    Manager.dom().loadScript({src: scriptUrl})
    .then(function (e) {
      console.log('Loaded antivert checker', e);
    })
    .catch(function (e) {
      console.error('Failed to load antivert checker', e);
    })
    .finally(function () {
      var filledChecker = setInterval(function () {
        // Check if there are unfilled units
        var isOGVertsLoaded = select(vertSlotsSelectorLoaded).elements.exists;
        var isUnfilledVertSlots = select(vertSlotsSelectorUnfilled).elements.exists;

        // Set the global adblocker enabled flag
        Manager.properties.global.isAdblockerEnabled = !select('#uj-antivert-detector').elements.exists;

        var needsCustomVerts = isUnfilledVertSlots
          || Manager.properties.global.isAdblockerEnabled
          || Manager.properties.meta.environment === 'development';

        console.log(
          'isAdblockerEnabled', Manager.properties.global.isAdblockerEnabled,
          'isOGVertsLoaded', isOGVertsLoaded,
          'isUnfilledVertSlots', isUnfilledVertSlots,
          'needsCustomVerts', needsCustomVerts,
        );

        if (!Manager.properties.global.isAdblockerEnabled && !isOGVertsLoaded) {
          return;
        }

        if (isOGVertsLoaded && !isUnfilledVertSlots) {
          return clearInterval(filledChecker);
        }

        if (needsCustomVerts) {
          clearInterval(filledChecker);
          fillUnits();
        } else {
          return;
        }
      }, 300)
    })
  })
</script>

  </ad-unit>

<p>Between ads</p>

<!-- Adunit - Adsense In-article Ad -->
<ad-unit><div class="uj-vert-unit" data-plan-id="basic" data-plan-visibility="visible">
      <ins class="adsbygoogle" style="display:block; text-align:center; " data-ad-layout="in-article" data-ad-format="fluid" data-ad-client="ca-pub-7372939085773564" data-ad-slot="7329067043"></ins>
      <script>
          (adsbygoogle = window.adsbygoogle || []).push({});
      </script>
    </div>

    <div id="vert-template" data-template="
  &lt;iframe
    id=&quot;{id}&quot;
    name=&quot;{id}&quot;
    class=&quot;lazyload&quot;
    style=&quot;max-height: 300px;&quot;
    sandbox=&quot;allow-forms allow-popups allow-popups-to-escape-sandbox allow-same-origin allow-scripts allow-top-navigation-by-user-activation&quot;
    width=&quot;100%&quot; height=&quot;100%&quot;
    frameborder=&quot;0&quot; marginwidth=&quot;0&quot; marginheight=&quot;0&quot; vspace=&quot;0&quot; hspace=&quot;0&quot; allowtransparency=&quot;true&quot; scrolling=&quot;no&quot;
    data-src=&quot;{src}&quot;
  &gt;&lt;/iframe&gt;
">
</div>

<script async="" defer="" type="text/javascript">
  Manager.ready(function () {
    var select = Manager.dom().select;
    var $adblockerDetector = select('#uj-antivert-detector');

    var $iframeVertTemplate = select('#vert-template').get(0).getAttribute('data-template');

    var qsDebug = new URLSearchParams(window.location.search).get('debug');

    var slotId = new Date().getTime();

    // Selectors
    var vertSlotsSelector = '.uj-vert-unit';
    var vertSlotsSelectorLoaded = '.uj-vert-unit ins[data-ad-status]';
    var vertSlotsSelectorUnfilled = '.uj-vert-unit ins[data-ad-status="unfilled"]';

    // Set the anti-vert detector script
    /*
      /bottomads.js
      /content-ads.js
      /core-ads.js
      /insertads.js
      /jsads.js
      /left_ads.js
      /nativeads.js
      /openads.js
      /popupads.js
      /ads/ad-script.js?ad_network=123&tracking_id=abc#banner_ad
    */
    // var scriptUrl = '/assets/js/taboola_ads.js';
    // var scriptUrl = 'assets/js/pagead.js';
    var scriptUrl = '/assets/js/popupads.js?t1=/ads/native.js&t2=/nativeads.js&t3=/content-ads.js&cb=' + Manager.properties.global.cacheBreaker;

    // Check if being handled already
    if (window._ujVertCheckerStarted) {
      return;
    }
    window._ujVertCheckerStarted = true;

    // Message Handler
    function messageHandler(event) {
      var message = event.data || {};
      var command = message.command || '';
      var payload = message.payload || {};

      // Log
      console.log('Message', command, message, event.source);

      // Handle the message
      if (command === 'uj-vert-unit:set-dimensions') {
        // Get the iframe
        var $iframe = select('#uj-vert-' + payload.id).get(0);

        // Set the dimensions
        $iframe.style.height = payload.height + 'px';
      } else if (command === 'uj-vert-unit:click') {
        // Navigate to the URL
        window.location.href = payload.url;
      }
    }

    // Fill the units
    function fillUnits() {
      var $vertSlots = select(vertSlotsSelector);
      var adURL = qsDebug === 'true'
        ? 'http://localhost:4001/verts/main'
        : 'https://promo-server.itwcreativeworks.com/verts/main'

      adURL = new URL(adURL);

      adURL.searchParams.set('parentURL', window.location.href);

      // Load the ad units
      $vertSlots.each(function ($el) {
        var isFilled = $el.matches('[data-ad-status="filled"]');
        var finalURL = new URL(adURL.toString());

        finalURL.searchParams.set('frameId', slotId);

        finalURL = finalURL.toString();

        if (isFilled) {
          return;
        }

        console.log('Loading ad unit', $el, finalURL);

        $el.innerHTML = $iframeVertTemplate
          .replace(/{id}/g, 'uj-vert-' + slotId++)
          .replace(/{src}/g, finalURL)
      })
    }

    window.addEventListener('message', messageHandler, false);

    // Load the antivert checker
    Manager.dom().loadScript({src: scriptUrl})
    .then(function (e) {
      console.log('Loaded antivert checker', e);
    })
    .catch(function (e) {
      console.error('Failed to load antivert checker', e);
    })
    .finally(function () {
      var filledChecker = setInterval(function () {
        // Check if there are unfilled units
        var isOGVertsLoaded = select(vertSlotsSelectorLoaded).elements.exists;
        var isUnfilledVertSlots = select(vertSlotsSelectorUnfilled).elements.exists;

        // Set the global adblocker enabled flag
        Manager.properties.global.isAdblockerEnabled = !select('#uj-antivert-detector').elements.exists;

        var needsCustomVerts = isUnfilledVertSlots
          || Manager.properties.global.isAdblockerEnabled
          || Manager.properties.meta.environment === 'development';

        console.log(
          'isAdblockerEnabled', Manager.properties.global.isAdblockerEnabled,
          'isOGVertsLoaded', isOGVertsLoaded,
          'isUnfilledVertSlots', isUnfilledVertSlots,
          'needsCustomVerts', needsCustomVerts,
        );

        if (!Manager.properties.global.isAdblockerEnabled && !isOGVertsLoaded) {
          return;
        }

        if (isOGVertsLoaded && !isUnfilledVertSlots) {
          return clearInterval(filledChecker);
        }

        if (needsCustomVerts) {
          clearInterval(filledChecker);
          fillUnits();
        } else {
          return;
        }
      }, 300)
    })
  })
</script>

  </ad-unit>

<p>After ads</p>
",
        "test_1_2": "<h2 id="title-inside-post-4">Title inside post 4</h2>
<p>This is a wonderful paragrah inside a post! This is post #4 with ID 1722471524.</p>

<p>Before ads</p>

<!-- Adunit - Adsense In-article Ad --><p>Between ads</p>

<!-- Adunit - Adsense In-article Ad --><p>After ads</p>
",
        "test_1_3": "<h2 id=\"title-inside-post-4\">Title inside post 4</h2> <p>This is a wonderful paragrah inside a post! This is post #4 with ID 1722471524.</p>  <p>Before ads</p>  <!-- Adunit - Adsense In-article Ad --> <p>Between ads</p>  <!-- Adunit - Adsense In-article Ad --> <p>After ads</p> ",


        "test_2": "<h2 id="title-inside-post-4">Title inside post 4</h2>
<p>This is a wonderful paragrah inside a post! This is post #4 with ID 1722471524.</p>

<p>Before ads</p>

<!-- Adunit - Adsense In-article Ad -->
<ad-unit><div class="uj-vert-unit" data-plan-id="basic" data-plan-visibility="visible">
      <ins class="adsbygoogle" style="display:block; text-align:center; " data-ad-layout="in-article" data-ad-format="fluid" data-ad-client="ca-pub-7372939085773564" data-ad-slot="7329067043"></ins>
      <script>
          (adsbygoogle = window.adsbygoogle || []).push({});
      </script>
    </div>

    <div id="vert-template" data-template="
  &lt;iframe
    id=&quot;{id}&quot;
    name=&quot;{id}&quot;
    class=&quot;lazyload&quot;
    style=&quot;max-height: 300px;&quot;
    sandbox=&quot;allow-forms allow-popups allow-popups-to-escape-sandbox allow-same-origin allow-scripts allow-top-navigation-by-user-activation&quot;
    width=&quot;100%&quot; height=&quot;100%&quot;
    frameborder=&quot;0&quot; marginwidth=&quot;0&quot; marginheight=&quot;0&quot; vspace=&quot;0&quot; hspace=&quot;0&quot; allowtransparency=&quot;true&quot; scrolling=&quot;no&quot;
    data-src=&quot;{src}&quot;
  &gt;&lt;/iframe&gt;
">
</div>

<script async="" defer="" type="text/javascript">
  Manager.ready(function () {
    var select = Manager.dom().select;
    var $adblockerDetector = select('#uj-antivert-detector');

    var $iframeVertTemplate = select('#vert-template').get(0).getAttribute('data-template');

    var qsDebug = new URLSearchParams(window.location.search).get('debug');

    var slotId = new Date().getTime();

    // Selectors
    var vertSlotsSelector = '.uj-vert-unit';
    var vertSlotsSelectorLoaded = '.uj-vert-unit ins[data-ad-status]';
    var vertSlotsSelectorUnfilled = '.uj-vert-unit ins[data-ad-status="unfilled"]';

    // Set the anti-vert detector script
    /*
      /bottomads.js
      /content-ads.js
      /core-ads.js
      /insertads.js
      /jsads.js
      /left_ads.js
      /nativeads.js
      /openads.js
      /popupads.js
      /ads/ad-script.js?ad_network=123&tracking_id=abc#banner_ad
    */
    // var scriptUrl = '/assets/js/taboola_ads.js';
    // var scriptUrl = 'assets/js/pagead.js';
    var scriptUrl = '/assets/js/popupads.js?t1=/ads/native.js&t2=/nativeads.js&t3=/content-ads.js&cb=' + Manager.properties.global.cacheBreaker;

    // Check if being handled already
    if (window._ujVertCheckerStarted) {
      return;
    }
    window._ujVertCheckerStarted = true;

    // Message Handler
    function messageHandler(event) {
      var message = event.data || {};
      var command = message.command || '';
      var payload = message.payload || {};

      // Log
      console.log('Message', command, message, event.source);

      // Handle the message
      if (command === 'uj-vert-unit:set-dimensions') {
        // Get the iframe
        var $iframe = select('#uj-vert-' + payload.id).get(0);

        // Set the dimensions
        $iframe.style.height = payload.height + 'px';
      } else if (command === 'uj-vert-unit:click') {
        // Navigate to the URL
        window.location.href = payload.url;
      }
    }

    // Fill the units
    function fillUnits() {
      var $vertSlots = select(vertSlotsSelector);
      var adURL = qsDebug === 'true'
        ? 'http://localhost:4001/verts/main'
        : 'https://promo-server.itwcreativeworks.com/verts/main'

      adURL = new URL(adURL);

      adURL.searchParams.set('parentURL', window.location.href);

      // Load the ad units
      $vertSlots.each(function ($el) {
        var isFilled = $el.matches('[data-ad-status="filled"]');
        var finalURL = new URL(adURL.toString());

        finalURL.searchParams.set('frameId', slotId);

        finalURL = finalURL.toString();

        if (isFilled) {
          return;
        }

        console.log('Loading ad unit', $el, finalURL);

        $el.innerHTML = $iframeVertTemplate
          .replace(/{id}/g, 'uj-vert-' + slotId++)
          .replace(/{src}/g, finalURL)
      })
    }

    window.addEventListener('message', messageHandler, false);

    // Load the antivert checker
    Manager.dom().loadScript({src: scriptUrl})
    .then(function (e) {
      console.log('Loaded antivert checker', e);
    })
    .catch(function (e) {
      console.error('Failed to load antivert checker', e);
    })
    .finally(function () {
      var filledChecker = setInterval(function () {
        // Check if there are unfilled units
        var isOGVertsLoaded = select(vertSlotsSelectorLoaded).elements.exists;
        var isUnfilledVertSlots = select(vertSlotsSelectorUnfilled).elements.exists;

        // Set the global adblocker enabled flag
        Manager.properties.global.isAdblockerEnabled = !select('#uj-antivert-detector').elements.exists;

        var needsCustomVerts = isUnfilledVertSlots
          || Manager.properties.global.isAdblockerEnabled
          || Manager.properties.meta.environment === 'development';

        console.log(
          'isAdblockerEnabled', Manager.properties.global.isAdblockerEnabled,
          'isOGVertsLoaded', isOGVertsLoaded,
          'isUnfilledVertSlots', isUnfilledVertSlots,
          'needsCustomVerts', needsCustomVerts,
        );

        if (!Manager.properties.global.isAdblockerEnabled && !isOGVertsLoaded) {
          return;
        }

        if (isOGVertsLoaded && !isUnfilledVertSlots) {
          return clearInterval(filledChecker);
        }

        if (needsCustomVerts) {
          clearInterval(filledChecker);
          fillUnits();
        } else {
          return;
        }
      }, 300)
    })
  })
</script>

  </ad-unit>

<p>Between ads</p>

<!-- Adunit - Adsense In-article Ad -->
<ad-unit><div class="uj-vert-unit" data-plan-id="basic" data-plan-visibility="visible">
      <ins class="adsbygoogle" style="display:block; text-align:center; " data-ad-layout="in-article" data-ad-format="fluid" data-ad-client="ca-pub-7372939085773564" data-ad-slot="7329067043"></ins>
      <script>
          (adsbygoogle = window.adsbygoogle || []).push({});
      </script>
    </div>

    <div id="vert-template" data-template="
  &lt;iframe
    id=&quot;{id}&quot;
    name=&quot;{id}&quot;
    class=&quot;lazyload&quot;
    style=&quot;max-height: 300px;&quot;
    sandbox=&quot;allow-forms allow-popups allow-popups-to-escape-sandbox allow-same-origin allow-scripts allow-top-navigation-by-user-activation&quot;
    width=&quot;100%&quot; height=&quot;100%&quot;
    frameborder=&quot;0&quot; marginwidth=&quot;0&quot; marginheight=&quot;0&quot; vspace=&quot;0&quot; hspace=&quot;0&quot; allowtransparency=&quot;true&quot; scrolling=&quot;no&quot;
    data-src=&quot;{src}&quot;
  &gt;&lt;/iframe&gt;
">
</div>

<script async="" defer="" type="text/javascript">
  Manager.ready(function () {
    var select = Manager.dom().select;
    var $adblockerDetector = select('#uj-antivert-detector');

    var $iframeVertTemplate = select('#vert-template').get(0).getAttribute('data-template');

    var qsDebug = new URLSearchParams(window.location.search).get('debug');

    var slotId = new Date().getTime();

    // Selectors
    var vertSlotsSelector = '.uj-vert-unit';
    var vertSlotsSelectorLoaded = '.uj-vert-unit ins[data-ad-status]';
    var vertSlotsSelectorUnfilled = '.uj-vert-unit ins[data-ad-status="unfilled"]';

    // Set the anti-vert detector script
    /*
      /bottomads.js
      /content-ads.js
      /core-ads.js
      /insertads.js
      /jsads.js
      /left_ads.js
      /nativeads.js
      /openads.js
      /popupads.js
      /ads/ad-script.js?ad_network=123&tracking_id=abc#banner_ad
    */
    // var scriptUrl = '/assets/js/taboola_ads.js';
    // var scriptUrl = 'assets/js/pagead.js';
    var scriptUrl = '/assets/js/popupads.js?t1=/ads/native.js&t2=/nativeads.js&t3=/content-ads.js&cb=' + Manager.properties.global.cacheBreaker;

    // Check if being handled already
    if (window._ujVertCheckerStarted) {
      return;
    }
    window._ujVertCheckerStarted = true;

    // Message Handler
    function messageHandler(event) {
      var message = event.data || {};
      var command = message.command || '';
      var payload = message.payload || {};

      // Log
      console.log('Message', command, message, event.source);

      // Handle the message
      if (command === 'uj-vert-unit:set-dimensions') {
        // Get the iframe
        var $iframe = select('#uj-vert-' + payload.id).get(0);

        // Set the dimensions
        $iframe.style.height = payload.height + 'px';
      } else if (command === 'uj-vert-unit:click') {
        // Navigate to the URL
        window.location.href = payload.url;
      }
    }

    // Fill the units
    function fillUnits() {
      var $vertSlots = select(vertSlotsSelector);
      var adURL = qsDebug === 'true'
        ? 'http://localhost:4001/verts/main'
        : 'https://promo-server.itwcreativeworks.com/verts/main'

      adURL = new URL(adURL);

      adURL.searchParams.set('parentURL', window.location.href);

      // Load the ad units
      $vertSlots.each(function ($el) {
        var isFilled = $el.matches('[data-ad-status="filled"]');
        var finalURL = new URL(adURL.toString());

        finalURL.searchParams.set('frameId', slotId);

        finalURL = finalURL.toString();

        if (isFilled) {
          return;
        }

        console.log('Loading ad unit', $el, finalURL);

        $el.innerHTML = $iframeVertTemplate
          .replace(/{id}/g, 'uj-vert-' + slotId++)
          .replace(/{src}/g, finalURL)
      })
    }

    window.addEventListener('message', messageHandler, false);

    // Load the antivert checker
    Manager.dom().loadScript({src: scriptUrl})
    .then(function (e) {
      console.log('Loaded antivert checker', e);
    })
    .catch(function (e) {
      console.error('Failed to load antivert checker', e);
    })
    .finally(function () {
      var filledChecker = setInterval(function () {
        // Check if there are unfilled units
        var isOGVertsLoaded = select(vertSlotsSelectorLoaded).elements.exists;
        var isUnfilledVertSlots = select(vertSlotsSelectorUnfilled).elements.exists;

        // Set the global adblocker enabled flag
        Manager.properties.global.isAdblockerEnabled = !select('#uj-antivert-detector').elements.exists;

        var needsCustomVerts = isUnfilledVertSlots
          || Manager.properties.global.isAdblockerEnabled
          || Manager.properties.meta.environment === 'development';

        console.log(
          'isAdblockerEnabled', Manager.properties.global.isAdblockerEnabled,
          'isOGVertsLoaded', isOGVertsLoaded,
          'isUnfilledVertSlots', isUnfilledVertSlots,
          'needsCustomVerts', needsCustomVerts,
        );

        if (!Manager.properties.global.isAdblockerEnabled && !isOGVertsLoaded) {
          return;
        }

        if (isOGVertsLoaded && !isUnfilledVertSlots) {
          return clearInterval(filledChecker);
        }

        if (needsCustomVerts) {
          clearInterval(filledChecker);
          fillUnits();
        } else {
          return;
        }
      }, 300)
    })
  })
</script>

  </ad-unit>

<p>After ads</p>
",
        "test_3": "<h2 id="title-inside-post-4">Title inside post 4</h2>
<p>This is a wonderful paragrah inside a post! This is post #4 with ID 1722471524.</p>

<p>Before ads</p>

<!-- Adunit - Adsense In-article Ad -->
<ad-unit><div class="uj-vert-unit" data-plan-id="basic" data-plan-visibility="visible">
      <ins class="adsbygoogle" style="display:block; text-align:center; " data-ad-layout="in-article" data-ad-format="fluid" data-ad-client="ca-pub-7372939085773564" data-ad-slot="7329067043"></ins>
      <script>
          (adsbygoogle = window.adsbygoogle || []).push({});
      </script>
    </div>

    <div id="vert-template" data-template="
  &lt;iframe
    id=&quot;{id}&quot;
    name=&quot;{id}&quot;
    class=&quot;lazyload&quot;
    style=&quot;max-height: 300px;&quot;
    sandbox=&quot;allow-forms allow-popups allow-popups-to-escape-sandbox allow-same-origin allow-scripts allow-top-navigation-by-user-activation&quot;
    width=&quot;100%&quot; height=&quot;100%&quot;
    frameborder=&quot;0&quot; marginwidth=&quot;0&quot; marginheight=&quot;0&quot; vspace=&quot;0&quot; hspace=&quot;0&quot; allowtransparency=&quot;true&quot; scrolling=&quot;no&quot;
    data-src=&quot;{src}&quot;
  &gt;&lt;/iframe&gt;
">
</div>

<script async="" defer="" type="text/javascript">
  Manager.ready(function () {
    var select = Manager.dom().select;
    var $adblockerDetector = select('#uj-antivert-detector');

    var $iframeVertTemplate = select('#vert-template').get(0).getAttribute('data-template');

    var qsDebug = new URLSearchParams(window.location.search).get('debug');

    var slotId = new Date().getTime();

    // Selectors
    var vertSlotsSelector = '.uj-vert-unit';
    var vertSlotsSelectorLoaded = '.uj-vert-unit ins[data-ad-status]';
    var vertSlotsSelectorUnfilled = '.uj-vert-unit ins[data-ad-status="unfilled"]';

    // Set the anti-vert detector script
    /*
      /bottomads.js
      /content-ads.js
      /core-ads.js
      /insertads.js
      /jsads.js
      /left_ads.js
      /nativeads.js
      /openads.js
      /popupads.js
      /ads/ad-script.js?ad_network=123&tracking_id=abc#banner_ad
    */
    // var scriptUrl = '/assets/js/taboola_ads.js';
    // var scriptUrl = 'assets/js/pagead.js';
    var scriptUrl = '/assets/js/popupads.js?t1=/ads/native.js&t2=/nativeads.js&t3=/content-ads.js&cb=' + Manager.properties.global.cacheBreaker;

    // Check if being handled already
    if (window._ujVertCheckerStarted) {
      return;
    }
    window._ujVertCheckerStarted = true;

    // Message Handler
    function messageHandler(event) {
      var message = event.data || {};
      var command = message.command || '';
      var payload = message.payload || {};

      // Log
      console.log('Message', command, message, event.source);

      // Handle the message
      if (command === 'uj-vert-unit:set-dimensions') {
        // Get the iframe
        var $iframe = select('#uj-vert-' + payload.id).get(0);

        // Set the dimensions
        $iframe.style.height = payload.height + 'px';
      } else if (command === 'uj-vert-unit:click') {
        // Navigate to the URL
        window.location.href = payload.url;
      }
    }

    // Fill the units
    function fillUnits() {
      var $vertSlots = select(vertSlotsSelector);
      var adURL = qsDebug === 'true'
        ? 'http://localhost:4001/verts/main'
        : 'https://promo-server.itwcreativeworks.com/verts/main'

      adURL = new URL(adURL);

      adURL.searchParams.set('parentURL', window.location.href);

      // Load the ad units
      $vertSlots.each(function ($el) {
        var isFilled = $el.matches('[data-ad-status="filled"]');
        var finalURL = new URL(adURL.toString());

        finalURL.searchParams.set('frameId', slotId);

        finalURL = finalURL.toString();

        if (isFilled) {
          return;
        }

        console.log('Loading ad unit', $el, finalURL);

        $el.innerHTML = $iframeVertTemplate
          .replace(/{id}/g, 'uj-vert-' + slotId++)
          .replace(/{src}/g, finalURL)
      })
    }

    window.addEventListener('message', messageHandler, false);

    // Load the antivert checker
    Manager.dom().loadScript({src: scriptUrl})
    .then(function (e) {
      console.log('Loaded antivert checker', e);
    })
    .catch(function (e) {
      console.error('Failed to load antivert checker', e);
    })
    .finally(function () {
      var filledChecker = setInterval(function () {
        // Check if there are unfilled units
        var isOGVertsLoaded = select(vertSlotsSelectorLoaded).elements.exists;
        var isUnfilledVertSlots = select(vertSlotsSelectorUnfilled).elements.exists;

        // Set the global adblocker enabled flag
        Manager.properties.global.isAdblockerEnabled = !select('#uj-antivert-detector').elements.exists;

        var needsCustomVerts = isUnfilledVertSlots
          || Manager.properties.global.isAdblockerEnabled
          || Manager.properties.meta.environment === 'development';

        console.log(
          'isAdblockerEnabled', Manager.properties.global.isAdblockerEnabled,
          'isOGVertsLoaded', isOGVertsLoaded,
          'isUnfilledVertSlots', isUnfilledVertSlots,
          'needsCustomVerts', needsCustomVerts,
        );

        if (!Manager.properties.global.isAdblockerEnabled && !isOGVertsLoaded) {
          return;
        }

        if (isOGVertsLoaded && !isUnfilledVertSlots) {
          return clearInterval(filledChecker);
        }

        if (needsCustomVerts) {
          clearInterval(filledChecker);
          fillUnits();
        } else {
          return;
        }
      }, 300)
    })
  })
</script>

  </ad-unit>

<p>Between ads</p>

<!-- Adunit - Adsense In-article Ad -->
<ad-unit><div class="uj-vert-unit" data-plan-id="basic" data-plan-visibility="visible">
      <ins class="adsbygoogle" style="display:block; text-align:center; " data-ad-layout="in-article" data-ad-format="fluid" data-ad-client="ca-pub-7372939085773564" data-ad-slot="7329067043"></ins>
      <script>
          (adsbygoogle = window.adsbygoogle || []).push({});
      </script>
    </div>

    <div id="vert-template" data-template="
  &lt;iframe
    id=&quot;{id}&quot;
    name=&quot;{id}&quot;
    class=&quot;lazyload&quot;
    style=&quot;max-height: 300px;&quot;
    sandbox=&quot;allow-forms allow-popups allow-popups-to-escape-sandbox allow-same-origin allow-scripts allow-top-navigation-by-user-activation&quot;
    width=&quot;100%&quot; height=&quot;100%&quot;
    frameborder=&quot;0&quot; marginwidth=&quot;0&quot; marginheight=&quot;0&quot; vspace=&quot;0&quot; hspace=&quot;0&quot; allowtransparency=&quot;true&quot; scrolling=&quot;no&quot;
    data-src=&quot;{src}&quot;
  &gt;&lt;/iframe&gt;
">
</div>

<script async="" defer="" type="text/javascript">
  Manager.ready(function () {
    var select = Manager.dom().select;
    var $adblockerDetector = select('#uj-antivert-detector');

    var $iframeVertTemplate = select('#vert-template').get(0).getAttribute('data-template');

    var qsDebug = new URLSearchParams(window.location.search).get('debug');

    var slotId = new Date().getTime();

    // Selectors
    var vertSlotsSelector = '.uj-vert-unit';
    var vertSlotsSelectorLoaded = '.uj-vert-unit ins[data-ad-status]';
    var vertSlotsSelectorUnfilled = '.uj-vert-unit ins[data-ad-status="unfilled"]';

    // Set the anti-vert detector script
    /*
      /bottomads.js
      /content-ads.js
      /core-ads.js
      /insertads.js
      /jsads.js
      /left_ads.js
      /nativeads.js
      /openads.js
      /popupads.js
      /ads/ad-script.js?ad_network=123&tracking_id=abc#banner_ad
    */
    // var scriptUrl = '/assets/js/taboola_ads.js';
    // var scriptUrl = 'assets/js/pagead.js';
    var scriptUrl = '/assets/js/popupads.js?t1=/ads/native.js&t2=/nativeads.js&t3=/content-ads.js&cb=' + Manager.properties.global.cacheBreaker;

    // Check if being handled already
    if (window._ujVertCheckerStarted) {
      return;
    }
    window._ujVertCheckerStarted = true;

    // Message Handler
    function messageHandler(event) {
      var message = event.data || {};
      var command = message.command || '';
      var payload = message.payload || {};

      // Log
      console.log('Message', command, message, event.source);

      // Handle the message
      if (command === 'uj-vert-unit:set-dimensions') {
        // Get the iframe
        var $iframe = select('#uj-vert-' + payload.id).get(0);

        // Set the dimensions
        $iframe.style.height = payload.height + 'px';
      } else if (command === 'uj-vert-unit:click') {
        // Navigate to the URL
        window.location.href = payload.url;
      }
    }

    // Fill the units
    function fillUnits() {
      var $vertSlots = select(vertSlotsSelector);
      var adURL = qsDebug === 'true'
        ? 'http://localhost:4001/verts/main'
        : 'https://promo-server.itwcreativeworks.com/verts/main'

      adURL = new URL(adURL);

      adURL.searchParams.set('parentURL', window.location.href);

      // Load the ad units
      $vertSlots.each(function ($el) {
        var isFilled = $el.matches('[data-ad-status="filled"]');
        var finalURL = new URL(adURL.toString());

        finalURL.searchParams.set('frameId', slotId);

        finalURL = finalURL.toString();

        if (isFilled) {
          return;
        }

        console.log('Loading ad unit', $el, finalURL);

        $el.innerHTML = $iframeVertTemplate
          .replace(/{id}/g, 'uj-vert-' + slotId++)
          .replace(/{src}/g, finalURL)
      })
    }

    window.addEventListener('message', messageHandler, false);

    // Load the antivert checker
    Manager.dom().loadScript({src: scriptUrl})
    .then(function (e) {
      console.log('Loaded antivert checker', e);
    })
    .catch(function (e) {
      console.error('Failed to load antivert checker', e);
    })
    .finally(function () {
      var filledChecker = setInterval(function () {
        // Check if there are unfilled units
        var isOGVertsLoaded = select(vertSlotsSelectorLoaded).elements.exists;
        var isUnfilledVertSlots = select(vertSlotsSelectorUnfilled).elements.exists;

        // Set the global adblocker enabled flag
        Manager.properties.global.isAdblockerEnabled = !select('#uj-antivert-detector').elements.exists;

        var needsCustomVerts = isUnfilledVertSlots
          || Manager.properties.global.isAdblockerEnabled
          || Manager.properties.meta.environment === 'development';

        console.log(
          'isAdblockerEnabled', Manager.properties.global.isAdblockerEnabled,
          'isOGVertsLoaded', isOGVertsLoaded,
          'isUnfilledVertSlots', isUnfilledVertSlots,
          'needsCustomVerts', needsCustomVerts,
        );

        if (!Manager.properties.global.isAdblockerEnabled && !isOGVertsLoaded) {
          return;
        }

        if (isOGVertsLoaded && !isUnfilledVertSlots) {
          return clearInterval(filledChecker);
        }

        if (needsCustomVerts) {
          clearInterval(filledChecker);
          fillUnits();
        } else {
          return;
        }
      }, 300)
    })
  })
</script>

  </ad-unit>

<p>After ads</p>
",
        "test_4": "<h2 id="title-inside-post-4">Title inside post 4</h2>
<p>This is a wonderful paragrah inside a post! This is post #4 with ID 1722471524.</p>

<p>Before ads</p>

<!-- Adunit - Adsense In-article Ad --><p>Between ads</p>

<!-- Adunit - Adsense In-article Ad --><p>After ads</p>
",
        "test_5": "<h2 id=\"title-inside-post-4\">Title inside post 4</h2>\n<p>This is a wonderful paragrah inside a post! This is post #4 with ID 1722471524.</p>\n\n<p>Before ads</p>\n\n<!-- Adunit - Adsense In-article Ad --><p>Between ads</p>\n\n<!-- Adunit - Adsense In-article Ad --><p>After ads</p>\n",

        "test_6": "<h2 id="title-inside-post-4">Title inside post 4</h2>
<p>This is a wonderful paragrah inside a post! This is post #4 with ID 1722471524.</p>

<p>Before ads</p>

<!-- Adunit - Adsense In-article Ad -->
<ad-unit><div class="uj-vert-unit" data-plan-id="basic" data-plan-visibility="visible">
      <ins class="adsbygoogle" style="display:block; text-align:center; " data-ad-layout="in-article" data-ad-format="fluid" data-ad-client="ca-pub-7372939085773564" data-ad-slot="7329067043"></ins>
      <script>
          (adsbygoogle = window.adsbygoogle || []).push({});
      </script>
    </div>

    <div id="vert-template" data-template="
  &lt;iframe
    id=&quot;{id}&quot;
    name=&quot;{id}&quot;
    class=&quot;lazyload&quot;
    style=&quot;max-height: 300px;&quot;
    sandbox=&quot;allow-forms allow-popups allow-popups-to-escape-sandbox allow-same-origin allow-scripts allow-top-navigation-by-user-activation&quot;
    width=&quot;100%&quot; height=&quot;100%&quot;
    frameborder=&quot;0&quot; marginwidth=&quot;0&quot; marginheight=&quot;0&quot; vspace=&quot;0&quot; hspace=&quot;0&quot; allowtransparency=&quot;true&quot; scrolling=&quot;no&quot;
    data-src=&quot;{src}&quot;
  &gt;&lt;/iframe&gt;
">
</div>

<script async="" defer="" type="text/javascript">
  Manager.ready(function () {
    var select = Manager.dom().select;
    var $adblockerDetector = select('#uj-antivert-detector');

    var $iframeVertTemplate = select('#vert-template').get(0).getAttribute('data-template');

    var qsDebug = new URLSearchParams(window.location.search).get('debug');

    var slotId = new Date().getTime();

    // Selectors
    var vertSlotsSelector = '.uj-vert-unit';
    var vertSlotsSelectorLoaded = '.uj-vert-unit ins[data-ad-status]';
    var vertSlotsSelectorUnfilled = '.uj-vert-unit ins[data-ad-status="unfilled"]';

    // Set the anti-vert detector script
    /*
      /bottomads.js
      /content-ads.js
      /core-ads.js
      /insertads.js
      /jsads.js
      /left_ads.js
      /nativeads.js
      /openads.js
      /popupads.js
      /ads/ad-script.js?ad_network=123&tracking_id=abc#banner_ad
    */
    // var scriptUrl = '/assets/js/taboola_ads.js';
    // var scriptUrl = 'assets/js/pagead.js';
    var scriptUrl = '/assets/js/popupads.js?t1=/ads/native.js&t2=/nativeads.js&t3=/content-ads.js&cb=' + Manager.properties.global.cacheBreaker;

    // Check if being handled already
    if (window._ujVertCheckerStarted) {
      return;
    }
    window._ujVertCheckerStarted = true;

    // Message Handler
    function messageHandler(event) {
      var message = event.data || {};
      var command = message.command || '';
      var payload = message.payload || {};

      // Log
      console.log('Message', command, message, event.source);

      // Handle the message
      if (command === 'uj-vert-unit:set-dimensions') {
        // Get the iframe
        var $iframe = select('#uj-vert-' + payload.id).get(0);

        // Set the dimensions
        $iframe.style.height = payload.height + 'px';
      } else if (command === 'uj-vert-unit:click') {
        // Navigate to the URL
        window.location.href = payload.url;
      }
    }

    // Fill the units
    function fillUnits() {
      var $vertSlots = select(vertSlotsSelector);
      var adURL = qsDebug === 'true'
        ? 'http://localhost:4001/verts/main'
        : 'https://promo-server.itwcreativeworks.com/verts/main'

      adURL = new URL(adURL);

      adURL.searchParams.set('parentURL', window.location.href);

      // Load the ad units
      $vertSlots.each(function ($el) {
        var isFilled = $el.matches('[data-ad-status="filled"]');
        var finalURL = new URL(adURL.toString());

        finalURL.searchParams.set('frameId', slotId);

        finalURL = finalURL.toString();

        if (isFilled) {
          return;
        }

        console.log('Loading ad unit', $el, finalURL);

        $el.innerHTML = $iframeVertTemplate
          .replace(/{id}/g, 'uj-vert-' + slotId++)
          .replace(/{src}/g, finalURL)
      })
    }

    window.addEventListener('message', messageHandler, false);

    // Load the antivert checker
    Manager.dom().loadScript({src: scriptUrl})
    .then(function (e) {
      console.log('Loaded antivert checker', e);
    })
    .catch(function (e) {
      console.error('Failed to load antivert checker', e);
    })
    .finally(function () {
      var filledChecker = setInterval(function () {
        // Check if there are unfilled units
        var isOGVertsLoaded = select(vertSlotsSelectorLoaded).elements.exists;
        var isUnfilledVertSlots = select(vertSlotsSelectorUnfilled).elements.exists;

        // Set the global adblocker enabled flag
        Manager.properties.global.isAdblockerEnabled = !select('#uj-antivert-detector').elements.exists;

        var needsCustomVerts = isUnfilledVertSlots
          || Manager.properties.global.isAdblockerEnabled
          || Manager.properties.meta.environment === 'development';

        console.log(
          'isAdblockerEnabled', Manager.properties.global.isAdblockerEnabled,
          'isOGVertsLoaded', isOGVertsLoaded,
          'isUnfilledVertSlots', isUnfilledVertSlots,
          'needsCustomVerts', needsCustomVerts,
        );

        if (!Manager.properties.global.isAdblockerEnabled && !isOGVertsLoaded) {
          return;
        }

        if (isOGVertsLoaded && !isUnfilledVertSlots) {
          return clearInterval(filledChecker);
        }

        if (needsCustomVerts) {
          clearInterval(filledChecker);
          fillUnits();
        } else {
          return;
        }
      }, 300)
    })
  })
</script>

  </ad-unit>

<p>Between ads</p>

<!-- Adunit - Adsense In-article Ad -->
<ad-unit><div class="uj-vert-unit" data-plan-id="basic" data-plan-visibility="visible">
      <ins class="adsbygoogle" style="display:block; text-align:center; " data-ad-layout="in-article" data-ad-format="fluid" data-ad-client="ca-pub-7372939085773564" data-ad-slot="7329067043"></ins>
      <script>
          (adsbygoogle = window.adsbygoogle || []).push({});
      </script>
    </div>

    <div id="vert-template" data-template="
  &lt;iframe
    id=&quot;{id}&quot;
    name=&quot;{id}&quot;
    class=&quot;lazyload&quot;
    style=&quot;max-height: 300px;&quot;
    sandbox=&quot;allow-forms allow-popups allow-popups-to-escape-sandbox allow-same-origin allow-scripts allow-top-navigation-by-user-activation&quot;
    width=&quot;100%&quot; height=&quot;100%&quot;
    frameborder=&quot;0&quot; marginwidth=&quot;0&quot; marginheight=&quot;0&quot; vspace=&quot;0&quot; hspace=&quot;0&quot; allowtransparency=&quot;true&quot; scrolling=&quot;no&quot;
    data-src=&quot;{src}&quot;
  &gt;&lt;/iframe&gt;
">
</div>

<script async="" defer="" type="text/javascript">
  Manager.ready(function () {
    var select = Manager.dom().select;
    var $adblockerDetector = select('#uj-antivert-detector');

    var $iframeVertTemplate = select('#vert-template').get(0).getAttribute('data-template');

    var qsDebug = new URLSearchParams(window.location.search).get('debug');

    var slotId = new Date().getTime();

    // Selectors
    var vertSlotsSelector = '.uj-vert-unit';
    var vertSlotsSelectorLoaded = '.uj-vert-unit ins[data-ad-status]';
    var vertSlotsSelectorUnfilled = '.uj-vert-unit ins[data-ad-status="unfilled"]';

    // Set the anti-vert detector script
    /*
      /bottomads.js
      /content-ads.js
      /core-ads.js
      /insertads.js
      /jsads.js
      /left_ads.js
      /nativeads.js
      /openads.js
      /popupads.js
      /ads/ad-script.js?ad_network=123&tracking_id=abc#banner_ad
    */
    // var scriptUrl = '/assets/js/taboola_ads.js';
    // var scriptUrl = 'assets/js/pagead.js';
    var scriptUrl = '/assets/js/popupads.js?t1=/ads/native.js&t2=/nativeads.js&t3=/content-ads.js&cb=' + Manager.properties.global.cacheBreaker;

    // Check if being handled already
    if (window._ujVertCheckerStarted) {
      return;
    }
    window._ujVertCheckerStarted = true;

    // Message Handler
    function messageHandler(event) {
      var message = event.data || {};
      var command = message.command || '';
      var payload = message.payload || {};

      // Log
      console.log('Message', command, message, event.source);

      // Handle the message
      if (command === 'uj-vert-unit:set-dimensions') {
        // Get the iframe
        var $iframe = select('#uj-vert-' + payload.id).get(0);

        // Set the dimensions
        $iframe.style.height = payload.height + 'px';
      } else if (command === 'uj-vert-unit:click') {
        // Navigate to the URL
        window.location.href = payload.url;
      }
    }

    // Fill the units
    function fillUnits() {
      var $vertSlots = select(vertSlotsSelector);
      var adURL = qsDebug === 'true'
        ? 'http://localhost:4001/verts/main'
        : 'https://promo-server.itwcreativeworks.com/verts/main'

      adURL = new URL(adURL);

      adURL.searchParams.set('parentURL', window.location.href);

      // Load the ad units
      $vertSlots.each(function ($el) {
        var isFilled = $el.matches('[data-ad-status="filled"]');
        var finalURL = new URL(adURL.toString());

        finalURL.searchParams.set('frameId', slotId);

        finalURL = finalURL.toString();

        if (isFilled) {
          return;
        }

        console.log('Loading ad unit', $el, finalURL);

        $el.innerHTML = $iframeVertTemplate
          .replace(/{id}/g, 'uj-vert-' + slotId++)
          .replace(/{src}/g, finalURL)
      })
    }

    window.addEventListener('message', messageHandler, false);

    // Load the antivert checker
    Manager.dom().loadScript({src: scriptUrl})
    .then(function (e) {
      console.log('Loaded antivert checker', e);
    })
    .catch(function (e) {
      console.error('Failed to load antivert checker', e);
    })
    .finally(function () {
      var filledChecker = setInterval(function () {
        // Check if there are unfilled units
        var isOGVertsLoaded = select(vertSlotsSelectorLoaded).elements.exists;
        var isUnfilledVertSlots = select(vertSlotsSelectorUnfilled).elements.exists;

        // Set the global adblocker enabled flag
        Manager.properties.global.isAdblockerEnabled = !select('#uj-antivert-detector').elements.exists;

        var needsCustomVerts = isUnfilledVertSlots
          || Manager.properties.global.isAdblockerEnabled
          || Manager.properties.meta.environment === 'development';

        console.log(
          'isAdblockerEnabled', Manager.properties.global.isAdblockerEnabled,
          'isOGVertsLoaded', isOGVertsLoaded,
          'isUnfilledVertSlots', isUnfilledVertSlots,
          'needsCustomVerts', needsCustomVerts,
        );

        if (!Manager.properties.global.isAdblockerEnabled && !isOGVertsLoaded) {
          return;
        }

        if (isOGVertsLoaded && !isUnfilledVertSlots) {
          return clearInterval(filledChecker);
        }

        if (needsCustomVerts) {
          clearInterval(filledChecker);
          fillUnits();
        } else {
          return;
        }
      }, 300)
    })
  })
</script>

  </ad-unit>

<p>After ads</p>
",
        "test_7": "<h2 id="title-inside-post-4">Title inside post 4</h2>
<p>This is a wonderful paragrah inside a post! This is post #4 with ID 1722471524.</p>

<p>Before ads</p>

<!-- Adunit - Adsense In-article Ad --><p>Between ads</p>

<!-- Adunit - Adsense In-article Ad --><p>After ads</p>
",
        "test_8": "<h2 id=\"title-inside-post-4\">Title inside post 4</h2>\n<p>This is a wonderful paragrah inside a post! This is post #4 with ID 1722471524.</p>\n\n<p>Before ads</p>\n\n<!-- Adunit - Adsense In-article Ad --><p>Between ads</p>\n\n<!-- Adunit - Adsense In-article Ad --><p>After ads</p>\n",

        "authors": [
          {
            "name": "Ultimate Jekyll"
          }
        ],
        "attachments": [
          {
            "url": ""
          }
        ],
        "summary": "This is a sample excerpt for post number 4",
        "tags": ["Marketing","Business"]
      },
    
      {"id": "https://template.itwcreativeworks.com/blog/example-post-3",
        "url": "https://template.itwcreativeworks.com/blog/example-post-3",
        "title": "Example Post 3",
        "date_published": "2024-01-03T00:00:00.000Z",
        "image": "",
        "content_text": "Title inside post 3 This is a wonderful paragrah inside a post! This is post #3 with ID 1722471523.  Before ads                                     Between ads                                     After ads ",
        "content_html": "<h2 id=\"title-inside-post-3\">Title inside post 3</h2> <p>This is a wonderful paragrah inside a post! This is post #3 with ID 1722471523.</p>  <p>Before ads</p>  <!-- Adunit - Adsense In-article Ad --><p>Between ads</p>  <!-- Adunit - Adsense In-article Ad --><p>After ads</p> ",

        "test_1": "<h2 id="title-inside-post-3">Title inside post 3</h2>
<p>This is a wonderful paragrah inside a post! This is post #3 with ID 1722471523.</p>

<p>Before ads</p>

<!-- Adunit - Adsense In-article Ad -->
<ad-unit><div class="uj-vert-unit" data-plan-id="basic" data-plan-visibility="visible">
      <ins class="adsbygoogle" style="display:block; text-align:center; " data-ad-layout="in-article" data-ad-format="fluid" data-ad-client="ca-pub-7372939085773564" data-ad-slot="7329067043"></ins>
      <script>
          (adsbygoogle = window.adsbygoogle || []).push({});
      </script>
    </div>

    <div id="vert-template" data-template="
  &lt;iframe
    id=&quot;{id}&quot;
    name=&quot;{id}&quot;
    class=&quot;lazyload&quot;
    style=&quot;max-height: 300px;&quot;
    sandbox=&quot;allow-forms allow-popups allow-popups-to-escape-sandbox allow-same-origin allow-scripts allow-top-navigation-by-user-activation&quot;
    width=&quot;100%&quot; height=&quot;100%&quot;
    frameborder=&quot;0&quot; marginwidth=&quot;0&quot; marginheight=&quot;0&quot; vspace=&quot;0&quot; hspace=&quot;0&quot; allowtransparency=&quot;true&quot; scrolling=&quot;no&quot;
    data-src=&quot;{src}&quot;
  &gt;&lt;/iframe&gt;
">
</div>

<script async="" defer="" type="text/javascript">
  Manager.ready(function () {
    var select = Manager.dom().select;
    var $adblockerDetector = select('#uj-antivert-detector');

    var $iframeVertTemplate = select('#vert-template').get(0).getAttribute('data-template');

    var qsDebug = new URLSearchParams(window.location.search).get('debug');

    var slotId = new Date().getTime();

    // Selectors
    var vertSlotsSelector = '.uj-vert-unit';
    var vertSlotsSelectorLoaded = '.uj-vert-unit ins[data-ad-status]';
    var vertSlotsSelectorUnfilled = '.uj-vert-unit ins[data-ad-status="unfilled"]';

    // Set the anti-vert detector script
    /*
      /bottomads.js
      /content-ads.js
      /core-ads.js
      /insertads.js
      /jsads.js
      /left_ads.js
      /nativeads.js
      /openads.js
      /popupads.js
      /ads/ad-script.js?ad_network=123&tracking_id=abc#banner_ad
    */
    // var scriptUrl = '/assets/js/taboola_ads.js';
    // var scriptUrl = 'assets/js/pagead.js';
    var scriptUrl = '/assets/js/popupads.js?t1=/ads/native.js&t2=/nativeads.js&t3=/content-ads.js&cb=' + Manager.properties.global.cacheBreaker;

    // Check if being handled already
    if (window._ujVertCheckerStarted) {
      return;
    }
    window._ujVertCheckerStarted = true;

    // Message Handler
    function messageHandler(event) {
      var message = event.data || {};
      var command = message.command || '';
      var payload = message.payload || {};

      // Log
      console.log('Message', command, message, event.source);

      // Handle the message
      if (command === 'uj-vert-unit:set-dimensions') {
        // Get the iframe
        var $iframe = select('#uj-vert-' + payload.id).get(0);

        // Set the dimensions
        $iframe.style.height = payload.height + 'px';
      } else if (command === 'uj-vert-unit:click') {
        // Navigate to the URL
        window.location.href = payload.url;
      }
    }

    // Fill the units
    function fillUnits() {
      var $vertSlots = select(vertSlotsSelector);
      var adURL = qsDebug === 'true'
        ? 'http://localhost:4001/verts/main'
        : 'https://promo-server.itwcreativeworks.com/verts/main'

      adURL = new URL(adURL);

      adURL.searchParams.set('parentURL', window.location.href);

      // Load the ad units
      $vertSlots.each(function ($el) {
        var isFilled = $el.matches('[data-ad-status="filled"]');
        var finalURL = new URL(adURL.toString());

        finalURL.searchParams.set('frameId', slotId);

        finalURL = finalURL.toString();

        if (isFilled) {
          return;
        }

        console.log('Loading ad unit', $el, finalURL);

        $el.innerHTML = $iframeVertTemplate
          .replace(/{id}/g, 'uj-vert-' + slotId++)
          .replace(/{src}/g, finalURL)
      })
    }

    window.addEventListener('message', messageHandler, false);

    // Load the antivert checker
    Manager.dom().loadScript({src: scriptUrl})
    .then(function (e) {
      console.log('Loaded antivert checker', e);
    })
    .catch(function (e) {
      console.error('Failed to load antivert checker', e);
    })
    .finally(function () {
      var filledChecker = setInterval(function () {
        // Check if there are unfilled units
        var isOGVertsLoaded = select(vertSlotsSelectorLoaded).elements.exists;
        var isUnfilledVertSlots = select(vertSlotsSelectorUnfilled).elements.exists;

        // Set the global adblocker enabled flag
        Manager.properties.global.isAdblockerEnabled = !select('#uj-antivert-detector').elements.exists;

        var needsCustomVerts = isUnfilledVertSlots
          || Manager.properties.global.isAdblockerEnabled
          || Manager.properties.meta.environment === 'development';

        console.log(
          'isAdblockerEnabled', Manager.properties.global.isAdblockerEnabled,
          'isOGVertsLoaded', isOGVertsLoaded,
          'isUnfilledVertSlots', isUnfilledVertSlots,
          'needsCustomVerts', needsCustomVerts,
        );

        if (!Manager.properties.global.isAdblockerEnabled && !isOGVertsLoaded) {
          return;
        }

        if (isOGVertsLoaded && !isUnfilledVertSlots) {
          return clearInterval(filledChecker);
        }

        if (needsCustomVerts) {
          clearInterval(filledChecker);
          fillUnits();
        } else {
          return;
        }
      }, 300)
    })
  })
</script>

  </ad-unit>

<p>Between ads</p>

<!-- Adunit - Adsense In-article Ad -->
<ad-unit><div class="uj-vert-unit" data-plan-id="basic" data-plan-visibility="visible">
      <ins class="adsbygoogle" style="display:block; text-align:center; " data-ad-layout="in-article" data-ad-format="fluid" data-ad-client="ca-pub-7372939085773564" data-ad-slot="7329067043"></ins>
      <script>
          (adsbygoogle = window.adsbygoogle || []).push({});
      </script>
    </div>

    <div id="vert-template" data-template="
  &lt;iframe
    id=&quot;{id}&quot;
    name=&quot;{id}&quot;
    class=&quot;lazyload&quot;
    style=&quot;max-height: 300px;&quot;
    sandbox=&quot;allow-forms allow-popups allow-popups-to-escape-sandbox allow-same-origin allow-scripts allow-top-navigation-by-user-activation&quot;
    width=&quot;100%&quot; height=&quot;100%&quot;
    frameborder=&quot;0&quot; marginwidth=&quot;0&quot; marginheight=&quot;0&quot; vspace=&quot;0&quot; hspace=&quot;0&quot; allowtransparency=&quot;true&quot; scrolling=&quot;no&quot;
    data-src=&quot;{src}&quot;
  &gt;&lt;/iframe&gt;
">
</div>

<script async="" defer="" type="text/javascript">
  Manager.ready(function () {
    var select = Manager.dom().select;
    var $adblockerDetector = select('#uj-antivert-detector');

    var $iframeVertTemplate = select('#vert-template').get(0).getAttribute('data-template');

    var qsDebug = new URLSearchParams(window.location.search).get('debug');

    var slotId = new Date().getTime();

    // Selectors
    var vertSlotsSelector = '.uj-vert-unit';
    var vertSlotsSelectorLoaded = '.uj-vert-unit ins[data-ad-status]';
    var vertSlotsSelectorUnfilled = '.uj-vert-unit ins[data-ad-status="unfilled"]';

    // Set the anti-vert detector script
    /*
      /bottomads.js
      /content-ads.js
      /core-ads.js
      /insertads.js
      /jsads.js
      /left_ads.js
      /nativeads.js
      /openads.js
      /popupads.js
      /ads/ad-script.js?ad_network=123&tracking_id=abc#banner_ad
    */
    // var scriptUrl = '/assets/js/taboola_ads.js';
    // var scriptUrl = 'assets/js/pagead.js';
    var scriptUrl = '/assets/js/popupads.js?t1=/ads/native.js&t2=/nativeads.js&t3=/content-ads.js&cb=' + Manager.properties.global.cacheBreaker;

    // Check if being handled already
    if (window._ujVertCheckerStarted) {
      return;
    }
    window._ujVertCheckerStarted = true;

    // Message Handler
    function messageHandler(event) {
      var message = event.data || {};
      var command = message.command || '';
      var payload = message.payload || {};

      // Log
      console.log('Message', command, message, event.source);

      // Handle the message
      if (command === 'uj-vert-unit:set-dimensions') {
        // Get the iframe
        var $iframe = select('#uj-vert-' + payload.id).get(0);

        // Set the dimensions
        $iframe.style.height = payload.height + 'px';
      } else if (command === 'uj-vert-unit:click') {
        // Navigate to the URL
        window.location.href = payload.url;
      }
    }

    // Fill the units
    function fillUnits() {
      var $vertSlots = select(vertSlotsSelector);
      var adURL = qsDebug === 'true'
        ? 'http://localhost:4001/verts/main'
        : 'https://promo-server.itwcreativeworks.com/verts/main'

      adURL = new URL(adURL);

      adURL.searchParams.set('parentURL', window.location.href);

      // Load the ad units
      $vertSlots.each(function ($el) {
        var isFilled = $el.matches('[data-ad-status="filled"]');
        var finalURL = new URL(adURL.toString());

        finalURL.searchParams.set('frameId', slotId);

        finalURL = finalURL.toString();

        if (isFilled) {
          return;
        }

        console.log('Loading ad unit', $el, finalURL);

        $el.innerHTML = $iframeVertTemplate
          .replace(/{id}/g, 'uj-vert-' + slotId++)
          .replace(/{src}/g, finalURL)
      })
    }

    window.addEventListener('message', messageHandler, false);

    // Load the antivert checker
    Manager.dom().loadScript({src: scriptUrl})
    .then(function (e) {
      console.log('Loaded antivert checker', e);
    })
    .catch(function (e) {
      console.error('Failed to load antivert checker', e);
    })
    .finally(function () {
      var filledChecker = setInterval(function () {
        // Check if there are unfilled units
        var isOGVertsLoaded = select(vertSlotsSelectorLoaded).elements.exists;
        var isUnfilledVertSlots = select(vertSlotsSelectorUnfilled).elements.exists;

        // Set the global adblocker enabled flag
        Manager.properties.global.isAdblockerEnabled = !select('#uj-antivert-detector').elements.exists;

        var needsCustomVerts = isUnfilledVertSlots
          || Manager.properties.global.isAdblockerEnabled
          || Manager.properties.meta.environment === 'development';

        console.log(
          'isAdblockerEnabled', Manager.properties.global.isAdblockerEnabled,
          'isOGVertsLoaded', isOGVertsLoaded,
          'isUnfilledVertSlots', isUnfilledVertSlots,
          'needsCustomVerts', needsCustomVerts,
        );

        if (!Manager.properties.global.isAdblockerEnabled && !isOGVertsLoaded) {
          return;
        }

        if (isOGVertsLoaded && !isUnfilledVertSlots) {
          return clearInterval(filledChecker);
        }

        if (needsCustomVerts) {
          clearInterval(filledChecker);
          fillUnits();
        } else {
          return;
        }
      }, 300)
    })
  })
</script>

  </ad-unit>

<p>After ads</p>
",
        "test_1_2": "<h2 id="title-inside-post-3">Title inside post 3</h2>
<p>This is a wonderful paragrah inside a post! This is post #3 with ID 1722471523.</p>

<p>Before ads</p>

<!-- Adunit - Adsense In-article Ad --><p>Between ads</p>

<!-- Adunit - Adsense In-article Ad --><p>After ads</p>
",
        "test_1_3": "<h2 id=\"title-inside-post-3\">Title inside post 3</h2> <p>This is a wonderful paragrah inside a post! This is post #3 with ID 1722471523.</p>  <p>Before ads</p>  <!-- Adunit - Adsense In-article Ad --> <p>Between ads</p>  <!-- Adunit - Adsense In-article Ad --> <p>After ads</p> ",


        "test_2": "<h2 id="title-inside-post-3">Title inside post 3</h2>
<p>This is a wonderful paragrah inside a post! This is post #3 with ID 1722471523.</p>

<p>Before ads</p>

<!-- Adunit - Adsense In-article Ad -->
<ad-unit><div class="uj-vert-unit" data-plan-id="basic" data-plan-visibility="visible">
      <ins class="adsbygoogle" style="display:block; text-align:center; " data-ad-layout="in-article" data-ad-format="fluid" data-ad-client="ca-pub-7372939085773564" data-ad-slot="7329067043"></ins>
      <script>
          (adsbygoogle = window.adsbygoogle || []).push({});
      </script>
    </div>

    <div id="vert-template" data-template="
  &lt;iframe
    id=&quot;{id}&quot;
    name=&quot;{id}&quot;
    class=&quot;lazyload&quot;
    style=&quot;max-height: 300px;&quot;
    sandbox=&quot;allow-forms allow-popups allow-popups-to-escape-sandbox allow-same-origin allow-scripts allow-top-navigation-by-user-activation&quot;
    width=&quot;100%&quot; height=&quot;100%&quot;
    frameborder=&quot;0&quot; marginwidth=&quot;0&quot; marginheight=&quot;0&quot; vspace=&quot;0&quot; hspace=&quot;0&quot; allowtransparency=&quot;true&quot; scrolling=&quot;no&quot;
    data-src=&quot;{src}&quot;
  &gt;&lt;/iframe&gt;
">
</div>

<script async="" defer="" type="text/javascript">
  Manager.ready(function () {
    var select = Manager.dom().select;
    var $adblockerDetector = select('#uj-antivert-detector');

    var $iframeVertTemplate = select('#vert-template').get(0).getAttribute('data-template');

    var qsDebug = new URLSearchParams(window.location.search).get('debug');

    var slotId = new Date().getTime();

    // Selectors
    var vertSlotsSelector = '.uj-vert-unit';
    var vertSlotsSelectorLoaded = '.uj-vert-unit ins[data-ad-status]';
    var vertSlotsSelectorUnfilled = '.uj-vert-unit ins[data-ad-status="unfilled"]';

    // Set the anti-vert detector script
    /*
      /bottomads.js
      /content-ads.js
      /core-ads.js
      /insertads.js
      /jsads.js
      /left_ads.js
      /nativeads.js
      /openads.js
      /popupads.js
      /ads/ad-script.js?ad_network=123&tracking_id=abc#banner_ad
    */
    // var scriptUrl = '/assets/js/taboola_ads.js';
    // var scriptUrl = 'assets/js/pagead.js';
    var scriptUrl = '/assets/js/popupads.js?t1=/ads/native.js&t2=/nativeads.js&t3=/content-ads.js&cb=' + Manager.properties.global.cacheBreaker;

    // Check if being handled already
    if (window._ujVertCheckerStarted) {
      return;
    }
    window._ujVertCheckerStarted = true;

    // Message Handler
    function messageHandler(event) {
      var message = event.data || {};
      var command = message.command || '';
      var payload = message.payload || {};

      // Log
      console.log('Message', command, message, event.source);

      // Handle the message
      if (command === 'uj-vert-unit:set-dimensions') {
        // Get the iframe
        var $iframe = select('#uj-vert-' + payload.id).get(0);

        // Set the dimensions
        $iframe.style.height = payload.height + 'px';
      } else if (command === 'uj-vert-unit:click') {
        // Navigate to the URL
        window.location.href = payload.url;
      }
    }

    // Fill the units
    function fillUnits() {
      var $vertSlots = select(vertSlotsSelector);
      var adURL = qsDebug === 'true'
        ? 'http://localhost:4001/verts/main'
        : 'https://promo-server.itwcreativeworks.com/verts/main'

      adURL = new URL(adURL);

      adURL.searchParams.set('parentURL', window.location.href);

      // Load the ad units
      $vertSlots.each(function ($el) {
        var isFilled = $el.matches('[data-ad-status="filled"]');
        var finalURL = new URL(adURL.toString());

        finalURL.searchParams.set('frameId', slotId);

        finalURL = finalURL.toString();

        if (isFilled) {
          return;
        }

        console.log('Loading ad unit', $el, finalURL);

        $el.innerHTML = $iframeVertTemplate
          .replace(/{id}/g, 'uj-vert-' + slotId++)
          .replace(/{src}/g, finalURL)
      })
    }

    window.addEventListener('message', messageHandler, false);

    // Load the antivert checker
    Manager.dom().loadScript({src: scriptUrl})
    .then(function (e) {
      console.log('Loaded antivert checker', e);
    })
    .catch(function (e) {
      console.error('Failed to load antivert checker', e);
    })
    .finally(function () {
      var filledChecker = setInterval(function () {
        // Check if there are unfilled units
        var isOGVertsLoaded = select(vertSlotsSelectorLoaded).elements.exists;
        var isUnfilledVertSlots = select(vertSlotsSelectorUnfilled).elements.exists;

        // Set the global adblocker enabled flag
        Manager.properties.global.isAdblockerEnabled = !select('#uj-antivert-detector').elements.exists;

        var needsCustomVerts = isUnfilledVertSlots
          || Manager.properties.global.isAdblockerEnabled
          || Manager.properties.meta.environment === 'development';

        console.log(
          'isAdblockerEnabled', Manager.properties.global.isAdblockerEnabled,
          'isOGVertsLoaded', isOGVertsLoaded,
          'isUnfilledVertSlots', isUnfilledVertSlots,
          'needsCustomVerts', needsCustomVerts,
        );

        if (!Manager.properties.global.isAdblockerEnabled && !isOGVertsLoaded) {
          return;
        }

        if (isOGVertsLoaded && !isUnfilledVertSlots) {
          return clearInterval(filledChecker);
        }

        if (needsCustomVerts) {
          clearInterval(filledChecker);
          fillUnits();
        } else {
          return;
        }
      }, 300)
    })
  })
</script>

  </ad-unit>

<p>Between ads</p>

<!-- Adunit - Adsense In-article Ad -->
<ad-unit><div class="uj-vert-unit" data-plan-id="basic" data-plan-visibility="visible">
      <ins class="adsbygoogle" style="display:block; text-align:center; " data-ad-layout="in-article" data-ad-format="fluid" data-ad-client="ca-pub-7372939085773564" data-ad-slot="7329067043"></ins>
      <script>
          (adsbygoogle = window.adsbygoogle || []).push({});
      </script>
    </div>

    <div id="vert-template" data-template="
  &lt;iframe
    id=&quot;{id}&quot;
    name=&quot;{id}&quot;
    class=&quot;lazyload&quot;
    style=&quot;max-height: 300px;&quot;
    sandbox=&quot;allow-forms allow-popups allow-popups-to-escape-sandbox allow-same-origin allow-scripts allow-top-navigation-by-user-activation&quot;
    width=&quot;100%&quot; height=&quot;100%&quot;
    frameborder=&quot;0&quot; marginwidth=&quot;0&quot; marginheight=&quot;0&quot; vspace=&quot;0&quot; hspace=&quot;0&quot; allowtransparency=&quot;true&quot; scrolling=&quot;no&quot;
    data-src=&quot;{src}&quot;
  &gt;&lt;/iframe&gt;
">
</div>

<script async="" defer="" type="text/javascript">
  Manager.ready(function () {
    var select = Manager.dom().select;
    var $adblockerDetector = select('#uj-antivert-detector');

    var $iframeVertTemplate = select('#vert-template').get(0).getAttribute('data-template');

    var qsDebug = new URLSearchParams(window.location.search).get('debug');

    var slotId = new Date().getTime();

    // Selectors
    var vertSlotsSelector = '.uj-vert-unit';
    var vertSlotsSelectorLoaded = '.uj-vert-unit ins[data-ad-status]';
    var vertSlotsSelectorUnfilled = '.uj-vert-unit ins[data-ad-status="unfilled"]';

    // Set the anti-vert detector script
    /*
      /bottomads.js
      /content-ads.js
      /core-ads.js
      /insertads.js
      /jsads.js
      /left_ads.js
      /nativeads.js
      /openads.js
      /popupads.js
      /ads/ad-script.js?ad_network=123&tracking_id=abc#banner_ad
    */
    // var scriptUrl = '/assets/js/taboola_ads.js';
    // var scriptUrl = 'assets/js/pagead.js';
    var scriptUrl = '/assets/js/popupads.js?t1=/ads/native.js&t2=/nativeads.js&t3=/content-ads.js&cb=' + Manager.properties.global.cacheBreaker;

    // Check if being handled already
    if (window._ujVertCheckerStarted) {
      return;
    }
    window._ujVertCheckerStarted = true;

    // Message Handler
    function messageHandler(event) {
      var message = event.data || {};
      var command = message.command || '';
      var payload = message.payload || {};

      // Log
      console.log('Message', command, message, event.source);

      // Handle the message
      if (command === 'uj-vert-unit:set-dimensions') {
        // Get the iframe
        var $iframe = select('#uj-vert-' + payload.id).get(0);

        // Set the dimensions
        $iframe.style.height = payload.height + 'px';
      } else if (command === 'uj-vert-unit:click') {
        // Navigate to the URL
        window.location.href = payload.url;
      }
    }

    // Fill the units
    function fillUnits() {
      var $vertSlots = select(vertSlotsSelector);
      var adURL = qsDebug === 'true'
        ? 'http://localhost:4001/verts/main'
        : 'https://promo-server.itwcreativeworks.com/verts/main'

      adURL = new URL(adURL);

      adURL.searchParams.set('parentURL', window.location.href);

      // Load the ad units
      $vertSlots.each(function ($el) {
        var isFilled = $el.matches('[data-ad-status="filled"]');
        var finalURL = new URL(adURL.toString());

        finalURL.searchParams.set('frameId', slotId);

        finalURL = finalURL.toString();

        if (isFilled) {
          return;
        }

        console.log('Loading ad unit', $el, finalURL);

        $el.innerHTML = $iframeVertTemplate
          .replace(/{id}/g, 'uj-vert-' + slotId++)
          .replace(/{src}/g, finalURL)
      })
    }

    window.addEventListener('message', messageHandler, false);

    // Load the antivert checker
    Manager.dom().loadScript({src: scriptUrl})
    .then(function (e) {
      console.log('Loaded antivert checker', e);
    })
    .catch(function (e) {
      console.error('Failed to load antivert checker', e);
    })
    .finally(function () {
      var filledChecker = setInterval(function () {
        // Check if there are unfilled units
        var isOGVertsLoaded = select(vertSlotsSelectorLoaded).elements.exists;
        var isUnfilledVertSlots = select(vertSlotsSelectorUnfilled).elements.exists;

        // Set the global adblocker enabled flag
        Manager.properties.global.isAdblockerEnabled = !select('#uj-antivert-detector').elements.exists;

        var needsCustomVerts = isUnfilledVertSlots
          || Manager.properties.global.isAdblockerEnabled
          || Manager.properties.meta.environment === 'development';

        console.log(
          'isAdblockerEnabled', Manager.properties.global.isAdblockerEnabled,
          'isOGVertsLoaded', isOGVertsLoaded,
          'isUnfilledVertSlots', isUnfilledVertSlots,
          'needsCustomVerts', needsCustomVerts,
        );

        if (!Manager.properties.global.isAdblockerEnabled && !isOGVertsLoaded) {
          return;
        }

        if (isOGVertsLoaded && !isUnfilledVertSlots) {
          return clearInterval(filledChecker);
        }

        if (needsCustomVerts) {
          clearInterval(filledChecker);
          fillUnits();
        } else {
          return;
        }
      }, 300)
    })
  })
</script>

  </ad-unit>

<p>After ads</p>
",
        "test_3": "<h2 id="title-inside-post-3">Title inside post 3</h2>
<p>This is a wonderful paragrah inside a post! This is post #3 with ID 1722471523.</p>

<p>Before ads</p>

<!-- Adunit - Adsense In-article Ad -->
<ad-unit><div class="uj-vert-unit" data-plan-id="basic" data-plan-visibility="visible">
      <ins class="adsbygoogle" style="display:block; text-align:center; " data-ad-layout="in-article" data-ad-format="fluid" data-ad-client="ca-pub-7372939085773564" data-ad-slot="7329067043"></ins>
      <script>
          (adsbygoogle = window.adsbygoogle || []).push({});
      </script>
    </div>

    <div id="vert-template" data-template="
  &lt;iframe
    id=&quot;{id}&quot;
    name=&quot;{id}&quot;
    class=&quot;lazyload&quot;
    style=&quot;max-height: 300px;&quot;
    sandbox=&quot;allow-forms allow-popups allow-popups-to-escape-sandbox allow-same-origin allow-scripts allow-top-navigation-by-user-activation&quot;
    width=&quot;100%&quot; height=&quot;100%&quot;
    frameborder=&quot;0&quot; marginwidth=&quot;0&quot; marginheight=&quot;0&quot; vspace=&quot;0&quot; hspace=&quot;0&quot; allowtransparency=&quot;true&quot; scrolling=&quot;no&quot;
    data-src=&quot;{src}&quot;
  &gt;&lt;/iframe&gt;
">
</div>

<script async="" defer="" type="text/javascript">
  Manager.ready(function () {
    var select = Manager.dom().select;
    var $adblockerDetector = select('#uj-antivert-detector');

    var $iframeVertTemplate = select('#vert-template').get(0).getAttribute('data-template');

    var qsDebug = new URLSearchParams(window.location.search).get('debug');

    var slotId = new Date().getTime();

    // Selectors
    var vertSlotsSelector = '.uj-vert-unit';
    var vertSlotsSelectorLoaded = '.uj-vert-unit ins[data-ad-status]';
    var vertSlotsSelectorUnfilled = '.uj-vert-unit ins[data-ad-status="unfilled"]';

    // Set the anti-vert detector script
    /*
      /bottomads.js
      /content-ads.js
      /core-ads.js
      /insertads.js
      /jsads.js
      /left_ads.js
      /nativeads.js
      /openads.js
      /popupads.js
      /ads/ad-script.js?ad_network=123&tracking_id=abc#banner_ad
    */
    // var scriptUrl = '/assets/js/taboola_ads.js';
    // var scriptUrl = 'assets/js/pagead.js';
    var scriptUrl = '/assets/js/popupads.js?t1=/ads/native.js&t2=/nativeads.js&t3=/content-ads.js&cb=' + Manager.properties.global.cacheBreaker;

    // Check if being handled already
    if (window._ujVertCheckerStarted) {
      return;
    }
    window._ujVertCheckerStarted = true;

    // Message Handler
    function messageHandler(event) {
      var message = event.data || {};
      var command = message.command || '';
      var payload = message.payload || {};

      // Log
      console.log('Message', command, message, event.source);

      // Handle the message
      if (command === 'uj-vert-unit:set-dimensions') {
        // Get the iframe
        var $iframe = select('#uj-vert-' + payload.id).get(0);

        // Set the dimensions
        $iframe.style.height = payload.height + 'px';
      } else if (command === 'uj-vert-unit:click') {
        // Navigate to the URL
        window.location.href = payload.url;
      }
    }

    // Fill the units
    function fillUnits() {
      var $vertSlots = select(vertSlotsSelector);
      var adURL = qsDebug === 'true'
        ? 'http://localhost:4001/verts/main'
        : 'https://promo-server.itwcreativeworks.com/verts/main'

      adURL = new URL(adURL);

      adURL.searchParams.set('parentURL', window.location.href);

      // Load the ad units
      $vertSlots.each(function ($el) {
        var isFilled = $el.matches('[data-ad-status="filled"]');
        var finalURL = new URL(adURL.toString());

        finalURL.searchParams.set('frameId', slotId);

        finalURL = finalURL.toString();

        if (isFilled) {
          return;
        }

        console.log('Loading ad unit', $el, finalURL);

        $el.innerHTML = $iframeVertTemplate
          .replace(/{id}/g, 'uj-vert-' + slotId++)
          .replace(/{src}/g, finalURL)
      })
    }

    window.addEventListener('message', messageHandler, false);

    // Load the antivert checker
    Manager.dom().loadScript({src: scriptUrl})
    .then(function (e) {
      console.log('Loaded antivert checker', e);
    })
    .catch(function (e) {
      console.error('Failed to load antivert checker', e);
    })
    .finally(function () {
      var filledChecker = setInterval(function () {
        // Check if there are unfilled units
        var isOGVertsLoaded = select(vertSlotsSelectorLoaded).elements.exists;
        var isUnfilledVertSlots = select(vertSlotsSelectorUnfilled).elements.exists;

        // Set the global adblocker enabled flag
        Manager.properties.global.isAdblockerEnabled = !select('#uj-antivert-detector').elements.exists;

        var needsCustomVerts = isUnfilledVertSlots
          || Manager.properties.global.isAdblockerEnabled
          || Manager.properties.meta.environment === 'development';

        console.log(
          'isAdblockerEnabled', Manager.properties.global.isAdblockerEnabled,
          'isOGVertsLoaded', isOGVertsLoaded,
          'isUnfilledVertSlots', isUnfilledVertSlots,
          'needsCustomVerts', needsCustomVerts,
        );

        if (!Manager.properties.global.isAdblockerEnabled && !isOGVertsLoaded) {
          return;
        }

        if (isOGVertsLoaded && !isUnfilledVertSlots) {
          return clearInterval(filledChecker);
        }

        if (needsCustomVerts) {
          clearInterval(filledChecker);
          fillUnits();
        } else {
          return;
        }
      }, 300)
    })
  })
</script>

  </ad-unit>

<p>Between ads</p>

<!-- Adunit - Adsense In-article Ad -->
<ad-unit><div class="uj-vert-unit" data-plan-id="basic" data-plan-visibility="visible">
      <ins class="adsbygoogle" style="display:block; text-align:center; " data-ad-layout="in-article" data-ad-format="fluid" data-ad-client="ca-pub-7372939085773564" data-ad-slot="7329067043"></ins>
      <script>
          (adsbygoogle = window.adsbygoogle || []).push({});
      </script>
    </div>

    <div id="vert-template" data-template="
  &lt;iframe
    id=&quot;{id}&quot;
    name=&quot;{id}&quot;
    class=&quot;lazyload&quot;
    style=&quot;max-height: 300px;&quot;
    sandbox=&quot;allow-forms allow-popups allow-popups-to-escape-sandbox allow-same-origin allow-scripts allow-top-navigation-by-user-activation&quot;
    width=&quot;100%&quot; height=&quot;100%&quot;
    frameborder=&quot;0&quot; marginwidth=&quot;0&quot; marginheight=&quot;0&quot; vspace=&quot;0&quot; hspace=&quot;0&quot; allowtransparency=&quot;true&quot; scrolling=&quot;no&quot;
    data-src=&quot;{src}&quot;
  &gt;&lt;/iframe&gt;
">
</div>

<script async="" defer="" type="text/javascript">
  Manager.ready(function () {
    var select = Manager.dom().select;
    var $adblockerDetector = select('#uj-antivert-detector');

    var $iframeVertTemplate = select('#vert-template').get(0).getAttribute('data-template');

    var qsDebug = new URLSearchParams(window.location.search).get('debug');

    var slotId = new Date().getTime();

    // Selectors
    var vertSlotsSelector = '.uj-vert-unit';
    var vertSlotsSelectorLoaded = '.uj-vert-unit ins[data-ad-status]';
    var vertSlotsSelectorUnfilled = '.uj-vert-unit ins[data-ad-status="unfilled"]';

    // Set the anti-vert detector script
    /*
      /bottomads.js
      /content-ads.js
      /core-ads.js
      /insertads.js
      /jsads.js
      /left_ads.js
      /nativeads.js
      /openads.js
      /popupads.js
      /ads/ad-script.js?ad_network=123&tracking_id=abc#banner_ad
    */
    // var scriptUrl = '/assets/js/taboola_ads.js';
    // var scriptUrl = 'assets/js/pagead.js';
    var scriptUrl = '/assets/js/popupads.js?t1=/ads/native.js&t2=/nativeads.js&t3=/content-ads.js&cb=' + Manager.properties.global.cacheBreaker;

    // Check if being handled already
    if (window._ujVertCheckerStarted) {
      return;
    }
    window._ujVertCheckerStarted = true;

    // Message Handler
    function messageHandler(event) {
      var message = event.data || {};
      var command = message.command || '';
      var payload = message.payload || {};

      // Log
      console.log('Message', command, message, event.source);

      // Handle the message
      if (command === 'uj-vert-unit:set-dimensions') {
        // Get the iframe
        var $iframe = select('#uj-vert-' + payload.id).get(0);

        // Set the dimensions
        $iframe.style.height = payload.height + 'px';
      } else if (command === 'uj-vert-unit:click') {
        // Navigate to the URL
        window.location.href = payload.url;
      }
    }

    // Fill the units
    function fillUnits() {
      var $vertSlots = select(vertSlotsSelector);
      var adURL = qsDebug === 'true'
        ? 'http://localhost:4001/verts/main'
        : 'https://promo-server.itwcreativeworks.com/verts/main'

      adURL = new URL(adURL);

      adURL.searchParams.set('parentURL', window.location.href);

      // Load the ad units
      $vertSlots.each(function ($el) {
        var isFilled = $el.matches('[data-ad-status="filled"]');
        var finalURL = new URL(adURL.toString());

        finalURL.searchParams.set('frameId', slotId);

        finalURL = finalURL.toString();

        if (isFilled) {
          return;
        }

        console.log('Loading ad unit', $el, finalURL);

        $el.innerHTML = $iframeVertTemplate
          .replace(/{id}/g, 'uj-vert-' + slotId++)
          .replace(/{src}/g, finalURL)
      })
    }

    window.addEventListener('message', messageHandler, false);

    // Load the antivert checker
    Manager.dom().loadScript({src: scriptUrl})
    .then(function (e) {
      console.log('Loaded antivert checker', e);
    })
    .catch(function (e) {
      console.error('Failed to load antivert checker', e);
    })
    .finally(function () {
      var filledChecker = setInterval(function () {
        // Check if there are unfilled units
        var isOGVertsLoaded = select(vertSlotsSelectorLoaded).elements.exists;
        var isUnfilledVertSlots = select(vertSlotsSelectorUnfilled).elements.exists;

        // Set the global adblocker enabled flag
        Manager.properties.global.isAdblockerEnabled = !select('#uj-antivert-detector').elements.exists;

        var needsCustomVerts = isUnfilledVertSlots
          || Manager.properties.global.isAdblockerEnabled
          || Manager.properties.meta.environment === 'development';

        console.log(
          'isAdblockerEnabled', Manager.properties.global.isAdblockerEnabled,
          'isOGVertsLoaded', isOGVertsLoaded,
          'isUnfilledVertSlots', isUnfilledVertSlots,
          'needsCustomVerts', needsCustomVerts,
        );

        if (!Manager.properties.global.isAdblockerEnabled && !isOGVertsLoaded) {
          return;
        }

        if (isOGVertsLoaded && !isUnfilledVertSlots) {
          return clearInterval(filledChecker);
        }

        if (needsCustomVerts) {
          clearInterval(filledChecker);
          fillUnits();
        } else {
          return;
        }
      }, 300)
    })
  })
</script>

  </ad-unit>

<p>After ads</p>
",
        "test_4": "<h2 id="title-inside-post-3">Title inside post 3</h2>
<p>This is a wonderful paragrah inside a post! This is post #3 with ID 1722471523.</p>

<p>Before ads</p>

<!-- Adunit - Adsense In-article Ad --><p>Between ads</p>

<!-- Adunit - Adsense In-article Ad --><p>After ads</p>
",
        "test_5": "<h2 id=\"title-inside-post-3\">Title inside post 3</h2>\n<p>This is a wonderful paragrah inside a post! This is post #3 with ID 1722471523.</p>\n\n<p>Before ads</p>\n\n<!-- Adunit - Adsense In-article Ad --><p>Between ads</p>\n\n<!-- Adunit - Adsense In-article Ad --><p>After ads</p>\n",

        "test_6": "<h2 id="title-inside-post-3">Title inside post 3</h2>
<p>This is a wonderful paragrah inside a post! This is post #3 with ID 1722471523.</p>

<p>Before ads</p>

<!-- Adunit - Adsense In-article Ad -->
<ad-unit><div class="uj-vert-unit" data-plan-id="basic" data-plan-visibility="visible">
      <ins class="adsbygoogle" style="display:block; text-align:center; " data-ad-layout="in-article" data-ad-format="fluid" data-ad-client="ca-pub-7372939085773564" data-ad-slot="7329067043"></ins>
      <script>
          (adsbygoogle = window.adsbygoogle || []).push({});
      </script>
    </div>

    <div id="vert-template" data-template="
  &lt;iframe
    id=&quot;{id}&quot;
    name=&quot;{id}&quot;
    class=&quot;lazyload&quot;
    style=&quot;max-height: 300px;&quot;
    sandbox=&quot;allow-forms allow-popups allow-popups-to-escape-sandbox allow-same-origin allow-scripts allow-top-navigation-by-user-activation&quot;
    width=&quot;100%&quot; height=&quot;100%&quot;
    frameborder=&quot;0&quot; marginwidth=&quot;0&quot; marginheight=&quot;0&quot; vspace=&quot;0&quot; hspace=&quot;0&quot; allowtransparency=&quot;true&quot; scrolling=&quot;no&quot;
    data-src=&quot;{src}&quot;
  &gt;&lt;/iframe&gt;
">
</div>

<script async="" defer="" type="text/javascript">
  Manager.ready(function () {
    var select = Manager.dom().select;
    var $adblockerDetector = select('#uj-antivert-detector');

    var $iframeVertTemplate = select('#vert-template').get(0).getAttribute('data-template');

    var qsDebug = new URLSearchParams(window.location.search).get('debug');

    var slotId = new Date().getTime();

    // Selectors
    var vertSlotsSelector = '.uj-vert-unit';
    var vertSlotsSelectorLoaded = '.uj-vert-unit ins[data-ad-status]';
    var vertSlotsSelectorUnfilled = '.uj-vert-unit ins[data-ad-status="unfilled"]';

    // Set the anti-vert detector script
    /*
      /bottomads.js
      /content-ads.js
      /core-ads.js
      /insertads.js
      /jsads.js
      /left_ads.js
      /nativeads.js
      /openads.js
      /popupads.js
      /ads/ad-script.js?ad_network=123&tracking_id=abc#banner_ad
    */
    // var scriptUrl = '/assets/js/taboola_ads.js';
    // var scriptUrl = 'assets/js/pagead.js';
    var scriptUrl = '/assets/js/popupads.js?t1=/ads/native.js&t2=/nativeads.js&t3=/content-ads.js&cb=' + Manager.properties.global.cacheBreaker;

    // Check if being handled already
    if (window._ujVertCheckerStarted) {
      return;
    }
    window._ujVertCheckerStarted = true;

    // Message Handler
    function messageHandler(event) {
      var message = event.data || {};
      var command = message.command || '';
      var payload = message.payload || {};

      // Log
      console.log('Message', command, message, event.source);

      // Handle the message
      if (command === 'uj-vert-unit:set-dimensions') {
        // Get the iframe
        var $iframe = select('#uj-vert-' + payload.id).get(0);

        // Set the dimensions
        $iframe.style.height = payload.height + 'px';
      } else if (command === 'uj-vert-unit:click') {
        // Navigate to the URL
        window.location.href = payload.url;
      }
    }

    // Fill the units
    function fillUnits() {
      var $vertSlots = select(vertSlotsSelector);
      var adURL = qsDebug === 'true'
        ? 'http://localhost:4001/verts/main'
        : 'https://promo-server.itwcreativeworks.com/verts/main'

      adURL = new URL(adURL);

      adURL.searchParams.set('parentURL', window.location.href);

      // Load the ad units
      $vertSlots.each(function ($el) {
        var isFilled = $el.matches('[data-ad-status="filled"]');
        var finalURL = new URL(adURL.toString());

        finalURL.searchParams.set('frameId', slotId);

        finalURL = finalURL.toString();

        if (isFilled) {
          return;
        }

        console.log('Loading ad unit', $el, finalURL);

        $el.innerHTML = $iframeVertTemplate
          .replace(/{id}/g, 'uj-vert-' + slotId++)
          .replace(/{src}/g, finalURL)
      })
    }

    window.addEventListener('message', messageHandler, false);

    // Load the antivert checker
    Manager.dom().loadScript({src: scriptUrl})
    .then(function (e) {
      console.log('Loaded antivert checker', e);
    })
    .catch(function (e) {
      console.error('Failed to load antivert checker', e);
    })
    .finally(function () {
      var filledChecker = setInterval(function () {
        // Check if there are unfilled units
        var isOGVertsLoaded = select(vertSlotsSelectorLoaded).elements.exists;
        var isUnfilledVertSlots = select(vertSlotsSelectorUnfilled).elements.exists;

        // Set the global adblocker enabled flag
        Manager.properties.global.isAdblockerEnabled = !select('#uj-antivert-detector').elements.exists;

        var needsCustomVerts = isUnfilledVertSlots
          || Manager.properties.global.isAdblockerEnabled
          || Manager.properties.meta.environment === 'development';

        console.log(
          'isAdblockerEnabled', Manager.properties.global.isAdblockerEnabled,
          'isOGVertsLoaded', isOGVertsLoaded,
          'isUnfilledVertSlots', isUnfilledVertSlots,
          'needsCustomVerts', needsCustomVerts,
        );

        if (!Manager.properties.global.isAdblockerEnabled && !isOGVertsLoaded) {
          return;
        }

        if (isOGVertsLoaded && !isUnfilledVertSlots) {
          return clearInterval(filledChecker);
        }

        if (needsCustomVerts) {
          clearInterval(filledChecker);
          fillUnits();
        } else {
          return;
        }
      }, 300)
    })
  })
</script>

  </ad-unit>

<p>Between ads</p>

<!-- Adunit - Adsense In-article Ad -->
<ad-unit><div class="uj-vert-unit" data-plan-id="basic" data-plan-visibility="visible">
      <ins class="adsbygoogle" style="display:block; text-align:center; " data-ad-layout="in-article" data-ad-format="fluid" data-ad-client="ca-pub-7372939085773564" data-ad-slot="7329067043"></ins>
      <script>
          (adsbygoogle = window.adsbygoogle || []).push({});
      </script>
    </div>

    <div id="vert-template" data-template="
  &lt;iframe
    id=&quot;{id}&quot;
    name=&quot;{id}&quot;
    class=&quot;lazyload&quot;
    style=&quot;max-height: 300px;&quot;
    sandbox=&quot;allow-forms allow-popups allow-popups-to-escape-sandbox allow-same-origin allow-scripts allow-top-navigation-by-user-activation&quot;
    width=&quot;100%&quot; height=&quot;100%&quot;
    frameborder=&quot;0&quot; marginwidth=&quot;0&quot; marginheight=&quot;0&quot; vspace=&quot;0&quot; hspace=&quot;0&quot; allowtransparency=&quot;true&quot; scrolling=&quot;no&quot;
    data-src=&quot;{src}&quot;
  &gt;&lt;/iframe&gt;
">
</div>

<script async="" defer="" type="text/javascript">
  Manager.ready(function () {
    var select = Manager.dom().select;
    var $adblockerDetector = select('#uj-antivert-detector');

    var $iframeVertTemplate = select('#vert-template').get(0).getAttribute('data-template');

    var qsDebug = new URLSearchParams(window.location.search).get('debug');

    var slotId = new Date().getTime();

    // Selectors
    var vertSlotsSelector = '.uj-vert-unit';
    var vertSlotsSelectorLoaded = '.uj-vert-unit ins[data-ad-status]';
    var vertSlotsSelectorUnfilled = '.uj-vert-unit ins[data-ad-status="unfilled"]';

    // Set the anti-vert detector script
    /*
      /bottomads.js
      /content-ads.js
      /core-ads.js
      /insertads.js
      /jsads.js
      /left_ads.js
      /nativeads.js
      /openads.js
      /popupads.js
      /ads/ad-script.js?ad_network=123&tracking_id=abc#banner_ad
    */
    // var scriptUrl = '/assets/js/taboola_ads.js';
    // var scriptUrl = 'assets/js/pagead.js';
    var scriptUrl = '/assets/js/popupads.js?t1=/ads/native.js&t2=/nativeads.js&t3=/content-ads.js&cb=' + Manager.properties.global.cacheBreaker;

    // Check if being handled already
    if (window._ujVertCheckerStarted) {
      return;
    }
    window._ujVertCheckerStarted = true;

    // Message Handler
    function messageHandler(event) {
      var message = event.data || {};
      var command = message.command || '';
      var payload = message.payload || {};

      // Log
      console.log('Message', command, message, event.source);

      // Handle the message
      if (command === 'uj-vert-unit:set-dimensions') {
        // Get the iframe
        var $iframe = select('#uj-vert-' + payload.id).get(0);

        // Set the dimensions
        $iframe.style.height = payload.height + 'px';
      } else if (command === 'uj-vert-unit:click') {
        // Navigate to the URL
        window.location.href = payload.url;
      }
    }

    // Fill the units
    function fillUnits() {
      var $vertSlots = select(vertSlotsSelector);
      var adURL = qsDebug === 'true'
        ? 'http://localhost:4001/verts/main'
        : 'https://promo-server.itwcreativeworks.com/verts/main'

      adURL = new URL(adURL);

      adURL.searchParams.set('parentURL', window.location.href);

      // Load the ad units
      $vertSlots.each(function ($el) {
        var isFilled = $el.matches('[data-ad-status="filled"]');
        var finalURL = new URL(adURL.toString());

        finalURL.searchParams.set('frameId', slotId);

        finalURL = finalURL.toString();

        if (isFilled) {
          return;
        }

        console.log('Loading ad unit', $el, finalURL);

        $el.innerHTML = $iframeVertTemplate
          .replace(/{id}/g, 'uj-vert-' + slotId++)
          .replace(/{src}/g, finalURL)
      })
    }

    window.addEventListener('message', messageHandler, false);

    // Load the antivert checker
    Manager.dom().loadScript({src: scriptUrl})
    .then(function (e) {
      console.log('Loaded antivert checker', e);
    })
    .catch(function (e) {
      console.error('Failed to load antivert checker', e);
    })
    .finally(function () {
      var filledChecker = setInterval(function () {
        // Check if there are unfilled units
        var isOGVertsLoaded = select(vertSlotsSelectorLoaded).elements.exists;
        var isUnfilledVertSlots = select(vertSlotsSelectorUnfilled).elements.exists;

        // Set the global adblocker enabled flag
        Manager.properties.global.isAdblockerEnabled = !select('#uj-antivert-detector').elements.exists;

        var needsCustomVerts = isUnfilledVertSlots
          || Manager.properties.global.isAdblockerEnabled
          || Manager.properties.meta.environment === 'development';

        console.log(
          'isAdblockerEnabled', Manager.properties.global.isAdblockerEnabled,
          'isOGVertsLoaded', isOGVertsLoaded,
          'isUnfilledVertSlots', isUnfilledVertSlots,
          'needsCustomVerts', needsCustomVerts,
        );

        if (!Manager.properties.global.isAdblockerEnabled && !isOGVertsLoaded) {
          return;
        }

        if (isOGVertsLoaded && !isUnfilledVertSlots) {
          return clearInterval(filledChecker);
        }

        if (needsCustomVerts) {
          clearInterval(filledChecker);
          fillUnits();
        } else {
          return;
        }
      }, 300)
    })
  })
</script>

  </ad-unit>

<p>After ads</p>
",
        "test_7": "<h2 id="title-inside-post-3">Title inside post 3</h2>
<p>This is a wonderful paragrah inside a post! This is post #3 with ID 1722471523.</p>

<p>Before ads</p>

<!-- Adunit - Adsense In-article Ad --><p>Between ads</p>

<!-- Adunit - Adsense In-article Ad --><p>After ads</p>
",
        "test_8": "<h2 id=\"title-inside-post-3\">Title inside post 3</h2>\n<p>This is a wonderful paragrah inside a post! This is post #3 with ID 1722471523.</p>\n\n<p>Before ads</p>\n\n<!-- Adunit - Adsense In-article Ad --><p>Between ads</p>\n\n<!-- Adunit - Adsense In-article Ad --><p>After ads</p>\n",

        "authors": [
          {
            "name": "Ultimate Jekyll"
          }
        ],
        "attachments": [
          {
            "url": ""
          }
        ],
        "summary": "This is a sample excerpt for post number 3",
        "tags": ["Marketing","Business"]
      },
    
      {"id": "https://template.itwcreativeworks.com/blog/example-post-2",
        "url": "https://template.itwcreativeworks.com/blog/example-post-2",
        "title": "Example Post 2",
        "date_published": "2024-01-02T00:00:00.000Z",
        "image": "",
        "content_text": "Title inside post 2 This is a wonderful paragrah inside a post! This is post #2 with ID 1722471522.  Before ads                                     Between ads                                     After ads ",
        "content_html": "<h2 id=\"title-inside-post-2\">Title inside post 2</h2> <p>This is a wonderful paragrah inside a post! This is post #2 with ID 1722471522.</p>  <p>Before ads</p>  <!-- Adunit - Adsense In-article Ad --><p>Between ads</p>  <!-- Adunit - Adsense In-article Ad --><p>After ads</p> ",

        "test_1": "<h2 id="title-inside-post-2">Title inside post 2</h2>
<p>This is a wonderful paragrah inside a post! This is post #2 with ID 1722471522.</p>

<p>Before ads</p>

<!-- Adunit - Adsense In-article Ad -->
<ad-unit><div class="uj-vert-unit" data-plan-id="basic" data-plan-visibility="visible">
      <ins class="adsbygoogle" style="display:block; text-align:center; " data-ad-layout="in-article" data-ad-format="fluid" data-ad-client="ca-pub-7372939085773564" data-ad-slot="7329067043"></ins>
      <script>
          (adsbygoogle = window.adsbygoogle || []).push({});
      </script>
    </div>

    <div id="vert-template" data-template="
  &lt;iframe
    id=&quot;{id}&quot;
    name=&quot;{id}&quot;
    class=&quot;lazyload&quot;
    style=&quot;max-height: 300px;&quot;
    sandbox=&quot;allow-forms allow-popups allow-popups-to-escape-sandbox allow-same-origin allow-scripts allow-top-navigation-by-user-activation&quot;
    width=&quot;100%&quot; height=&quot;100%&quot;
    frameborder=&quot;0&quot; marginwidth=&quot;0&quot; marginheight=&quot;0&quot; vspace=&quot;0&quot; hspace=&quot;0&quot; allowtransparency=&quot;true&quot; scrolling=&quot;no&quot;
    data-src=&quot;{src}&quot;
  &gt;&lt;/iframe&gt;
">
</div>

<script async="" defer="" type="text/javascript">
  Manager.ready(function () {
    var select = Manager.dom().select;
    var $adblockerDetector = select('#uj-antivert-detector');

    var $iframeVertTemplate = select('#vert-template').get(0).getAttribute('data-template');

    var qsDebug = new URLSearchParams(window.location.search).get('debug');

    var slotId = new Date().getTime();

    // Selectors
    var vertSlotsSelector = '.uj-vert-unit';
    var vertSlotsSelectorLoaded = '.uj-vert-unit ins[data-ad-status]';
    var vertSlotsSelectorUnfilled = '.uj-vert-unit ins[data-ad-status="unfilled"]';

    // Set the anti-vert detector script
    /*
      /bottomads.js
      /content-ads.js
      /core-ads.js
      /insertads.js
      /jsads.js
      /left_ads.js
      /nativeads.js
      /openads.js
      /popupads.js
      /ads/ad-script.js?ad_network=123&tracking_id=abc#banner_ad
    */
    // var scriptUrl = '/assets/js/taboola_ads.js';
    // var scriptUrl = 'assets/js/pagead.js';
    var scriptUrl = '/assets/js/popupads.js?t1=/ads/native.js&t2=/nativeads.js&t3=/content-ads.js&cb=' + Manager.properties.global.cacheBreaker;

    // Check if being handled already
    if (window._ujVertCheckerStarted) {
      return;
    }
    window._ujVertCheckerStarted = true;

    // Message Handler
    function messageHandler(event) {
      var message = event.data || {};
      var command = message.command || '';
      var payload = message.payload || {};

      // Log
      console.log('Message', command, message, event.source);

      // Handle the message
      if (command === 'uj-vert-unit:set-dimensions') {
        // Get the iframe
        var $iframe = select('#uj-vert-' + payload.id).get(0);

        // Set the dimensions
        $iframe.style.height = payload.height + 'px';
      } else if (command === 'uj-vert-unit:click') {
        // Navigate to the URL
        window.location.href = payload.url;
      }
    }

    // Fill the units
    function fillUnits() {
      var $vertSlots = select(vertSlotsSelector);
      var adURL = qsDebug === 'true'
        ? 'http://localhost:4001/verts/main'
        : 'https://promo-server.itwcreativeworks.com/verts/main'

      adURL = new URL(adURL);

      adURL.searchParams.set('parentURL', window.location.href);

      // Load the ad units
      $vertSlots.each(function ($el) {
        var isFilled = $el.matches('[data-ad-status="filled"]');
        var finalURL = new URL(adURL.toString());

        finalURL.searchParams.set('frameId', slotId);

        finalURL = finalURL.toString();

        if (isFilled) {
          return;
        }

        console.log('Loading ad unit', $el, finalURL);

        $el.innerHTML = $iframeVertTemplate
          .replace(/{id}/g, 'uj-vert-' + slotId++)
          .replace(/{src}/g, finalURL)
      })
    }

    window.addEventListener('message', messageHandler, false);

    // Load the antivert checker
    Manager.dom().loadScript({src: scriptUrl})
    .then(function (e) {
      console.log('Loaded antivert checker', e);
    })
    .catch(function (e) {
      console.error('Failed to load antivert checker', e);
    })
    .finally(function () {
      var filledChecker = setInterval(function () {
        // Check if there are unfilled units
        var isOGVertsLoaded = select(vertSlotsSelectorLoaded).elements.exists;
        var isUnfilledVertSlots = select(vertSlotsSelectorUnfilled).elements.exists;

        // Set the global adblocker enabled flag
        Manager.properties.global.isAdblockerEnabled = !select('#uj-antivert-detector').elements.exists;

        var needsCustomVerts = isUnfilledVertSlots
          || Manager.properties.global.isAdblockerEnabled
          || Manager.properties.meta.environment === 'development';

        console.log(
          'isAdblockerEnabled', Manager.properties.global.isAdblockerEnabled,
          'isOGVertsLoaded', isOGVertsLoaded,
          'isUnfilledVertSlots', isUnfilledVertSlots,
          'needsCustomVerts', needsCustomVerts,
        );

        if (!Manager.properties.global.isAdblockerEnabled && !isOGVertsLoaded) {
          return;
        }

        if (isOGVertsLoaded && !isUnfilledVertSlots) {
          return clearInterval(filledChecker);
        }

        if (needsCustomVerts) {
          clearInterval(filledChecker);
          fillUnits();
        } else {
          return;
        }
      }, 300)
    })
  })
</script>

  </ad-unit>

<p>Between ads</p>

<!-- Adunit - Adsense In-article Ad -->
<ad-unit><div class="uj-vert-unit" data-plan-id="basic" data-plan-visibility="visible">
      <ins class="adsbygoogle" style="display:block; text-align:center; " data-ad-layout="in-article" data-ad-format="fluid" data-ad-client="ca-pub-7372939085773564" data-ad-slot="7329067043"></ins>
      <script>
          (adsbygoogle = window.adsbygoogle || []).push({});
      </script>
    </div>

    <div id="vert-template" data-template="
  &lt;iframe
    id=&quot;{id}&quot;
    name=&quot;{id}&quot;
    class=&quot;lazyload&quot;
    style=&quot;max-height: 300px;&quot;
    sandbox=&quot;allow-forms allow-popups allow-popups-to-escape-sandbox allow-same-origin allow-scripts allow-top-navigation-by-user-activation&quot;
    width=&quot;100%&quot; height=&quot;100%&quot;
    frameborder=&quot;0&quot; marginwidth=&quot;0&quot; marginheight=&quot;0&quot; vspace=&quot;0&quot; hspace=&quot;0&quot; allowtransparency=&quot;true&quot; scrolling=&quot;no&quot;
    data-src=&quot;{src}&quot;
  &gt;&lt;/iframe&gt;
">
</div>

<script async="" defer="" type="text/javascript">
  Manager.ready(function () {
    var select = Manager.dom().select;
    var $adblockerDetector = select('#uj-antivert-detector');

    var $iframeVertTemplate = select('#vert-template').get(0).getAttribute('data-template');

    var qsDebug = new URLSearchParams(window.location.search).get('debug');

    var slotId = new Date().getTime();

    // Selectors
    var vertSlotsSelector = '.uj-vert-unit';
    var vertSlotsSelectorLoaded = '.uj-vert-unit ins[data-ad-status]';
    var vertSlotsSelectorUnfilled = '.uj-vert-unit ins[data-ad-status="unfilled"]';

    // Set the anti-vert detector script
    /*
      /bottomads.js
      /content-ads.js
      /core-ads.js
      /insertads.js
      /jsads.js
      /left_ads.js
      /nativeads.js
      /openads.js
      /popupads.js
      /ads/ad-script.js?ad_network=123&tracking_id=abc#banner_ad
    */
    // var scriptUrl = '/assets/js/taboola_ads.js';
    // var scriptUrl = 'assets/js/pagead.js';
    var scriptUrl = '/assets/js/popupads.js?t1=/ads/native.js&t2=/nativeads.js&t3=/content-ads.js&cb=' + Manager.properties.global.cacheBreaker;

    // Check if being handled already
    if (window._ujVertCheckerStarted) {
      return;
    }
    window._ujVertCheckerStarted = true;

    // Message Handler
    function messageHandler(event) {
      var message = event.data || {};
      var command = message.command || '';
      var payload = message.payload || {};

      // Log
      console.log('Message', command, message, event.source);

      // Handle the message
      if (command === 'uj-vert-unit:set-dimensions') {
        // Get the iframe
        var $iframe = select('#uj-vert-' + payload.id).get(0);

        // Set the dimensions
        $iframe.style.height = payload.height + 'px';
      } else if (command === 'uj-vert-unit:click') {
        // Navigate to the URL
        window.location.href = payload.url;
      }
    }

    // Fill the units
    function fillUnits() {
      var $vertSlots = select(vertSlotsSelector);
      var adURL = qsDebug === 'true'
        ? 'http://localhost:4001/verts/main'
        : 'https://promo-server.itwcreativeworks.com/verts/main'

      adURL = new URL(adURL);

      adURL.searchParams.set('parentURL', window.location.href);

      // Load the ad units
      $vertSlots.each(function ($el) {
        var isFilled = $el.matches('[data-ad-status="filled"]');
        var finalURL = new URL(adURL.toString());

        finalURL.searchParams.set('frameId', slotId);

        finalURL = finalURL.toString();

        if (isFilled) {
          return;
        }

        console.log('Loading ad unit', $el, finalURL);

        $el.innerHTML = $iframeVertTemplate
          .replace(/{id}/g, 'uj-vert-' + slotId++)
          .replace(/{src}/g, finalURL)
      })
    }

    window.addEventListener('message', messageHandler, false);

    // Load the antivert checker
    Manager.dom().loadScript({src: scriptUrl})
    .then(function (e) {
      console.log('Loaded antivert checker', e);
    })
    .catch(function (e) {
      console.error('Failed to load antivert checker', e);
    })
    .finally(function () {
      var filledChecker = setInterval(function () {
        // Check if there are unfilled units
        var isOGVertsLoaded = select(vertSlotsSelectorLoaded).elements.exists;
        var isUnfilledVertSlots = select(vertSlotsSelectorUnfilled).elements.exists;

        // Set the global adblocker enabled flag
        Manager.properties.global.isAdblockerEnabled = !select('#uj-antivert-detector').elements.exists;

        var needsCustomVerts = isUnfilledVertSlots
          || Manager.properties.global.isAdblockerEnabled
          || Manager.properties.meta.environment === 'development';

        console.log(
          'isAdblockerEnabled', Manager.properties.global.isAdblockerEnabled,
          'isOGVertsLoaded', isOGVertsLoaded,
          'isUnfilledVertSlots', isUnfilledVertSlots,
          'needsCustomVerts', needsCustomVerts,
        );

        if (!Manager.properties.global.isAdblockerEnabled && !isOGVertsLoaded) {
          return;
        }

        if (isOGVertsLoaded && !isUnfilledVertSlots) {
          return clearInterval(filledChecker);
        }

        if (needsCustomVerts) {
          clearInterval(filledChecker);
          fillUnits();
        } else {
          return;
        }
      }, 300)
    })
  })
</script>

  </ad-unit>

<p>After ads</p>
",
        "test_1_2": "<h2 id="title-inside-post-2">Title inside post 2</h2>
<p>This is a wonderful paragrah inside a post! This is post #2 with ID 1722471522.</p>

<p>Before ads</p>

<!-- Adunit - Adsense In-article Ad --><p>Between ads</p>

<!-- Adunit - Adsense In-article Ad --><p>After ads</p>
",
        "test_1_3": "<h2 id=\"title-inside-post-2\">Title inside post 2</h2> <p>This is a wonderful paragrah inside a post! This is post #2 with ID 1722471522.</p>  <p>Before ads</p>  <!-- Adunit - Adsense In-article Ad --> <p>Between ads</p>  <!-- Adunit - Adsense In-article Ad --> <p>After ads</p> ",


        "test_2": "<h2 id="title-inside-post-2">Title inside post 2</h2>
<p>This is a wonderful paragrah inside a post! This is post #2 with ID 1722471522.</p>

<p>Before ads</p>

<!-- Adunit - Adsense In-article Ad -->
<ad-unit><div class="uj-vert-unit" data-plan-id="basic" data-plan-visibility="visible">
      <ins class="adsbygoogle" style="display:block; text-align:center; " data-ad-layout="in-article" data-ad-format="fluid" data-ad-client="ca-pub-7372939085773564" data-ad-slot="7329067043"></ins>
      <script>
          (adsbygoogle = window.adsbygoogle || []).push({});
      </script>
    </div>

    <div id="vert-template" data-template="
  &lt;iframe
    id=&quot;{id}&quot;
    name=&quot;{id}&quot;
    class=&quot;lazyload&quot;
    style=&quot;max-height: 300px;&quot;
    sandbox=&quot;allow-forms allow-popups allow-popups-to-escape-sandbox allow-same-origin allow-scripts allow-top-navigation-by-user-activation&quot;
    width=&quot;100%&quot; height=&quot;100%&quot;
    frameborder=&quot;0&quot; marginwidth=&quot;0&quot; marginheight=&quot;0&quot; vspace=&quot;0&quot; hspace=&quot;0&quot; allowtransparency=&quot;true&quot; scrolling=&quot;no&quot;
    data-src=&quot;{src}&quot;
  &gt;&lt;/iframe&gt;
">
</div>

<script async="" defer="" type="text/javascript">
  Manager.ready(function () {
    var select = Manager.dom().select;
    var $adblockerDetector = select('#uj-antivert-detector');

    var $iframeVertTemplate = select('#vert-template').get(0).getAttribute('data-template');

    var qsDebug = new URLSearchParams(window.location.search).get('debug');

    var slotId = new Date().getTime();

    // Selectors
    var vertSlotsSelector = '.uj-vert-unit';
    var vertSlotsSelectorLoaded = '.uj-vert-unit ins[data-ad-status]';
    var vertSlotsSelectorUnfilled = '.uj-vert-unit ins[data-ad-status="unfilled"]';

    // Set the anti-vert detector script
    /*
      /bottomads.js
      /content-ads.js
      /core-ads.js
      /insertads.js
      /jsads.js
      /left_ads.js
      /nativeads.js
      /openads.js
      /popupads.js
      /ads/ad-script.js?ad_network=123&tracking_id=abc#banner_ad
    */
    // var scriptUrl = '/assets/js/taboola_ads.js';
    // var scriptUrl = 'assets/js/pagead.js';
    var scriptUrl = '/assets/js/popupads.js?t1=/ads/native.js&t2=/nativeads.js&t3=/content-ads.js&cb=' + Manager.properties.global.cacheBreaker;

    // Check if being handled already
    if (window._ujVertCheckerStarted) {
      return;
    }
    window._ujVertCheckerStarted = true;

    // Message Handler
    function messageHandler(event) {
      var message = event.data || {};
      var command = message.command || '';
      var payload = message.payload || {};

      // Log
      console.log('Message', command, message, event.source);

      // Handle the message
      if (command === 'uj-vert-unit:set-dimensions') {
        // Get the iframe
        var $iframe = select('#uj-vert-' + payload.id).get(0);

        // Set the dimensions
        $iframe.style.height = payload.height + 'px';
      } else if (command === 'uj-vert-unit:click') {
        // Navigate to the URL
        window.location.href = payload.url;
      }
    }

    // Fill the units
    function fillUnits() {
      var $vertSlots = select(vertSlotsSelector);
      var adURL = qsDebug === 'true'
        ? 'http://localhost:4001/verts/main'
        : 'https://promo-server.itwcreativeworks.com/verts/main'

      adURL = new URL(adURL);

      adURL.searchParams.set('parentURL', window.location.href);

      // Load the ad units
      $vertSlots.each(function ($el) {
        var isFilled = $el.matches('[data-ad-status="filled"]');
        var finalURL = new URL(adURL.toString());

        finalURL.searchParams.set('frameId', slotId);

        finalURL = finalURL.toString();

        if (isFilled) {
          return;
        }

        console.log('Loading ad unit', $el, finalURL);

        $el.innerHTML = $iframeVertTemplate
          .replace(/{id}/g, 'uj-vert-' + slotId++)
          .replace(/{src}/g, finalURL)
      })
    }

    window.addEventListener('message', messageHandler, false);

    // Load the antivert checker
    Manager.dom().loadScript({src: scriptUrl})
    .then(function (e) {
      console.log('Loaded antivert checker', e);
    })
    .catch(function (e) {
      console.error('Failed to load antivert checker', e);
    })
    .finally(function () {
      var filledChecker = setInterval(function () {
        // Check if there are unfilled units
        var isOGVertsLoaded = select(vertSlotsSelectorLoaded).elements.exists;
        var isUnfilledVertSlots = select(vertSlotsSelectorUnfilled).elements.exists;

        // Set the global adblocker enabled flag
        Manager.properties.global.isAdblockerEnabled = !select('#uj-antivert-detector').elements.exists;

        var needsCustomVerts = isUnfilledVertSlots
          || Manager.properties.global.isAdblockerEnabled
          || Manager.properties.meta.environment === 'development';

        console.log(
          'isAdblockerEnabled', Manager.properties.global.isAdblockerEnabled,
          'isOGVertsLoaded', isOGVertsLoaded,
          'isUnfilledVertSlots', isUnfilledVertSlots,
          'needsCustomVerts', needsCustomVerts,
        );

        if (!Manager.properties.global.isAdblockerEnabled && !isOGVertsLoaded) {
          return;
        }

        if (isOGVertsLoaded && !isUnfilledVertSlots) {
          return clearInterval(filledChecker);
        }

        if (needsCustomVerts) {
          clearInterval(filledChecker);
          fillUnits();
        } else {
          return;
        }
      }, 300)
    })
  })
</script>

  </ad-unit>

<p>Between ads</p>

<!-- Adunit - Adsense In-article Ad -->
<ad-unit><div class="uj-vert-unit" data-plan-id="basic" data-plan-visibility="visible">
      <ins class="adsbygoogle" style="display:block; text-align:center; " data-ad-layout="in-article" data-ad-format="fluid" data-ad-client="ca-pub-7372939085773564" data-ad-slot="7329067043"></ins>
      <script>
          (adsbygoogle = window.adsbygoogle || []).push({});
      </script>
    </div>

    <div id="vert-template" data-template="
  &lt;iframe
    id=&quot;{id}&quot;
    name=&quot;{id}&quot;
    class=&quot;lazyload&quot;
    style=&quot;max-height: 300px;&quot;
    sandbox=&quot;allow-forms allow-popups allow-popups-to-escape-sandbox allow-same-origin allow-scripts allow-top-navigation-by-user-activation&quot;
    width=&quot;100%&quot; height=&quot;100%&quot;
    frameborder=&quot;0&quot; marginwidth=&quot;0&quot; marginheight=&quot;0&quot; vspace=&quot;0&quot; hspace=&quot;0&quot; allowtransparency=&quot;true&quot; scrolling=&quot;no&quot;
    data-src=&quot;{src}&quot;
  &gt;&lt;/iframe&gt;
">
</div>

<script async="" defer="" type="text/javascript">
  Manager.ready(function () {
    var select = Manager.dom().select;
    var $adblockerDetector = select('#uj-antivert-detector');

    var $iframeVertTemplate = select('#vert-template').get(0).getAttribute('data-template');

    var qsDebug = new URLSearchParams(window.location.search).get('debug');

    var slotId = new Date().getTime();

    // Selectors
    var vertSlotsSelector = '.uj-vert-unit';
    var vertSlotsSelectorLoaded = '.uj-vert-unit ins[data-ad-status]';
    var vertSlotsSelectorUnfilled = '.uj-vert-unit ins[data-ad-status="unfilled"]';

    // Set the anti-vert detector script
    /*
      /bottomads.js
      /content-ads.js
      /core-ads.js
      /insertads.js
      /jsads.js
      /left_ads.js
      /nativeads.js
      /openads.js
      /popupads.js
      /ads/ad-script.js?ad_network=123&tracking_id=abc#banner_ad
    */
    // var scriptUrl = '/assets/js/taboola_ads.js';
    // var scriptUrl = 'assets/js/pagead.js';
    var scriptUrl = '/assets/js/popupads.js?t1=/ads/native.js&t2=/nativeads.js&t3=/content-ads.js&cb=' + Manager.properties.global.cacheBreaker;

    // Check if being handled already
    if (window._ujVertCheckerStarted) {
      return;
    }
    window._ujVertCheckerStarted = true;

    // Message Handler
    function messageHandler(event) {
      var message = event.data || {};
      var command = message.command || '';
      var payload = message.payload || {};

      // Log
      console.log('Message', command, message, event.source);

      // Handle the message
      if (command === 'uj-vert-unit:set-dimensions') {
        // Get the iframe
        var $iframe = select('#uj-vert-' + payload.id).get(0);

        // Set the dimensions
        $iframe.style.height = payload.height + 'px';
      } else if (command === 'uj-vert-unit:click') {
        // Navigate to the URL
        window.location.href = payload.url;
      }
    }

    // Fill the units
    function fillUnits() {
      var $vertSlots = select(vertSlotsSelector);
      var adURL = qsDebug === 'true'
        ? 'http://localhost:4001/verts/main'
        : 'https://promo-server.itwcreativeworks.com/verts/main'

      adURL = new URL(adURL);

      adURL.searchParams.set('parentURL', window.location.href);

      // Load the ad units
      $vertSlots.each(function ($el) {
        var isFilled = $el.matches('[data-ad-status="filled"]');
        var finalURL = new URL(adURL.toString());

        finalURL.searchParams.set('frameId', slotId);

        finalURL = finalURL.toString();

        if (isFilled) {
          return;
        }

        console.log('Loading ad unit', $el, finalURL);

        $el.innerHTML = $iframeVertTemplate
          .replace(/{id}/g, 'uj-vert-' + slotId++)
          .replace(/{src}/g, finalURL)
      })
    }

    window.addEventListener('message', messageHandler, false);

    // Load the antivert checker
    Manager.dom().loadScript({src: scriptUrl})
    .then(function (e) {
      console.log('Loaded antivert checker', e);
    })
    .catch(function (e) {
      console.error('Failed to load antivert checker', e);
    })
    .finally(function () {
      var filledChecker = setInterval(function () {
        // Check if there are unfilled units
        var isOGVertsLoaded = select(vertSlotsSelectorLoaded).elements.exists;
        var isUnfilledVertSlots = select(vertSlotsSelectorUnfilled).elements.exists;

        // Set the global adblocker enabled flag
        Manager.properties.global.isAdblockerEnabled = !select('#uj-antivert-detector').elements.exists;

        var needsCustomVerts = isUnfilledVertSlots
          || Manager.properties.global.isAdblockerEnabled
          || Manager.properties.meta.environment === 'development';

        console.log(
          'isAdblockerEnabled', Manager.properties.global.isAdblockerEnabled,
          'isOGVertsLoaded', isOGVertsLoaded,
          'isUnfilledVertSlots', isUnfilledVertSlots,
          'needsCustomVerts', needsCustomVerts,
        );

        if (!Manager.properties.global.isAdblockerEnabled && !isOGVertsLoaded) {
          return;
        }

        if (isOGVertsLoaded && !isUnfilledVertSlots) {
          return clearInterval(filledChecker);
        }

        if (needsCustomVerts) {
          clearInterval(filledChecker);
          fillUnits();
        } else {
          return;
        }
      }, 300)
    })
  })
</script>

  </ad-unit>

<p>After ads</p>
",
        "test_3": "<h2 id="title-inside-post-2">Title inside post 2</h2>
<p>This is a wonderful paragrah inside a post! This is post #2 with ID 1722471522.</p>

<p>Before ads</p>

<!-- Adunit - Adsense In-article Ad -->
<ad-unit><div class="uj-vert-unit" data-plan-id="basic" data-plan-visibility="visible">
      <ins class="adsbygoogle" style="display:block; text-align:center; " data-ad-layout="in-article" data-ad-format="fluid" data-ad-client="ca-pub-7372939085773564" data-ad-slot="7329067043"></ins>
      <script>
          (adsbygoogle = window.adsbygoogle || []).push({});
      </script>
    </div>

    <div id="vert-template" data-template="
  &lt;iframe
    id=&quot;{id}&quot;
    name=&quot;{id}&quot;
    class=&quot;lazyload&quot;
    style=&quot;max-height: 300px;&quot;
    sandbox=&quot;allow-forms allow-popups allow-popups-to-escape-sandbox allow-same-origin allow-scripts allow-top-navigation-by-user-activation&quot;
    width=&quot;100%&quot; height=&quot;100%&quot;
    frameborder=&quot;0&quot; marginwidth=&quot;0&quot; marginheight=&quot;0&quot; vspace=&quot;0&quot; hspace=&quot;0&quot; allowtransparency=&quot;true&quot; scrolling=&quot;no&quot;
    data-src=&quot;{src}&quot;
  &gt;&lt;/iframe&gt;
">
</div>

<script async="" defer="" type="text/javascript">
  Manager.ready(function () {
    var select = Manager.dom().select;
    var $adblockerDetector = select('#uj-antivert-detector');

    var $iframeVertTemplate = select('#vert-template').get(0).getAttribute('data-template');

    var qsDebug = new URLSearchParams(window.location.search).get('debug');

    var slotId = new Date().getTime();

    // Selectors
    var vertSlotsSelector = '.uj-vert-unit';
    var vertSlotsSelectorLoaded = '.uj-vert-unit ins[data-ad-status]';
    var vertSlotsSelectorUnfilled = '.uj-vert-unit ins[data-ad-status="unfilled"]';

    // Set the anti-vert detector script
    /*
      /bottomads.js
      /content-ads.js
      /core-ads.js
      /insertads.js
      /jsads.js
      /left_ads.js
      /nativeads.js
      /openads.js
      /popupads.js
      /ads/ad-script.js?ad_network=123&tracking_id=abc#banner_ad
    */
    // var scriptUrl = '/assets/js/taboola_ads.js';
    // var scriptUrl = 'assets/js/pagead.js';
    var scriptUrl = '/assets/js/popupads.js?t1=/ads/native.js&t2=/nativeads.js&t3=/content-ads.js&cb=' + Manager.properties.global.cacheBreaker;

    // Check if being handled already
    if (window._ujVertCheckerStarted) {
      return;
    }
    window._ujVertCheckerStarted = true;

    // Message Handler
    function messageHandler(event) {
      var message = event.data || {};
      var command = message.command || '';
      var payload = message.payload || {};

      // Log
      console.log('Message', command, message, event.source);

      // Handle the message
      if (command === 'uj-vert-unit:set-dimensions') {
        // Get the iframe
        var $iframe = select('#uj-vert-' + payload.id).get(0);

        // Set the dimensions
        $iframe.style.height = payload.height + 'px';
      } else if (command === 'uj-vert-unit:click') {
        // Navigate to the URL
        window.location.href = payload.url;
      }
    }

    // Fill the units
    function fillUnits() {
      var $vertSlots = select(vertSlotsSelector);
      var adURL = qsDebug === 'true'
        ? 'http://localhost:4001/verts/main'
        : 'https://promo-server.itwcreativeworks.com/verts/main'

      adURL = new URL(adURL);

      adURL.searchParams.set('parentURL', window.location.href);

      // Load the ad units
      $vertSlots.each(function ($el) {
        var isFilled = $el.matches('[data-ad-status="filled"]');
        var finalURL = new URL(adURL.toString());

        finalURL.searchParams.set('frameId', slotId);

        finalURL = finalURL.toString();

        if (isFilled) {
          return;
        }

        console.log('Loading ad unit', $el, finalURL);

        $el.innerHTML = $iframeVertTemplate
          .replace(/{id}/g, 'uj-vert-' + slotId++)
          .replace(/{src}/g, finalURL)
      })
    }

    window.addEventListener('message', messageHandler, false);

    // Load the antivert checker
    Manager.dom().loadScript({src: scriptUrl})
    .then(function (e) {
      console.log('Loaded antivert checker', e);
    })
    .catch(function (e) {
      console.error('Failed to load antivert checker', e);
    })
    .finally(function () {
      var filledChecker = setInterval(function () {
        // Check if there are unfilled units
        var isOGVertsLoaded = select(vertSlotsSelectorLoaded).elements.exists;
        var isUnfilledVertSlots = select(vertSlotsSelectorUnfilled).elements.exists;

        // Set the global adblocker enabled flag
        Manager.properties.global.isAdblockerEnabled = !select('#uj-antivert-detector').elements.exists;

        var needsCustomVerts = isUnfilledVertSlots
          || Manager.properties.global.isAdblockerEnabled
          || Manager.properties.meta.environment === 'development';

        console.log(
          'isAdblockerEnabled', Manager.properties.global.isAdblockerEnabled,
          'isOGVertsLoaded', isOGVertsLoaded,
          'isUnfilledVertSlots', isUnfilledVertSlots,
          'needsCustomVerts', needsCustomVerts,
        );

        if (!Manager.properties.global.isAdblockerEnabled && !isOGVertsLoaded) {
          return;
        }

        if (isOGVertsLoaded && !isUnfilledVertSlots) {
          return clearInterval(filledChecker);
        }

        if (needsCustomVerts) {
          clearInterval(filledChecker);
          fillUnits();
        } else {
          return;
        }
      }, 300)
    })
  })
</script>

  </ad-unit>

<p>Between ads</p>

<!-- Adunit - Adsense In-article Ad -->
<ad-unit><div class="uj-vert-unit" data-plan-id="basic" data-plan-visibility="visible">
      <ins class="adsbygoogle" style="display:block; text-align:center; " data-ad-layout="in-article" data-ad-format="fluid" data-ad-client="ca-pub-7372939085773564" data-ad-slot="7329067043"></ins>
      <script>
          (adsbygoogle = window.adsbygoogle || []).push({});
      </script>
    </div>

    <div id="vert-template" data-template="
  &lt;iframe
    id=&quot;{id}&quot;
    name=&quot;{id}&quot;
    class=&quot;lazyload&quot;
    style=&quot;max-height: 300px;&quot;
    sandbox=&quot;allow-forms allow-popups allow-popups-to-escape-sandbox allow-same-origin allow-scripts allow-top-navigation-by-user-activation&quot;
    width=&quot;100%&quot; height=&quot;100%&quot;
    frameborder=&quot;0&quot; marginwidth=&quot;0&quot; marginheight=&quot;0&quot; vspace=&quot;0&quot; hspace=&quot;0&quot; allowtransparency=&quot;true&quot; scrolling=&quot;no&quot;
    data-src=&quot;{src}&quot;
  &gt;&lt;/iframe&gt;
">
</div>

<script async="" defer="" type="text/javascript">
  Manager.ready(function () {
    var select = Manager.dom().select;
    var $adblockerDetector = select('#uj-antivert-detector');

    var $iframeVertTemplate = select('#vert-template').get(0).getAttribute('data-template');

    var qsDebug = new URLSearchParams(window.location.search).get('debug');

    var slotId = new Date().getTime();

    // Selectors
    var vertSlotsSelector = '.uj-vert-unit';
    var vertSlotsSelectorLoaded = '.uj-vert-unit ins[data-ad-status]';
    var vertSlotsSelectorUnfilled = '.uj-vert-unit ins[data-ad-status="unfilled"]';

    // Set the anti-vert detector script
    /*
      /bottomads.js
      /content-ads.js
      /core-ads.js
      /insertads.js
      /jsads.js
      /left_ads.js
      /nativeads.js
      /openads.js
      /popupads.js
      /ads/ad-script.js?ad_network=123&tracking_id=abc#banner_ad
    */
    // var scriptUrl = '/assets/js/taboola_ads.js';
    // var scriptUrl = 'assets/js/pagead.js';
    var scriptUrl = '/assets/js/popupads.js?t1=/ads/native.js&t2=/nativeads.js&t3=/content-ads.js&cb=' + Manager.properties.global.cacheBreaker;

    // Check if being handled already
    if (window._ujVertCheckerStarted) {
      return;
    }
    window._ujVertCheckerStarted = true;

    // Message Handler
    function messageHandler(event) {
      var message = event.data || {};
      var command = message.command || '';
      var payload = message.payload || {};

      // Log
      console.log('Message', command, message, event.source);

      // Handle the message
      if (command === 'uj-vert-unit:set-dimensions') {
        // Get the iframe
        var $iframe = select('#uj-vert-' + payload.id).get(0);

        // Set the dimensions
        $iframe.style.height = payload.height + 'px';
      } else if (command === 'uj-vert-unit:click') {
        // Navigate to the URL
        window.location.href = payload.url;
      }
    }

    // Fill the units
    function fillUnits() {
      var $vertSlots = select(vertSlotsSelector);
      var adURL = qsDebug === 'true'
        ? 'http://localhost:4001/verts/main'
        : 'https://promo-server.itwcreativeworks.com/verts/main'

      adURL = new URL(adURL);

      adURL.searchParams.set('parentURL', window.location.href);

      // Load the ad units
      $vertSlots.each(function ($el) {
        var isFilled = $el.matches('[data-ad-status="filled"]');
        var finalURL = new URL(adURL.toString());

        finalURL.searchParams.set('frameId', slotId);

        finalURL = finalURL.toString();

        if (isFilled) {
          return;
        }

        console.log('Loading ad unit', $el, finalURL);

        $el.innerHTML = $iframeVertTemplate
          .replace(/{id}/g, 'uj-vert-' + slotId++)
          .replace(/{src}/g, finalURL)
      })
    }

    window.addEventListener('message', messageHandler, false);

    // Load the antivert checker
    Manager.dom().loadScript({src: scriptUrl})
    .then(function (e) {
      console.log('Loaded antivert checker', e);
    })
    .catch(function (e) {
      console.error('Failed to load antivert checker', e);
    })
    .finally(function () {
      var filledChecker = setInterval(function () {
        // Check if there are unfilled units
        var isOGVertsLoaded = select(vertSlotsSelectorLoaded).elements.exists;
        var isUnfilledVertSlots = select(vertSlotsSelectorUnfilled).elements.exists;

        // Set the global adblocker enabled flag
        Manager.properties.global.isAdblockerEnabled = !select('#uj-antivert-detector').elements.exists;

        var needsCustomVerts = isUnfilledVertSlots
          || Manager.properties.global.isAdblockerEnabled
          || Manager.properties.meta.environment === 'development';

        console.log(
          'isAdblockerEnabled', Manager.properties.global.isAdblockerEnabled,
          'isOGVertsLoaded', isOGVertsLoaded,
          'isUnfilledVertSlots', isUnfilledVertSlots,
          'needsCustomVerts', needsCustomVerts,
        );

        if (!Manager.properties.global.isAdblockerEnabled && !isOGVertsLoaded) {
          return;
        }

        if (isOGVertsLoaded && !isUnfilledVertSlots) {
          return clearInterval(filledChecker);
        }

        if (needsCustomVerts) {
          clearInterval(filledChecker);
          fillUnits();
        } else {
          return;
        }
      }, 300)
    })
  })
</script>

  </ad-unit>

<p>After ads</p>
",
        "test_4": "<h2 id="title-inside-post-2">Title inside post 2</h2>
<p>This is a wonderful paragrah inside a post! This is post #2 with ID 1722471522.</p>

<p>Before ads</p>

<!-- Adunit - Adsense In-article Ad --><p>Between ads</p>

<!-- Adunit - Adsense In-article Ad --><p>After ads</p>
",
        "test_5": "<h2 id=\"title-inside-post-2\">Title inside post 2</h2>\n<p>This is a wonderful paragrah inside a post! This is post #2 with ID 1722471522.</p>\n\n<p>Before ads</p>\n\n<!-- Adunit - Adsense In-article Ad --><p>Between ads</p>\n\n<!-- Adunit - Adsense In-article Ad --><p>After ads</p>\n",

        "test_6": "<h2 id="title-inside-post-2">Title inside post 2</h2>
<p>This is a wonderful paragrah inside a post! This is post #2 with ID 1722471522.</p>

<p>Before ads</p>

<!-- Adunit - Adsense In-article Ad -->
<ad-unit><div class="uj-vert-unit" data-plan-id="basic" data-plan-visibility="visible">
      <ins class="adsbygoogle" style="display:block; text-align:center; " data-ad-layout="in-article" data-ad-format="fluid" data-ad-client="ca-pub-7372939085773564" data-ad-slot="7329067043"></ins>
      <script>
          (adsbygoogle = window.adsbygoogle || []).push({});
      </script>
    </div>

    <div id="vert-template" data-template="
  &lt;iframe
    id=&quot;{id}&quot;
    name=&quot;{id}&quot;
    class=&quot;lazyload&quot;
    style=&quot;max-height: 300px;&quot;
    sandbox=&quot;allow-forms allow-popups allow-popups-to-escape-sandbox allow-same-origin allow-scripts allow-top-navigation-by-user-activation&quot;
    width=&quot;100%&quot; height=&quot;100%&quot;
    frameborder=&quot;0&quot; marginwidth=&quot;0&quot; marginheight=&quot;0&quot; vspace=&quot;0&quot; hspace=&quot;0&quot; allowtransparency=&quot;true&quot; scrolling=&quot;no&quot;
    data-src=&quot;{src}&quot;
  &gt;&lt;/iframe&gt;
">
</div>

<script async="" defer="" type="text/javascript">
  Manager.ready(function () {
    var select = Manager.dom().select;
    var $adblockerDetector = select('#uj-antivert-detector');

    var $iframeVertTemplate = select('#vert-template').get(0).getAttribute('data-template');

    var qsDebug = new URLSearchParams(window.location.search).get('debug');

    var slotId = new Date().getTime();

    // Selectors
    var vertSlotsSelector = '.uj-vert-unit';
    var vertSlotsSelectorLoaded = '.uj-vert-unit ins[data-ad-status]';
    var vertSlotsSelectorUnfilled = '.uj-vert-unit ins[data-ad-status="unfilled"]';

    // Set the anti-vert detector script
    /*
      /bottomads.js
      /content-ads.js
      /core-ads.js
      /insertads.js
      /jsads.js
      /left_ads.js
      /nativeads.js
      /openads.js
      /popupads.js
      /ads/ad-script.js?ad_network=123&tracking_id=abc#banner_ad
    */
    // var scriptUrl = '/assets/js/taboola_ads.js';
    // var scriptUrl = 'assets/js/pagead.js';
    var scriptUrl = '/assets/js/popupads.js?t1=/ads/native.js&t2=/nativeads.js&t3=/content-ads.js&cb=' + Manager.properties.global.cacheBreaker;

    // Check if being handled already
    if (window._ujVertCheckerStarted) {
      return;
    }
    window._ujVertCheckerStarted = true;

    // Message Handler
    function messageHandler(event) {
      var message = event.data || {};
      var command = message.command || '';
      var payload = message.payload || {};

      // Log
      console.log('Message', command, message, event.source);

      // Handle the message
      if (command === 'uj-vert-unit:set-dimensions') {
        // Get the iframe
        var $iframe = select('#uj-vert-' + payload.id).get(0);

        // Set the dimensions
        $iframe.style.height = payload.height + 'px';
      } else if (command === 'uj-vert-unit:click') {
        // Navigate to the URL
        window.location.href = payload.url;
      }
    }

    // Fill the units
    function fillUnits() {
      var $vertSlots = select(vertSlotsSelector);
      var adURL = qsDebug === 'true'
        ? 'http://localhost:4001/verts/main'
        : 'https://promo-server.itwcreativeworks.com/verts/main'

      adURL = new URL(adURL);

      adURL.searchParams.set('parentURL', window.location.href);

      // Load the ad units
      $vertSlots.each(function ($el) {
        var isFilled = $el.matches('[data-ad-status="filled"]');
        var finalURL = new URL(adURL.toString());

        finalURL.searchParams.set('frameId', slotId);

        finalURL = finalURL.toString();

        if (isFilled) {
          return;
        }

        console.log('Loading ad unit', $el, finalURL);

        $el.innerHTML = $iframeVertTemplate
          .replace(/{id}/g, 'uj-vert-' + slotId++)
          .replace(/{src}/g, finalURL)
      })
    }

    window.addEventListener('message', messageHandler, false);

    // Load the antivert checker
    Manager.dom().loadScript({src: scriptUrl})
    .then(function (e) {
      console.log('Loaded antivert checker', e);
    })
    .catch(function (e) {
      console.error('Failed to load antivert checker', e);
    })
    .finally(function () {
      var filledChecker = setInterval(function () {
        // Check if there are unfilled units
        var isOGVertsLoaded = select(vertSlotsSelectorLoaded).elements.exists;
        var isUnfilledVertSlots = select(vertSlotsSelectorUnfilled).elements.exists;

        // Set the global adblocker enabled flag
        Manager.properties.global.isAdblockerEnabled = !select('#uj-antivert-detector').elements.exists;

        var needsCustomVerts = isUnfilledVertSlots
          || Manager.properties.global.isAdblockerEnabled
          || Manager.properties.meta.environment === 'development';

        console.log(
          'isAdblockerEnabled', Manager.properties.global.isAdblockerEnabled,
          'isOGVertsLoaded', isOGVertsLoaded,
          'isUnfilledVertSlots', isUnfilledVertSlots,
          'needsCustomVerts', needsCustomVerts,
        );

        if (!Manager.properties.global.isAdblockerEnabled && !isOGVertsLoaded) {
          return;
        }

        if (isOGVertsLoaded && !isUnfilledVertSlots) {
          return clearInterval(filledChecker);
        }

        if (needsCustomVerts) {
          clearInterval(filledChecker);
          fillUnits();
        } else {
          return;
        }
      }, 300)
    })
  })
</script>

  </ad-unit>

<p>Between ads</p>

<!-- Adunit - Adsense In-article Ad -->
<ad-unit><div class="uj-vert-unit" data-plan-id="basic" data-plan-visibility="visible">
      <ins class="adsbygoogle" style="display:block; text-align:center; " data-ad-layout="in-article" data-ad-format="fluid" data-ad-client="ca-pub-7372939085773564" data-ad-slot="7329067043"></ins>
      <script>
          (adsbygoogle = window.adsbygoogle || []).push({});
      </script>
    </div>

    <div id="vert-template" data-template="
  &lt;iframe
    id=&quot;{id}&quot;
    name=&quot;{id}&quot;
    class=&quot;lazyload&quot;
    style=&quot;max-height: 300px;&quot;
    sandbox=&quot;allow-forms allow-popups allow-popups-to-escape-sandbox allow-same-origin allow-scripts allow-top-navigation-by-user-activation&quot;
    width=&quot;100%&quot; height=&quot;100%&quot;
    frameborder=&quot;0&quot; marginwidth=&quot;0&quot; marginheight=&quot;0&quot; vspace=&quot;0&quot; hspace=&quot;0&quot; allowtransparency=&quot;true&quot; scrolling=&quot;no&quot;
    data-src=&quot;{src}&quot;
  &gt;&lt;/iframe&gt;
">
</div>

<script async="" defer="" type="text/javascript">
  Manager.ready(function () {
    var select = Manager.dom().select;
    var $adblockerDetector = select('#uj-antivert-detector');

    var $iframeVertTemplate = select('#vert-template').get(0).getAttribute('data-template');

    var qsDebug = new URLSearchParams(window.location.search).get('debug');

    var slotId = new Date().getTime();

    // Selectors
    var vertSlotsSelector = '.uj-vert-unit';
    var vertSlotsSelectorLoaded = '.uj-vert-unit ins[data-ad-status]';
    var vertSlotsSelectorUnfilled = '.uj-vert-unit ins[data-ad-status="unfilled"]';

    // Set the anti-vert detector script
    /*
      /bottomads.js
      /content-ads.js
      /core-ads.js
      /insertads.js
      /jsads.js
      /left_ads.js
      /nativeads.js
      /openads.js
      /popupads.js
      /ads/ad-script.js?ad_network=123&tracking_id=abc#banner_ad
    */
    // var scriptUrl = '/assets/js/taboola_ads.js';
    // var scriptUrl = 'assets/js/pagead.js';
    var scriptUrl = '/assets/js/popupads.js?t1=/ads/native.js&t2=/nativeads.js&t3=/content-ads.js&cb=' + Manager.properties.global.cacheBreaker;

    // Check if being handled already
    if (window._ujVertCheckerStarted) {
      return;
    }
    window._ujVertCheckerStarted = true;

    // Message Handler
    function messageHandler(event) {
      var message = event.data || {};
      var command = message.command || '';
      var payload = message.payload || {};

      // Log
      console.log('Message', command, message, event.source);

      // Handle the message
      if (command === 'uj-vert-unit:set-dimensions') {
        // Get the iframe
        var $iframe = select('#uj-vert-' + payload.id).get(0);

        // Set the dimensions
        $iframe.style.height = payload.height + 'px';
      } else if (command === 'uj-vert-unit:click') {
        // Navigate to the URL
        window.location.href = payload.url;
      }
    }

    // Fill the units
    function fillUnits() {
      var $vertSlots = select(vertSlotsSelector);
      var adURL = qsDebug === 'true'
        ? 'http://localhost:4001/verts/main'
        : 'https://promo-server.itwcreativeworks.com/verts/main'

      adURL = new URL(adURL);

      adURL.searchParams.set('parentURL', window.location.href);

      // Load the ad units
      $vertSlots.each(function ($el) {
        var isFilled = $el.matches('[data-ad-status="filled"]');
        var finalURL = new URL(adURL.toString());

        finalURL.searchParams.set('frameId', slotId);

        finalURL = finalURL.toString();

        if (isFilled) {
          return;
        }

        console.log('Loading ad unit', $el, finalURL);

        $el.innerHTML = $iframeVertTemplate
          .replace(/{id}/g, 'uj-vert-' + slotId++)
          .replace(/{src}/g, finalURL)
      })
    }

    window.addEventListener('message', messageHandler, false);

    // Load the antivert checker
    Manager.dom().loadScript({src: scriptUrl})
    .then(function (e) {
      console.log('Loaded antivert checker', e);
    })
    .catch(function (e) {
      console.error('Failed to load antivert checker', e);
    })
    .finally(function () {
      var filledChecker = setInterval(function () {
        // Check if there are unfilled units
        var isOGVertsLoaded = select(vertSlotsSelectorLoaded).elements.exists;
        var isUnfilledVertSlots = select(vertSlotsSelectorUnfilled).elements.exists;

        // Set the global adblocker enabled flag
        Manager.properties.global.isAdblockerEnabled = !select('#uj-antivert-detector').elements.exists;

        var needsCustomVerts = isUnfilledVertSlots
          || Manager.properties.global.isAdblockerEnabled
          || Manager.properties.meta.environment === 'development';

        console.log(
          'isAdblockerEnabled', Manager.properties.global.isAdblockerEnabled,
          'isOGVertsLoaded', isOGVertsLoaded,
          'isUnfilledVertSlots', isUnfilledVertSlots,
          'needsCustomVerts', needsCustomVerts,
        );

        if (!Manager.properties.global.isAdblockerEnabled && !isOGVertsLoaded) {
          return;
        }

        if (isOGVertsLoaded && !isUnfilledVertSlots) {
          return clearInterval(filledChecker);
        }

        if (needsCustomVerts) {
          clearInterval(filledChecker);
          fillUnits();
        } else {
          return;
        }
      }, 300)
    })
  })
</script>

  </ad-unit>

<p>After ads</p>
",
        "test_7": "<h2 id="title-inside-post-2">Title inside post 2</h2>
<p>This is a wonderful paragrah inside a post! This is post #2 with ID 1722471522.</p>

<p>Before ads</p>

<!-- Adunit - Adsense In-article Ad --><p>Between ads</p>

<!-- Adunit - Adsense In-article Ad --><p>After ads</p>
",
        "test_8": "<h2 id=\"title-inside-post-2\">Title inside post 2</h2>\n<p>This is a wonderful paragrah inside a post! This is post #2 with ID 1722471522.</p>\n\n<p>Before ads</p>\n\n<!-- Adunit - Adsense In-article Ad --><p>Between ads</p>\n\n<!-- Adunit - Adsense In-article Ad --><p>After ads</p>\n",

        "authors": [
          {
            "name": "Ultimate Jekyll"
          }
        ],
        "attachments": [
          {
            "url": ""
          }
        ],
        "summary": "This is a sample excerpt for post number 2",
        "tags": ["Marketing","Business"]
      },
    
      {"id": "https://template.itwcreativeworks.com/blog/example-post-1",
        "url": "https://template.itwcreativeworks.com/blog/example-post-1",
        "title": "Example Post 1",
        "date_published": "2024-01-01T00:00:00.000Z",
        "image": "",
        "content_text": "Title inside post 1 This is a wonderful paragrah inside a post! This is post #1 with ID 1722471521.  Before ads                                     Between ads                                     After ads ",
        "content_html": "<h2 id=\"title-inside-post-1\">Title inside post 1</h2> <p>This is a wonderful paragrah inside a post! This is post #1 with ID 1722471521.</p>  <p>Before ads</p>  <!-- Adunit - Adsense In-article Ad --><p>Between ads</p>  <!-- Adunit - Adsense In-article Ad --><p>After ads</p> ",

        "test_1": "<h2 id="title-inside-post-1">Title inside post 1</h2>
<p>This is a wonderful paragrah inside a post! This is post #1 with ID 1722471521.</p>

<p>Before ads</p>

<!-- Adunit - Adsense In-article Ad -->
<ad-unit><div class="uj-vert-unit" data-plan-id="basic" data-plan-visibility="visible">
      <ins class="adsbygoogle" style="display:block; text-align:center; " data-ad-layout="in-article" data-ad-format="fluid" data-ad-client="ca-pub-7372939085773564" data-ad-slot="7329067043"></ins>
      <script>
          (adsbygoogle = window.adsbygoogle || []).push({});
      </script>
    </div>

    <div id="vert-template" data-template="
  &lt;iframe
    id=&quot;{id}&quot;
    name=&quot;{id}&quot;
    class=&quot;lazyload&quot;
    style=&quot;max-height: 300px;&quot;
    sandbox=&quot;allow-forms allow-popups allow-popups-to-escape-sandbox allow-same-origin allow-scripts allow-top-navigation-by-user-activation&quot;
    width=&quot;100%&quot; height=&quot;100%&quot;
    frameborder=&quot;0&quot; marginwidth=&quot;0&quot; marginheight=&quot;0&quot; vspace=&quot;0&quot; hspace=&quot;0&quot; allowtransparency=&quot;true&quot; scrolling=&quot;no&quot;
    data-src=&quot;{src}&quot;
  &gt;&lt;/iframe&gt;
">
</div>

<script async="" defer="" type="text/javascript">
  Manager.ready(function () {
    var select = Manager.dom().select;
    var $adblockerDetector = select('#uj-antivert-detector');

    var $iframeVertTemplate = select('#vert-template').get(0).getAttribute('data-template');

    var qsDebug = new URLSearchParams(window.location.search).get('debug');

    var slotId = new Date().getTime();

    // Selectors
    var vertSlotsSelector = '.uj-vert-unit';
    var vertSlotsSelectorLoaded = '.uj-vert-unit ins[data-ad-status]';
    var vertSlotsSelectorUnfilled = '.uj-vert-unit ins[data-ad-status="unfilled"]';

    // Set the anti-vert detector script
    /*
      /bottomads.js
      /content-ads.js
      /core-ads.js
      /insertads.js
      /jsads.js
      /left_ads.js
      /nativeads.js
      /openads.js
      /popupads.js
      /ads/ad-script.js?ad_network=123&tracking_id=abc#banner_ad
    */
    // var scriptUrl = '/assets/js/taboola_ads.js';
    // var scriptUrl = 'assets/js/pagead.js';
    var scriptUrl = '/assets/js/popupads.js?t1=/ads/native.js&t2=/nativeads.js&t3=/content-ads.js&cb=' + Manager.properties.global.cacheBreaker;

    // Check if being handled already
    if (window._ujVertCheckerStarted) {
      return;
    }
    window._ujVertCheckerStarted = true;

    // Message Handler
    function messageHandler(event) {
      var message = event.data || {};
      var command = message.command || '';
      var payload = message.payload || {};

      // Log
      console.log('Message', command, message, event.source);

      // Handle the message
      if (command === 'uj-vert-unit:set-dimensions') {
        // Get the iframe
        var $iframe = select('#uj-vert-' + payload.id).get(0);

        // Set the dimensions
        $iframe.style.height = payload.height + 'px';
      } else if (command === 'uj-vert-unit:click') {
        // Navigate to the URL
        window.location.href = payload.url;
      }
    }

    // Fill the units
    function fillUnits() {
      var $vertSlots = select(vertSlotsSelector);
      var adURL = qsDebug === 'true'
        ? 'http://localhost:4001/verts/main'
        : 'https://promo-server.itwcreativeworks.com/verts/main'

      adURL = new URL(adURL);

      adURL.searchParams.set('parentURL', window.location.href);

      // Load the ad units
      $vertSlots.each(function ($el) {
        var isFilled = $el.matches('[data-ad-status="filled"]');
        var finalURL = new URL(adURL.toString());

        finalURL.searchParams.set('frameId', slotId);

        finalURL = finalURL.toString();

        if (isFilled) {
          return;
        }

        console.log('Loading ad unit', $el, finalURL);

        $el.innerHTML = $iframeVertTemplate
          .replace(/{id}/g, 'uj-vert-' + slotId++)
          .replace(/{src}/g, finalURL)
      })
    }

    window.addEventListener('message', messageHandler, false);

    // Load the antivert checker
    Manager.dom().loadScript({src: scriptUrl})
    .then(function (e) {
      console.log('Loaded antivert checker', e);
    })
    .catch(function (e) {
      console.error('Failed to load antivert checker', e);
    })
    .finally(function () {
      var filledChecker = setInterval(function () {
        // Check if there are unfilled units
        var isOGVertsLoaded = select(vertSlotsSelectorLoaded).elements.exists;
        var isUnfilledVertSlots = select(vertSlotsSelectorUnfilled).elements.exists;

        // Set the global adblocker enabled flag
        Manager.properties.global.isAdblockerEnabled = !select('#uj-antivert-detector').elements.exists;

        var needsCustomVerts = isUnfilledVertSlots
          || Manager.properties.global.isAdblockerEnabled
          || Manager.properties.meta.environment === 'development';

        console.log(
          'isAdblockerEnabled', Manager.properties.global.isAdblockerEnabled,
          'isOGVertsLoaded', isOGVertsLoaded,
          'isUnfilledVertSlots', isUnfilledVertSlots,
          'needsCustomVerts', needsCustomVerts,
        );

        if (!Manager.properties.global.isAdblockerEnabled && !isOGVertsLoaded) {
          return;
        }

        if (isOGVertsLoaded && !isUnfilledVertSlots) {
          return clearInterval(filledChecker);
        }

        if (needsCustomVerts) {
          clearInterval(filledChecker);
          fillUnits();
        } else {
          return;
        }
      }, 300)
    })
  })
</script>

  </ad-unit>

<p>Between ads</p>

<!-- Adunit - Adsense In-article Ad -->
<ad-unit><div class="uj-vert-unit" data-plan-id="basic" data-plan-visibility="visible">
      <ins class="adsbygoogle" style="display:block; text-align:center; " data-ad-layout="in-article" data-ad-format="fluid" data-ad-client="ca-pub-7372939085773564" data-ad-slot="7329067043"></ins>
      <script>
          (adsbygoogle = window.adsbygoogle || []).push({});
      </script>
    </div>

    <div id="vert-template" data-template="
  &lt;iframe
    id=&quot;{id}&quot;
    name=&quot;{id}&quot;
    class=&quot;lazyload&quot;
    style=&quot;max-height: 300px;&quot;
    sandbox=&quot;allow-forms allow-popups allow-popups-to-escape-sandbox allow-same-origin allow-scripts allow-top-navigation-by-user-activation&quot;
    width=&quot;100%&quot; height=&quot;100%&quot;
    frameborder=&quot;0&quot; marginwidth=&quot;0&quot; marginheight=&quot;0&quot; vspace=&quot;0&quot; hspace=&quot;0&quot; allowtransparency=&quot;true&quot; scrolling=&quot;no&quot;
    data-src=&quot;{src}&quot;
  &gt;&lt;/iframe&gt;
">
</div>

<script async="" defer="" type="text/javascript">
  Manager.ready(function () {
    var select = Manager.dom().select;
    var $adblockerDetector = select('#uj-antivert-detector');

    var $iframeVertTemplate = select('#vert-template').get(0).getAttribute('data-template');

    var qsDebug = new URLSearchParams(window.location.search).get('debug');

    var slotId = new Date().getTime();

    // Selectors
    var vertSlotsSelector = '.uj-vert-unit';
    var vertSlotsSelectorLoaded = '.uj-vert-unit ins[data-ad-status]';
    var vertSlotsSelectorUnfilled = '.uj-vert-unit ins[data-ad-status="unfilled"]';

    // Set the anti-vert detector script
    /*
      /bottomads.js
      /content-ads.js
      /core-ads.js
      /insertads.js
      /jsads.js
      /left_ads.js
      /nativeads.js
      /openads.js
      /popupads.js
      /ads/ad-script.js?ad_network=123&tracking_id=abc#banner_ad
    */
    // var scriptUrl = '/assets/js/taboola_ads.js';
    // var scriptUrl = 'assets/js/pagead.js';
    var scriptUrl = '/assets/js/popupads.js?t1=/ads/native.js&t2=/nativeads.js&t3=/content-ads.js&cb=' + Manager.properties.global.cacheBreaker;

    // Check if being handled already
    if (window._ujVertCheckerStarted) {
      return;
    }
    window._ujVertCheckerStarted = true;

    // Message Handler
    function messageHandler(event) {
      var message = event.data || {};
      var command = message.command || '';
      var payload = message.payload || {};

      // Log
      console.log('Message', command, message, event.source);

      // Handle the message
      if (command === 'uj-vert-unit:set-dimensions') {
        // Get the iframe
        var $iframe = select('#uj-vert-' + payload.id).get(0);

        // Set the dimensions
        $iframe.style.height = payload.height + 'px';
      } else if (command === 'uj-vert-unit:click') {
        // Navigate to the URL
        window.location.href = payload.url;
      }
    }

    // Fill the units
    function fillUnits() {
      var $vertSlots = select(vertSlotsSelector);
      var adURL = qsDebug === 'true'
        ? 'http://localhost:4001/verts/main'
        : 'https://promo-server.itwcreativeworks.com/verts/main'

      adURL = new URL(adURL);

      adURL.searchParams.set('parentURL', window.location.href);

      // Load the ad units
      $vertSlots.each(function ($el) {
        var isFilled = $el.matches('[data-ad-status="filled"]');
        var finalURL = new URL(adURL.toString());

        finalURL.searchParams.set('frameId', slotId);

        finalURL = finalURL.toString();

        if (isFilled) {
          return;
        }

        console.log('Loading ad unit', $el, finalURL);

        $el.innerHTML = $iframeVertTemplate
          .replace(/{id}/g, 'uj-vert-' + slotId++)
          .replace(/{src}/g, finalURL)
      })
    }

    window.addEventListener('message', messageHandler, false);

    // Load the antivert checker
    Manager.dom().loadScript({src: scriptUrl})
    .then(function (e) {
      console.log('Loaded antivert checker', e);
    })
    .catch(function (e) {
      console.error('Failed to load antivert checker', e);
    })
    .finally(function () {
      var filledChecker = setInterval(function () {
        // Check if there are unfilled units
        var isOGVertsLoaded = select(vertSlotsSelectorLoaded).elements.exists;
        var isUnfilledVertSlots = select(vertSlotsSelectorUnfilled).elements.exists;

        // Set the global adblocker enabled flag
        Manager.properties.global.isAdblockerEnabled = !select('#uj-antivert-detector').elements.exists;

        var needsCustomVerts = isUnfilledVertSlots
          || Manager.properties.global.isAdblockerEnabled
          || Manager.properties.meta.environment === 'development';

        console.log(
          'isAdblockerEnabled', Manager.properties.global.isAdblockerEnabled,
          'isOGVertsLoaded', isOGVertsLoaded,
          'isUnfilledVertSlots', isUnfilledVertSlots,
          'needsCustomVerts', needsCustomVerts,
        );

        if (!Manager.properties.global.isAdblockerEnabled && !isOGVertsLoaded) {
          return;
        }

        if (isOGVertsLoaded && !isUnfilledVertSlots) {
          return clearInterval(filledChecker);
        }

        if (needsCustomVerts) {
          clearInterval(filledChecker);
          fillUnits();
        } else {
          return;
        }
      }, 300)
    })
  })
</script>

  </ad-unit>

<p>After ads</p>
",
        "test_1_2": "<h2 id="title-inside-post-1">Title inside post 1</h2>
<p>This is a wonderful paragrah inside a post! This is post #1 with ID 1722471521.</p>

<p>Before ads</p>

<!-- Adunit - Adsense In-article Ad --><p>Between ads</p>

<!-- Adunit - Adsense In-article Ad --><p>After ads</p>
",
        "test_1_3": "<h2 id=\"title-inside-post-1\">Title inside post 1</h2> <p>This is a wonderful paragrah inside a post! This is post #1 with ID 1722471521.</p>  <p>Before ads</p>  <!-- Adunit - Adsense In-article Ad --> <p>Between ads</p>  <!-- Adunit - Adsense In-article Ad --> <p>After ads</p> ",


        "test_2": "<h2 id="title-inside-post-1">Title inside post 1</h2>
<p>This is a wonderful paragrah inside a post! This is post #1 with ID 1722471521.</p>

<p>Before ads</p>

<!-- Adunit - Adsense In-article Ad -->
<ad-unit><div class="uj-vert-unit" data-plan-id="basic" data-plan-visibility="visible">
      <ins class="adsbygoogle" style="display:block; text-align:center; " data-ad-layout="in-article" data-ad-format="fluid" data-ad-client="ca-pub-7372939085773564" data-ad-slot="7329067043"></ins>
      <script>
          (adsbygoogle = window.adsbygoogle || []).push({});
      </script>
    </div>

    <div id="vert-template" data-template="
  &lt;iframe
    id=&quot;{id}&quot;
    name=&quot;{id}&quot;
    class=&quot;lazyload&quot;
    style=&quot;max-height: 300px;&quot;
    sandbox=&quot;allow-forms allow-popups allow-popups-to-escape-sandbox allow-same-origin allow-scripts allow-top-navigation-by-user-activation&quot;
    width=&quot;100%&quot; height=&quot;100%&quot;
    frameborder=&quot;0&quot; marginwidth=&quot;0&quot; marginheight=&quot;0&quot; vspace=&quot;0&quot; hspace=&quot;0&quot; allowtransparency=&quot;true&quot; scrolling=&quot;no&quot;
    data-src=&quot;{src}&quot;
  &gt;&lt;/iframe&gt;
">
</div>

<script async="" defer="" type="text/javascript">
  Manager.ready(function () {
    var select = Manager.dom().select;
    var $adblockerDetector = select('#uj-antivert-detector');

    var $iframeVertTemplate = select('#vert-template').get(0).getAttribute('data-template');

    var qsDebug = new URLSearchParams(window.location.search).get('debug');

    var slotId = new Date().getTime();

    // Selectors
    var vertSlotsSelector = '.uj-vert-unit';
    var vertSlotsSelectorLoaded = '.uj-vert-unit ins[data-ad-status]';
    var vertSlotsSelectorUnfilled = '.uj-vert-unit ins[data-ad-status="unfilled"]';

    // Set the anti-vert detector script
    /*
      /bottomads.js
      /content-ads.js
      /core-ads.js
      /insertads.js
      /jsads.js
      /left_ads.js
      /nativeads.js
      /openads.js
      /popupads.js
      /ads/ad-script.js?ad_network=123&tracking_id=abc#banner_ad
    */
    // var scriptUrl = '/assets/js/taboola_ads.js';
    // var scriptUrl = 'assets/js/pagead.js';
    var scriptUrl = '/assets/js/popupads.js?t1=/ads/native.js&t2=/nativeads.js&t3=/content-ads.js&cb=' + Manager.properties.global.cacheBreaker;

    // Check if being handled already
    if (window._ujVertCheckerStarted) {
      return;
    }
    window._ujVertCheckerStarted = true;

    // Message Handler
    function messageHandler(event) {
      var message = event.data || {};
      var command = message.command || '';
      var payload = message.payload || {};

      // Log
      console.log('Message', command, message, event.source);

      // Handle the message
      if (command === 'uj-vert-unit:set-dimensions') {
        // Get the iframe
        var $iframe = select('#uj-vert-' + payload.id).get(0);

        // Set the dimensions
        $iframe.style.height = payload.height + 'px';
      } else if (command === 'uj-vert-unit:click') {
        // Navigate to the URL
        window.location.href = payload.url;
      }
    }

    // Fill the units
    function fillUnits() {
      var $vertSlots = select(vertSlotsSelector);
      var adURL = qsDebug === 'true'
        ? 'http://localhost:4001/verts/main'
        : 'https://promo-server.itwcreativeworks.com/verts/main'

      adURL = new URL(adURL);

      adURL.searchParams.set('parentURL', window.location.href);

      // Load the ad units
      $vertSlots.each(function ($el) {
        var isFilled = $el.matches('[data-ad-status="filled"]');
        var finalURL = new URL(adURL.toString());

        finalURL.searchParams.set('frameId', slotId);

        finalURL = finalURL.toString();

        if (isFilled) {
          return;
        }

        console.log('Loading ad unit', $el, finalURL);

        $el.innerHTML = $iframeVertTemplate
          .replace(/{id}/g, 'uj-vert-' + slotId++)
          .replace(/{src}/g, finalURL)
      })
    }

    window.addEventListener('message', messageHandler, false);

    // Load the antivert checker
    Manager.dom().loadScript({src: scriptUrl})
    .then(function (e) {
      console.log('Loaded antivert checker', e);
    })
    .catch(function (e) {
      console.error('Failed to load antivert checker', e);
    })
    .finally(function () {
      var filledChecker = setInterval(function () {
        // Check if there are unfilled units
        var isOGVertsLoaded = select(vertSlotsSelectorLoaded).elements.exists;
        var isUnfilledVertSlots = select(vertSlotsSelectorUnfilled).elements.exists;

        // Set the global adblocker enabled flag
        Manager.properties.global.isAdblockerEnabled = !select('#uj-antivert-detector').elements.exists;

        var needsCustomVerts = isUnfilledVertSlots
          || Manager.properties.global.isAdblockerEnabled
          || Manager.properties.meta.environment === 'development';

        console.log(
          'isAdblockerEnabled', Manager.properties.global.isAdblockerEnabled,
          'isOGVertsLoaded', isOGVertsLoaded,
          'isUnfilledVertSlots', isUnfilledVertSlots,
          'needsCustomVerts', needsCustomVerts,
        );

        if (!Manager.properties.global.isAdblockerEnabled && !isOGVertsLoaded) {
          return;
        }

        if (isOGVertsLoaded && !isUnfilledVertSlots) {
          return clearInterval(filledChecker);
        }

        if (needsCustomVerts) {
          clearInterval(filledChecker);
          fillUnits();
        } else {
          return;
        }
      }, 300)
    })
  })
</script>

  </ad-unit>

<p>Between ads</p>

<!-- Adunit - Adsense In-article Ad -->
<ad-unit><div class="uj-vert-unit" data-plan-id="basic" data-plan-visibility="visible">
      <ins class="adsbygoogle" style="display:block; text-align:center; " data-ad-layout="in-article" data-ad-format="fluid" data-ad-client="ca-pub-7372939085773564" data-ad-slot="7329067043"></ins>
      <script>
          (adsbygoogle = window.adsbygoogle || []).push({});
      </script>
    </div>

    <div id="vert-template" data-template="
  &lt;iframe
    id=&quot;{id}&quot;
    name=&quot;{id}&quot;
    class=&quot;lazyload&quot;
    style=&quot;max-height: 300px;&quot;
    sandbox=&quot;allow-forms allow-popups allow-popups-to-escape-sandbox allow-same-origin allow-scripts allow-top-navigation-by-user-activation&quot;
    width=&quot;100%&quot; height=&quot;100%&quot;
    frameborder=&quot;0&quot; marginwidth=&quot;0&quot; marginheight=&quot;0&quot; vspace=&quot;0&quot; hspace=&quot;0&quot; allowtransparency=&quot;true&quot; scrolling=&quot;no&quot;
    data-src=&quot;{src}&quot;
  &gt;&lt;/iframe&gt;
">
</div>

<script async="" defer="" type="text/javascript">
  Manager.ready(function () {
    var select = Manager.dom().select;
    var $adblockerDetector = select('#uj-antivert-detector');

    var $iframeVertTemplate = select('#vert-template').get(0).getAttribute('data-template');

    var qsDebug = new URLSearchParams(window.location.search).get('debug');

    var slotId = new Date().getTime();

    // Selectors
    var vertSlotsSelector = '.uj-vert-unit';
    var vertSlotsSelectorLoaded = '.uj-vert-unit ins[data-ad-status]';
    var vertSlotsSelectorUnfilled = '.uj-vert-unit ins[data-ad-status="unfilled"]';

    // Set the anti-vert detector script
    /*
      /bottomads.js
      /content-ads.js
      /core-ads.js
      /insertads.js
      /jsads.js
      /left_ads.js
      /nativeads.js
      /openads.js
      /popupads.js
      /ads/ad-script.js?ad_network=123&tracking_id=abc#banner_ad
    */
    // var scriptUrl = '/assets/js/taboola_ads.js';
    // var scriptUrl = 'assets/js/pagead.js';
    var scriptUrl = '/assets/js/popupads.js?t1=/ads/native.js&t2=/nativeads.js&t3=/content-ads.js&cb=' + Manager.properties.global.cacheBreaker;

    // Check if being handled already
    if (window._ujVertCheckerStarted) {
      return;
    }
    window._ujVertCheckerStarted = true;

    // Message Handler
    function messageHandler(event) {
      var message = event.data || {};
      var command = message.command || '';
      var payload = message.payload || {};

      // Log
      console.log('Message', command, message, event.source);

      // Handle the message
      if (command === 'uj-vert-unit:set-dimensions') {
        // Get the iframe
        var $iframe = select('#uj-vert-' + payload.id).get(0);

        // Set the dimensions
        $iframe.style.height = payload.height + 'px';
      } else if (command === 'uj-vert-unit:click') {
        // Navigate to the URL
        window.location.href = payload.url;
      }
    }

    // Fill the units
    function fillUnits() {
      var $vertSlots = select(vertSlotsSelector);
      var adURL = qsDebug === 'true'
        ? 'http://localhost:4001/verts/main'
        : 'https://promo-server.itwcreativeworks.com/verts/main'

      adURL = new URL(adURL);

      adURL.searchParams.set('parentURL', window.location.href);

      // Load the ad units
      $vertSlots.each(function ($el) {
        var isFilled = $el.matches('[data-ad-status="filled"]');
        var finalURL = new URL(adURL.toString());

        finalURL.searchParams.set('frameId', slotId);

        finalURL = finalURL.toString();

        if (isFilled) {
          return;
        }

        console.log('Loading ad unit', $el, finalURL);

        $el.innerHTML = $iframeVertTemplate
          .replace(/{id}/g, 'uj-vert-' + slotId++)
          .replace(/{src}/g, finalURL)
      })
    }

    window.addEventListener('message', messageHandler, false);

    // Load the antivert checker
    Manager.dom().loadScript({src: scriptUrl})
    .then(function (e) {
      console.log('Loaded antivert checker', e);
    })
    .catch(function (e) {
      console.error('Failed to load antivert checker', e);
    })
    .finally(function () {
      var filledChecker = setInterval(function () {
        // Check if there are unfilled units
        var isOGVertsLoaded = select(vertSlotsSelectorLoaded).elements.exists;
        var isUnfilledVertSlots = select(vertSlotsSelectorUnfilled).elements.exists;

        // Set the global adblocker enabled flag
        Manager.properties.global.isAdblockerEnabled = !select('#uj-antivert-detector').elements.exists;

        var needsCustomVerts = isUnfilledVertSlots
          || Manager.properties.global.isAdblockerEnabled
          || Manager.properties.meta.environment === 'development';

        console.log(
          'isAdblockerEnabled', Manager.properties.global.isAdblockerEnabled,
          'isOGVertsLoaded', isOGVertsLoaded,
          'isUnfilledVertSlots', isUnfilledVertSlots,
          'needsCustomVerts', needsCustomVerts,
        );

        if (!Manager.properties.global.isAdblockerEnabled && !isOGVertsLoaded) {
          return;
        }

        if (isOGVertsLoaded && !isUnfilledVertSlots) {
          return clearInterval(filledChecker);
        }

        if (needsCustomVerts) {
          clearInterval(filledChecker);
          fillUnits();
        } else {
          return;
        }
      }, 300)
    })
  })
</script>

  </ad-unit>

<p>After ads</p>
",
        "test_3": "<h2 id="title-inside-post-1">Title inside post 1</h2>
<p>This is a wonderful paragrah inside a post! This is post #1 with ID 1722471521.</p>

<p>Before ads</p>

<!-- Adunit - Adsense In-article Ad -->
<ad-unit><div class="uj-vert-unit" data-plan-id="basic" data-plan-visibility="visible">
      <ins class="adsbygoogle" style="display:block; text-align:center; " data-ad-layout="in-article" data-ad-format="fluid" data-ad-client="ca-pub-7372939085773564" data-ad-slot="7329067043"></ins>
      <script>
          (adsbygoogle = window.adsbygoogle || []).push({});
      </script>
    </div>

    <div id="vert-template" data-template="
  &lt;iframe
    id=&quot;{id}&quot;
    name=&quot;{id}&quot;
    class=&quot;lazyload&quot;
    style=&quot;max-height: 300px;&quot;
    sandbox=&quot;allow-forms allow-popups allow-popups-to-escape-sandbox allow-same-origin allow-scripts allow-top-navigation-by-user-activation&quot;
    width=&quot;100%&quot; height=&quot;100%&quot;
    frameborder=&quot;0&quot; marginwidth=&quot;0&quot; marginheight=&quot;0&quot; vspace=&quot;0&quot; hspace=&quot;0&quot; allowtransparency=&quot;true&quot; scrolling=&quot;no&quot;
    data-src=&quot;{src}&quot;
  &gt;&lt;/iframe&gt;
">
</div>

<script async="" defer="" type="text/javascript">
  Manager.ready(function () {
    var select = Manager.dom().select;
    var $adblockerDetector = select('#uj-antivert-detector');

    var $iframeVertTemplate = select('#vert-template').get(0).getAttribute('data-template');

    var qsDebug = new URLSearchParams(window.location.search).get('debug');

    var slotId = new Date().getTime();

    // Selectors
    var vertSlotsSelector = '.uj-vert-unit';
    var vertSlotsSelectorLoaded = '.uj-vert-unit ins[data-ad-status]';
    var vertSlotsSelectorUnfilled = '.uj-vert-unit ins[data-ad-status="unfilled"]';

    // Set the anti-vert detector script
    /*
      /bottomads.js
      /content-ads.js
      /core-ads.js
      /insertads.js
      /jsads.js
      /left_ads.js
      /nativeads.js
      /openads.js
      /popupads.js
      /ads/ad-script.js?ad_network=123&tracking_id=abc#banner_ad
    */
    // var scriptUrl = '/assets/js/taboola_ads.js';
    // var scriptUrl = 'assets/js/pagead.js';
    var scriptUrl = '/assets/js/popupads.js?t1=/ads/native.js&t2=/nativeads.js&t3=/content-ads.js&cb=' + Manager.properties.global.cacheBreaker;

    // Check if being handled already
    if (window._ujVertCheckerStarted) {
      return;
    }
    window._ujVertCheckerStarted = true;

    // Message Handler
    function messageHandler(event) {
      var message = event.data || {};
      var command = message.command || '';
      var payload = message.payload || {};

      // Log
      console.log('Message', command, message, event.source);

      // Handle the message
      if (command === 'uj-vert-unit:set-dimensions') {
        // Get the iframe
        var $iframe = select('#uj-vert-' + payload.id).get(0);

        // Set the dimensions
        $iframe.style.height = payload.height + 'px';
      } else if (command === 'uj-vert-unit:click') {
        // Navigate to the URL
        window.location.href = payload.url;
      }
    }

    // Fill the units
    function fillUnits() {
      var $vertSlots = select(vertSlotsSelector);
      var adURL = qsDebug === 'true'
        ? 'http://localhost:4001/verts/main'
        : 'https://promo-server.itwcreativeworks.com/verts/main'

      adURL = new URL(adURL);

      adURL.searchParams.set('parentURL', window.location.href);

      // Load the ad units
      $vertSlots.each(function ($el) {
        var isFilled = $el.matches('[data-ad-status="filled"]');
        var finalURL = new URL(adURL.toString());

        finalURL.searchParams.set('frameId', slotId);

        finalURL = finalURL.toString();

        if (isFilled) {
          return;
        }

        console.log('Loading ad unit', $el, finalURL);

        $el.innerHTML = $iframeVertTemplate
          .replace(/{id}/g, 'uj-vert-' + slotId++)
          .replace(/{src}/g, finalURL)
      })
    }

    window.addEventListener('message', messageHandler, false);

    // Load the antivert checker
    Manager.dom().loadScript({src: scriptUrl})
    .then(function (e) {
      console.log('Loaded antivert checker', e);
    })
    .catch(function (e) {
      console.error('Failed to load antivert checker', e);
    })
    .finally(function () {
      var filledChecker = setInterval(function () {
        // Check if there are unfilled units
        var isOGVertsLoaded = select(vertSlotsSelectorLoaded).elements.exists;
        var isUnfilledVertSlots = select(vertSlotsSelectorUnfilled).elements.exists;

        // Set the global adblocker enabled flag
        Manager.properties.global.isAdblockerEnabled = !select('#uj-antivert-detector').elements.exists;

        var needsCustomVerts = isUnfilledVertSlots
          || Manager.properties.global.isAdblockerEnabled
          || Manager.properties.meta.environment === 'development';

        console.log(
          'isAdblockerEnabled', Manager.properties.global.isAdblockerEnabled,
          'isOGVertsLoaded', isOGVertsLoaded,
          'isUnfilledVertSlots', isUnfilledVertSlots,
          'needsCustomVerts', needsCustomVerts,
        );

        if (!Manager.properties.global.isAdblockerEnabled && !isOGVertsLoaded) {
          return;
        }

        if (isOGVertsLoaded && !isUnfilledVertSlots) {
          return clearInterval(filledChecker);
        }

        if (needsCustomVerts) {
          clearInterval(filledChecker);
          fillUnits();
        } else {
          return;
        }
      }, 300)
    })
  })
</script>

  </ad-unit>

<p>Between ads</p>

<!-- Adunit - Adsense In-article Ad -->
<ad-unit><div class="uj-vert-unit" data-plan-id="basic" data-plan-visibility="visible">
      <ins class="adsbygoogle" style="display:block; text-align:center; " data-ad-layout="in-article" data-ad-format="fluid" data-ad-client="ca-pub-7372939085773564" data-ad-slot="7329067043"></ins>
      <script>
          (adsbygoogle = window.adsbygoogle || []).push({});
      </script>
    </div>

    <div id="vert-template" data-template="
  &lt;iframe
    id=&quot;{id}&quot;
    name=&quot;{id}&quot;
    class=&quot;lazyload&quot;
    style=&quot;max-height: 300px;&quot;
    sandbox=&quot;allow-forms allow-popups allow-popups-to-escape-sandbox allow-same-origin allow-scripts allow-top-navigation-by-user-activation&quot;
    width=&quot;100%&quot; height=&quot;100%&quot;
    frameborder=&quot;0&quot; marginwidth=&quot;0&quot; marginheight=&quot;0&quot; vspace=&quot;0&quot; hspace=&quot;0&quot; allowtransparency=&quot;true&quot; scrolling=&quot;no&quot;
    data-src=&quot;{src}&quot;
  &gt;&lt;/iframe&gt;
">
</div>

<script async="" defer="" type="text/javascript">
  Manager.ready(function () {
    var select = Manager.dom().select;
    var $adblockerDetector = select('#uj-antivert-detector');

    var $iframeVertTemplate = select('#vert-template').get(0).getAttribute('data-template');

    var qsDebug = new URLSearchParams(window.location.search).get('debug');

    var slotId = new Date().getTime();

    // Selectors
    var vertSlotsSelector = '.uj-vert-unit';
    var vertSlotsSelectorLoaded = '.uj-vert-unit ins[data-ad-status]';
    var vertSlotsSelectorUnfilled = '.uj-vert-unit ins[data-ad-status="unfilled"]';

    // Set the anti-vert detector script
    /*
      /bottomads.js
      /content-ads.js
      /core-ads.js
      /insertads.js
      /jsads.js
      /left_ads.js
      /nativeads.js
      /openads.js
      /popupads.js
      /ads/ad-script.js?ad_network=123&tracking_id=abc#banner_ad
    */
    // var scriptUrl = '/assets/js/taboola_ads.js';
    // var scriptUrl = 'assets/js/pagead.js';
    var scriptUrl = '/assets/js/popupads.js?t1=/ads/native.js&t2=/nativeads.js&t3=/content-ads.js&cb=' + Manager.properties.global.cacheBreaker;

    // Check if being handled already
    if (window._ujVertCheckerStarted) {
      return;
    }
    window._ujVertCheckerStarted = true;

    // Message Handler
    function messageHandler(event) {
      var message = event.data || {};
      var command = message.command || '';
      var payload = message.payload || {};

      // Log
      console.log('Message', command, message, event.source);

      // Handle the message
      if (command === 'uj-vert-unit:set-dimensions') {
        // Get the iframe
        var $iframe = select('#uj-vert-' + payload.id).get(0);

        // Set the dimensions
        $iframe.style.height = payload.height + 'px';
      } else if (command === 'uj-vert-unit:click') {
        // Navigate to the URL
        window.location.href = payload.url;
      }
    }

    // Fill the units
    function fillUnits() {
      var $vertSlots = select(vertSlotsSelector);
      var adURL = qsDebug === 'true'
        ? 'http://localhost:4001/verts/main'
        : 'https://promo-server.itwcreativeworks.com/verts/main'

      adURL = new URL(adURL);

      adURL.searchParams.set('parentURL', window.location.href);

      // Load the ad units
      $vertSlots.each(function ($el) {
        var isFilled = $el.matches('[data-ad-status="filled"]');
        var finalURL = new URL(adURL.toString());

        finalURL.searchParams.set('frameId', slotId);

        finalURL = finalURL.toString();

        if (isFilled) {
          return;
        }

        console.log('Loading ad unit', $el, finalURL);

        $el.innerHTML = $iframeVertTemplate
          .replace(/{id}/g, 'uj-vert-' + slotId++)
          .replace(/{src}/g, finalURL)
      })
    }

    window.addEventListener('message', messageHandler, false);

    // Load the antivert checker
    Manager.dom().loadScript({src: scriptUrl})
    .then(function (e) {
      console.log('Loaded antivert checker', e);
    })
    .catch(function (e) {
      console.error('Failed to load antivert checker', e);
    })
    .finally(function () {
      var filledChecker = setInterval(function () {
        // Check if there are unfilled units
        var isOGVertsLoaded = select(vertSlotsSelectorLoaded).elements.exists;
        var isUnfilledVertSlots = select(vertSlotsSelectorUnfilled).elements.exists;

        // Set the global adblocker enabled flag
        Manager.properties.global.isAdblockerEnabled = !select('#uj-antivert-detector').elements.exists;

        var needsCustomVerts = isUnfilledVertSlots
          || Manager.properties.global.isAdblockerEnabled
          || Manager.properties.meta.environment === 'development';

        console.log(
          'isAdblockerEnabled', Manager.properties.global.isAdblockerEnabled,
          'isOGVertsLoaded', isOGVertsLoaded,
          'isUnfilledVertSlots', isUnfilledVertSlots,
          'needsCustomVerts', needsCustomVerts,
        );

        if (!Manager.properties.global.isAdblockerEnabled && !isOGVertsLoaded) {
          return;
        }

        if (isOGVertsLoaded && !isUnfilledVertSlots) {
          return clearInterval(filledChecker);
        }

        if (needsCustomVerts) {
          clearInterval(filledChecker);
          fillUnits();
        } else {
          return;
        }
      }, 300)
    })
  })
</script>

  </ad-unit>

<p>After ads</p>
",
        "test_4": "<h2 id="title-inside-post-1">Title inside post 1</h2>
<p>This is a wonderful paragrah inside a post! This is post #1 with ID 1722471521.</p>

<p>Before ads</p>

<!-- Adunit - Adsense In-article Ad --><p>Between ads</p>

<!-- Adunit - Adsense In-article Ad --><p>After ads</p>
",
        "test_5": "<h2 id=\"title-inside-post-1\">Title inside post 1</h2>\n<p>This is a wonderful paragrah inside a post! This is post #1 with ID 1722471521.</p>\n\n<p>Before ads</p>\n\n<!-- Adunit - Adsense In-article Ad --><p>Between ads</p>\n\n<!-- Adunit - Adsense In-article Ad --><p>After ads</p>\n",

        "test_6": "<h2 id="title-inside-post-1">Title inside post 1</h2>
<p>This is a wonderful paragrah inside a post! This is post #1 with ID 1722471521.</p>

<p>Before ads</p>

<!-- Adunit - Adsense In-article Ad -->
<ad-unit><div class="uj-vert-unit" data-plan-id="basic" data-plan-visibility="visible">
      <ins class="adsbygoogle" style="display:block; text-align:center; " data-ad-layout="in-article" data-ad-format="fluid" data-ad-client="ca-pub-7372939085773564" data-ad-slot="7329067043"></ins>
      <script>
          (adsbygoogle = window.adsbygoogle || []).push({});
      </script>
    </div>

    <div id="vert-template" data-template="
  &lt;iframe
    id=&quot;{id}&quot;
    name=&quot;{id}&quot;
    class=&quot;lazyload&quot;
    style=&quot;max-height: 300px;&quot;
    sandbox=&quot;allow-forms allow-popups allow-popups-to-escape-sandbox allow-same-origin allow-scripts allow-top-navigation-by-user-activation&quot;
    width=&quot;100%&quot; height=&quot;100%&quot;
    frameborder=&quot;0&quot; marginwidth=&quot;0&quot; marginheight=&quot;0&quot; vspace=&quot;0&quot; hspace=&quot;0&quot; allowtransparency=&quot;true&quot; scrolling=&quot;no&quot;
    data-src=&quot;{src}&quot;
  &gt;&lt;/iframe&gt;
">
</div>

<script async="" defer="" type="text/javascript">
  Manager.ready(function () {
    var select = Manager.dom().select;
    var $adblockerDetector = select('#uj-antivert-detector');

    var $iframeVertTemplate = select('#vert-template').get(0).getAttribute('data-template');

    var qsDebug = new URLSearchParams(window.location.search).get('debug');

    var slotId = new Date().getTime();

    // Selectors
    var vertSlotsSelector = '.uj-vert-unit';
    var vertSlotsSelectorLoaded = '.uj-vert-unit ins[data-ad-status]';
    var vertSlotsSelectorUnfilled = '.uj-vert-unit ins[data-ad-status="unfilled"]';

    // Set the anti-vert detector script
    /*
      /bottomads.js
      /content-ads.js
      /core-ads.js
      /insertads.js
      /jsads.js
      /left_ads.js
      /nativeads.js
      /openads.js
      /popupads.js
      /ads/ad-script.js?ad_network=123&tracking_id=abc#banner_ad
    */
    // var scriptUrl = '/assets/js/taboola_ads.js';
    // var scriptUrl = 'assets/js/pagead.js';
    var scriptUrl = '/assets/js/popupads.js?t1=/ads/native.js&t2=/nativeads.js&t3=/content-ads.js&cb=' + Manager.properties.global.cacheBreaker;

    // Check if being handled already
    if (window._ujVertCheckerStarted) {
      return;
    }
    window._ujVertCheckerStarted = true;

    // Message Handler
    function messageHandler(event) {
      var message = event.data || {};
      var command = message.command || '';
      var payload = message.payload || {};

      // Log
      console.log('Message', command, message, event.source);

      // Handle the message
      if (command === 'uj-vert-unit:set-dimensions') {
        // Get the iframe
        var $iframe = select('#uj-vert-' + payload.id).get(0);

        // Set the dimensions
        $iframe.style.height = payload.height + 'px';
      } else if (command === 'uj-vert-unit:click') {
        // Navigate to the URL
        window.location.href = payload.url;
      }
    }

    // Fill the units
    function fillUnits() {
      var $vertSlots = select(vertSlotsSelector);
      var adURL = qsDebug === 'true'
        ? 'http://localhost:4001/verts/main'
        : 'https://promo-server.itwcreativeworks.com/verts/main'

      adURL = new URL(adURL);

      adURL.searchParams.set('parentURL', window.location.href);

      // Load the ad units
      $vertSlots.each(function ($el) {
        var isFilled = $el.matches('[data-ad-status="filled"]');
        var finalURL = new URL(adURL.toString());

        finalURL.searchParams.set('frameId', slotId);

        finalURL = finalURL.toString();

        if (isFilled) {
          return;
        }

        console.log('Loading ad unit', $el, finalURL);

        $el.innerHTML = $iframeVertTemplate
          .replace(/{id}/g, 'uj-vert-' + slotId++)
          .replace(/{src}/g, finalURL)
      })
    }

    window.addEventListener('message', messageHandler, false);

    // Load the antivert checker
    Manager.dom().loadScript({src: scriptUrl})
    .then(function (e) {
      console.log('Loaded antivert checker', e);
    })
    .catch(function (e) {
      console.error('Failed to load antivert checker', e);
    })
    .finally(function () {
      var filledChecker = setInterval(function () {
        // Check if there are unfilled units
        var isOGVertsLoaded = select(vertSlotsSelectorLoaded).elements.exists;
        var isUnfilledVertSlots = select(vertSlotsSelectorUnfilled).elements.exists;

        // Set the global adblocker enabled flag
        Manager.properties.global.isAdblockerEnabled = !select('#uj-antivert-detector').elements.exists;

        var needsCustomVerts = isUnfilledVertSlots
          || Manager.properties.global.isAdblockerEnabled
          || Manager.properties.meta.environment === 'development';

        console.log(
          'isAdblockerEnabled', Manager.properties.global.isAdblockerEnabled,
          'isOGVertsLoaded', isOGVertsLoaded,
          'isUnfilledVertSlots', isUnfilledVertSlots,
          'needsCustomVerts', needsCustomVerts,
        );

        if (!Manager.properties.global.isAdblockerEnabled && !isOGVertsLoaded) {
          return;
        }

        if (isOGVertsLoaded && !isUnfilledVertSlots) {
          return clearInterval(filledChecker);
        }

        if (needsCustomVerts) {
          clearInterval(filledChecker);
          fillUnits();
        } else {
          return;
        }
      }, 300)
    })
  })
</script>

  </ad-unit>

<p>Between ads</p>

<!-- Adunit - Adsense In-article Ad -->
<ad-unit><div class="uj-vert-unit" data-plan-id="basic" data-plan-visibility="visible">
      <ins class="adsbygoogle" style="display:block; text-align:center; " data-ad-layout="in-article" data-ad-format="fluid" data-ad-client="ca-pub-7372939085773564" data-ad-slot="7329067043"></ins>
      <script>
          (adsbygoogle = window.adsbygoogle || []).push({});
      </script>
    </div>

    <div id="vert-template" data-template="
  &lt;iframe
    id=&quot;{id}&quot;
    name=&quot;{id}&quot;
    class=&quot;lazyload&quot;
    style=&quot;max-height: 300px;&quot;
    sandbox=&quot;allow-forms allow-popups allow-popups-to-escape-sandbox allow-same-origin allow-scripts allow-top-navigation-by-user-activation&quot;
    width=&quot;100%&quot; height=&quot;100%&quot;
    frameborder=&quot;0&quot; marginwidth=&quot;0&quot; marginheight=&quot;0&quot; vspace=&quot;0&quot; hspace=&quot;0&quot; allowtransparency=&quot;true&quot; scrolling=&quot;no&quot;
    data-src=&quot;{src}&quot;
  &gt;&lt;/iframe&gt;
">
</div>

<script async="" defer="" type="text/javascript">
  Manager.ready(function () {
    var select = Manager.dom().select;
    var $adblockerDetector = select('#uj-antivert-detector');

    var $iframeVertTemplate = select('#vert-template').get(0).getAttribute('data-template');

    var qsDebug = new URLSearchParams(window.location.search).get('debug');

    var slotId = new Date().getTime();

    // Selectors
    var vertSlotsSelector = '.uj-vert-unit';
    var vertSlotsSelectorLoaded = '.uj-vert-unit ins[data-ad-status]';
    var vertSlotsSelectorUnfilled = '.uj-vert-unit ins[data-ad-status="unfilled"]';

    // Set the anti-vert detector script
    /*
      /bottomads.js
      /content-ads.js
      /core-ads.js
      /insertads.js
      /jsads.js
      /left_ads.js
      /nativeads.js
      /openads.js
      /popupads.js
      /ads/ad-script.js?ad_network=123&tracking_id=abc#banner_ad
    */
    // var scriptUrl = '/assets/js/taboola_ads.js';
    // var scriptUrl = 'assets/js/pagead.js';
    var scriptUrl = '/assets/js/popupads.js?t1=/ads/native.js&t2=/nativeads.js&t3=/content-ads.js&cb=' + Manager.properties.global.cacheBreaker;

    // Check if being handled already
    if (window._ujVertCheckerStarted) {
      return;
    }
    window._ujVertCheckerStarted = true;

    // Message Handler
    function messageHandler(event) {
      var message = event.data || {};
      var command = message.command || '';
      var payload = message.payload || {};

      // Log
      console.log('Message', command, message, event.source);

      // Handle the message
      if (command === 'uj-vert-unit:set-dimensions') {
        // Get the iframe
        var $iframe = select('#uj-vert-' + payload.id).get(0);

        // Set the dimensions
        $iframe.style.height = payload.height + 'px';
      } else if (command === 'uj-vert-unit:click') {
        // Navigate to the URL
        window.location.href = payload.url;
      }
    }

    // Fill the units
    function fillUnits() {
      var $vertSlots = select(vertSlotsSelector);
      var adURL = qsDebug === 'true'
        ? 'http://localhost:4001/verts/main'
        : 'https://promo-server.itwcreativeworks.com/verts/main'

      adURL = new URL(adURL);

      adURL.searchParams.set('parentURL', window.location.href);

      // Load the ad units
      $vertSlots.each(function ($el) {
        var isFilled = $el.matches('[data-ad-status="filled"]');
        var finalURL = new URL(adURL.toString());

        finalURL.searchParams.set('frameId', slotId);

        finalURL = finalURL.toString();

        if (isFilled) {
          return;
        }

        console.log('Loading ad unit', $el, finalURL);

        $el.innerHTML = $iframeVertTemplate
          .replace(/{id}/g, 'uj-vert-' + slotId++)
          .replace(/{src}/g, finalURL)
      })
    }

    window.addEventListener('message', messageHandler, false);

    // Load the antivert checker
    Manager.dom().loadScript({src: scriptUrl})
    .then(function (e) {
      console.log('Loaded antivert checker', e);
    })
    .catch(function (e) {
      console.error('Failed to load antivert checker', e);
    })
    .finally(function () {
      var filledChecker = setInterval(function () {
        // Check if there are unfilled units
        var isOGVertsLoaded = select(vertSlotsSelectorLoaded).elements.exists;
        var isUnfilledVertSlots = select(vertSlotsSelectorUnfilled).elements.exists;

        // Set the global adblocker enabled flag
        Manager.properties.global.isAdblockerEnabled = !select('#uj-antivert-detector').elements.exists;

        var needsCustomVerts = isUnfilledVertSlots
          || Manager.properties.global.isAdblockerEnabled
          || Manager.properties.meta.environment === 'development';

        console.log(
          'isAdblockerEnabled', Manager.properties.global.isAdblockerEnabled,
          'isOGVertsLoaded', isOGVertsLoaded,
          'isUnfilledVertSlots', isUnfilledVertSlots,
          'needsCustomVerts', needsCustomVerts,
        );

        if (!Manager.properties.global.isAdblockerEnabled && !isOGVertsLoaded) {
          return;
        }

        if (isOGVertsLoaded && !isUnfilledVertSlots) {
          return clearInterval(filledChecker);
        }

        if (needsCustomVerts) {
          clearInterval(filledChecker);
          fillUnits();
        } else {
          return;
        }
      }, 300)
    })
  })
</script>

  </ad-unit>

<p>After ads</p>
",
        "test_7": "<h2 id="title-inside-post-1">Title inside post 1</h2>
<p>This is a wonderful paragrah inside a post! This is post #1 with ID 1722471521.</p>

<p>Before ads</p>

<!-- Adunit - Adsense In-article Ad --><p>Between ads</p>

<!-- Adunit - Adsense In-article Ad --><p>After ads</p>
",
        "test_8": "<h2 id=\"title-inside-post-1\">Title inside post 1</h2>\n<p>This is a wonderful paragrah inside a post! This is post #1 with ID 1722471521.</p>\n\n<p>Before ads</p>\n\n<!-- Adunit - Adsense In-article Ad --><p>Between ads</p>\n\n<!-- Adunit - Adsense In-article Ad --><p>After ads</p>\n",

        "authors": [
          {
            "name": "Ultimate Jekyll"
          }
        ],
        "attachments": [
          {
            "url": ""
          }
        ],
        "summary": "This is a sample excerpt for post number 1",
        "tags": ["Marketing","Business"]
      }
    
  ]
}
